# TB-02 · 基座与台地内核实施技术方案及任务序列

> 状态：待实施。编制日期：2026-09-14。本次仅交付方案，不代表内核、样板或随机池准入已完成。
> 按本次要求保存于项目根目录；作为专项施工文档，实施后将稳定机制回填权威文档，完成记录归档至 changelog。
> 规划依据：[07 地形美术](docs/plan/tech/07-terrain-art.md) §1.2、§7.4；物理语义依据：[06 地图模板](docs/plan/tech/06-terrain-templates.md) §5.3、§5.6～5.8；现状依据：[14 地形与路网](docs/current/tech/14-terrain-and-network.md)、当前源码。

## 1. 交付目标与范围

实现独立静态模板 `plateau_settlement_v1`：平缓且可建的台面、真实阻路的陡峭台缘、两个可以完整通过道路走廊的缓坡入口、坡脚可达水源。玩家可观察高处开阔用地与下行取水、采集成本之间的取舍。

“基座”在本任务中指物理高程骨架，不指 Canvas 沙盘侧壁，也不意味着重新建设已落地的阶段二生成框架。不得恢复 T1 旧版平顶压平逻辑；旧模板维持各自语义。

交付分成三个里程碑：

| 里程碑 | 完成条件 | 与后续关系 |
| --- | --- | --- |
| M1 显式模板可用 | 指定 profile 可生成；地理、路网、生存、存档与确定性门禁通过 | TB-02 内核主体完成，可供 TA-10 使用 |
| M2 实机场景通过 | Chrome 真实世界内确认入口、接地、遮挡、交互及性能；TA-10 完成必要表现问题修复 | TB-02 验证基础可读性；TA-10 单独登记表现层完成状态 |
| M3 随机池准入 | M1/M2 证据完整，单独修改随机映射并回归 | 才能默认随机抽到台地 |

本期不包含盆地/冲积扇、湖泊/瀑布、动态水文、桥梁、多层洞穴、额外矿产、建筑防御加成或强制居民迁入台面。装饰只复用既有通用生成器；新增台缘岩层、院地等交由 TA-10。R0、整个 D-B、TB-03 与 TA-09 不构成硬前置。

## 2. 源码基线与实施前提

核对入口：[geo/terrain.rs](crates/sim_core/src/geo/terrain.rs)、[geo/corridor.rs](crates/sim_core/src/geo/corridor.rs)、[terrain_network.rs](crates/sim_core/src/spatial/terrain_network.rs)、[creation_fallback.rs](crates/sim_core/src/spatial/creation_fallback.rs)。

| 当前事实 | 对实施的约束 |
| --- | --- |
| 生成器常量当前为 8 | 不沿用 07 文中残留的 4/5；实施时从当时分支当前值递增，不预约未来版本号 |
| `resolve_profile()` 随机池已有 5 张模板 | 显式模板交付期间不改变五路映射；`flat_baseline` 继续只用于诊断/降级 |
| 阶段化生成与世界级有界降级已接入 | 复用框架，不新建第二套生成重试器 |
| `TerrainFeatureKind` 当前只有 River/RiverBank/ShallowFord/SpringValley | 不能照抄规划中预留枚举值；首版优先不新增台地枚举或 FABS section |
| `generate_base_relief()` 等仍集中于体量较大的 terrain.rs | 新算法放独立模块；必要的等价拆分独立验收，新增文件遵守 800 行上限 |
| 坡度、flags 在第 6 步统一定稿；路网消费定稿地表 | 不直接把入口涂成可通行，也不在建路后改高程 |
| `corridor::route()` 返回可行路径，不保证两条不同入口路线 | 两入口需要独立约束与生产路网核验，不能运行两次同一最短路充数 |

开始代码工作前读取根规则及将修改目录的局部 AGENTS.md（sim_core、geo、spatial、ecology、sim_wasm、frontend 按影响面选择）。本方案所有新增模块、字段和数值均为拟定内容；未运行台地探针，不声称已经通过数值窗口。

## 3. 生成算法

### 3.1 独立 profile 与临时几何

拟新增 `geo/plateau.rs`，提供台地参数规划、高程采样、入口几何与局部验收辅助函数。`terrain.rs` 保留 profile 注册和阶段编排，`geo/mod.rs` 注册模块。

拟定义 `PlateauGeometry`，保存中心、局部轴向、台面半尺寸、圆角半径、抬升高度、台缘宽度、两入口中心线/核心宽度/肩部范围、坡脚水源候选及台面候选区。生成阶段通过 `GenesisScratch` 共享，不作为第二份持久高程真相源。

布局消费可采用同参数纯函数重新派生几何；必须共用同一函数、固定运算顺序，结果只用于候选定位。最终通行、高程和占地检查一律读 `TerrainMap.cells`。加载存档不按新算法重建地形；运行期若需要入口语义，应另行审查持久化契约，不能用缺失的 scratch 冒充已恢复事实。

### 3.2 圆角矩形台面与两类过渡带

世界坐标转换为台地局部坐标 `p=(u,v)`。以半尺寸 `b`、圆角半径 `r` 计算圆角矩形 SDF：

```text
q = abs(p) - (b - r)
d = length(max(q, 0)) + min(max(q.x, q.y), 0) - r
S(t) = t² × (3 - 2t), t ∈ [0,1]
z = base(p) + H × [1 - S(clamp(d / B(p), 0, 1))] + masked_noise(p)
```

`d≤0` 为台面，台缘向外过渡。`B(p)` 在普通台缘使用 `edge_band`，仅在两个入口局部替换为 `ramp_band`。首版入口固定在局部坐标相对两侧，世界方向可由独立种子规则确定；入口避开圆角，二者有明确不重叠的核心通道。

沿入口中心方向设置纵向有效范围，横向设置核心区和光滑肩部。核心区 `B=ramp_band`，肩部平滑回到 `edge_band`，有效范围外严格恢复普通台缘。禁止仅按横向距离扩大一整条无限带，造成额外开口。

采用地图模板方案初值：`H` 为 18～26m，`edge_band=0.6H`，`ramp_band=4H`，入口核心横向宽度至少 32m。smoothstep 的一维峰值梯度为 `1.5H/B`；这只用于初算，不能替代最终二维离散坡度验收。肩部横向梯度、底面倾斜、噪声以及栅格差分都会改变真实坡度。

### 3.3 分辨率、噪声与完整入口保护

- 采样间距按实际 `world_size/(grid_res-1)` 计算；当前默认间距约 6.4m，约 13m 的台缘仅跨两个网格，首轮必须验证相位偏移导致的漏崖/封路。
- 入口核心覆盖道路全宽，并额外考虑 `corridor::segment_valid()` 的保守栅格扩张；以整条胶囊走廊通过为准，不以中心线宽 32m 自动认定可用。
- 台面与入口核心降低高频噪声，接缝处使用连续掩码；普通台缘保留有限低频轮廓扰动。入口保护带内削弱轮廓扭曲，保证两个开口稳定。
- 扰动只影响实际高程，不能先在前端画出不存在的不规则台缘。TB-02 提供物理轮廓，TA-10 负责相应材质和遮挡表现。
- 若 `edge_band` 在支持分辨率下不能形成稳定阻路带，先校准 H/带宽/台面尺寸并重测；不默认提高全局网格分辨率，不降低全局通行标准。
- 世界边界必须容纳台面、入口全长、坡脚资源区与绕行带。过小世界或极端配置触发可诊断失败，由既有有界降级处理；不能裁掉一条入口后仍宣称台地成功。

### 3.4 表面与建造派生

第 2 步只完成高程和候选几何，第 6 步统一重算坡度并派生地表。陡峭台缘按既有规则产生 `RockFace` / `NO_WALK`，禁建阈值及道路最大坡度继续使用当前规则和配置；不能将台地整体标为 `RiverTerrace`。

台面必须提供至少 3 个互不重叠的完整房屋候选，通过现有 `validate_footprint()` 检查坡度、起伏、越界与地表合法性，并证明可接入入口路网。候选不能只检查中心点，也不允许修改生产建造门槛使其通过。候选区是可用土地，不预先生成房屋或写入 Agent 任务。

### 3.5 坡脚水源与生产路网

首版沿用独立清泉 POI 语义，不新增湖面或 WaterPool。可用现有 `SpringValley` 特征表达坡脚候选位置；拟为本 profile 使用固定 ID 30/31，实施前在 `validation.rs` 现有 profile 分支中核对、登记归属和顺序。特征只提供地形候选，水库存仍由生态系统生成。

在 `prepare_terrain_layout()` 相关布局路径增加仅作用于本 profile 的候选消费：至少有一个实际可交互的水源位于坡脚，并可从两个入口到达。若配置禁用水源，按明确配置错误/生存门禁失败处理，不额外补发库存。

按确定顺序建立“台面公共节点 → 入口内侧 → 入口外侧 → 坡脚资源网络”。每段复用 `corridor::route()`，提交走 `commit_terrain_path()`，拟合后再次验证完整曲线。入口锚点必须在地表上且避开肩部；通用路网后续增加捷径也不能绕过曲线校验。

“两条路线”定义为从同一台面可达区到真实坡脚水源网络，分别跨越入口 A/B；入口段互不重合，允许台面或坡脚公共路段共用。用诊断中临时屏蔽另一入口的办法分别证实 A/B 的通行能力，并核对生产车道图确实保留两条分支。屏蔽仅作用于诊断副本，不修改运行世界。

## 4. 确定性、配置与跨层契约

### 4.1 随机和降级

新布局只使用 `seed + 固定盐值 + 固定槽位` 的无状态整数哈希，或专属固定消费顺序 RNG；复用 TB-01 噪声实现，不新增共享模拟 RNG 消耗。不用系统时间、`DefaultHasher` 或依赖集合遍历顺序的抽样。

候选按固定顺序评估，次数有界；失败不能重抽世界种子。世界级失败复用 `new_seeded_with_config_bounded()`，台地主骨架不能通过“关闭结构子特征”假装修复。最终降级至 `flat_baseline` 必须记录 requested/effective profile 和失败原因。默认种子矩阵中降级成功仍计作台地生成失败。

专属检查需实际接入生产门禁链：不能仅在第 7 步计算后丢弃错误。纯几何检查可在 `validation.rs` 分发；涉及配置、道路和水源的检查在世界门禁侧执行。建议失败码包括 `PlateauRampBlocked`、`PlateauEdgeLeaking`、`PlateauBuildAreaInsufficient`、`PlateauWaterUnreachable`，再加现有 Geometry/RoadNetwork/Survival 前缀。

### 4.2 配置建议

以下为参数组建议，TB-02-01 校准后定默认值；不引入暂时无消费点的开关。

| 拟新增前端字段组 | 用途与边界 |
| --- | --- |
| `terrainPlateauHeightMin/Max` | 抬升高度抽样范围，初值 18/26m |
| `terrainPlateauHalfWidthRatio/HalfDepthRatio` | 台面相对世界尺寸，需兼顾三处房屋与入口长度；默认值由首轮探针确定 |
| `terrainPlateauCornerRadiusRatio` | 圆角，限制在合法半尺寸内 |
| `terrainPlateauEdgeBandRatio/RampBandRatio` | 初值 0.6/4.0；对应独立过渡带 |
| `terrainPlateauRampWidth`、`terrainPlateauRampShoulderWidth` | 核心宽度至少 32m、肩部宽度由完整走廊校准 |
| `terrainPlateauTopNoiseGain/RampNoiseGain/OutlineWarp` | 台面、入口噪声及轮廓扰动的有界幅度 |

每个实际新增字段同步 `config.rs` 字段及 doc 注释、前端 `config.js`、`examples/config.json`、`tools/config-check.js` 影响映射，并具有真实读取点。Rust 使用 snake_case 对应名、前端为默认值唯一来源。校验 NaN/无穷、范围倒置、零带宽、尺寸不足；不允许除零后靠高程 clamp 隐藏问题。生成用超参不散落成算法字面量；协议 ID、盐值和数学常数不作为可调玩法配置。

### 4.3 存档、快照与前端

首版优先沿用已有 cells、features、profile 字符串和静态帧，台面/入口 scratch 不入档，不为纯派生几何新增 FABS section。逐项审查以下路径：

| 层 | 接入事项 |
| --- | --- |
| profile 注册 | `terrain.rs` 显式分派、`geo/mod.rs` 重导出、配置注释/选择入口、诊断 profile 列表 |
| 静态特征 | `validation.rs` 本 profile 的 ID/kind/边界检查；泉源特征高程在定稿后正确接地 |
| 存档 | `world_save.rs` 支持新 profile 白名单，保存实际 profile；拒绝版本不匹配，不拼接旧路网与新高程 |
| 快照 | 核对 `snapshot.rs`、`world_snapshot.rs`、`snapshot_bin/encode.rs` 与 `dict.rs`、`snapshot-bin.js`、`rustworld.js` 现有通道完整传递新世界 |
| 前端 | 显式配置可创建；静态地表与已有特征可渲染；LOAD/REWIND/RESET 清缓存；空数组替换、null 增量保留 |
| 版本 | 物理生成改变递增生成器版本；random 映射改变单独处理；SAVE_FORMAT_VERSION/FABS 版本仅在实际不兼容结构变化时调整 |

若后续证明必须新增持久入口字段或地貌枚举，先扩展本契约，再在同一可交付单元同步四处快照、字典、格式及存档检查。不得把前端新增装饰当作内核物理完成证据。

## 5. 实施任务序列

任务默认顺序执行。每项完成需记录代码范围、门禁结果和未解决问题；部分实现不得提前勾选。

| 状态 / 编号 | 任务与产物 | 依赖 | 完成标准 |
| --- | --- | --- | --- |
| [x] TB-02-01 | 冻结基线和参数：记录提交、WASM 哈希、完整配置；台地尺度/入口肩部/分辨率临时原型与探针；确定上述参数默认值和支持范围 | TB-01、阶段二 | 证明同一栅格世界可同时满足陡缘与双入口，不修改既有门槛；留存初始矩阵指标 |
| [x] TB-02-02 | 模块边界与配置：新增 `plateau.rs`，必要时等价抽取现有辅助函数；新增参数一次完成四处同步及消费 | 01 | 旧显式模板物理字段与基线一致；配置检查通过；无未消费字段，无新增超长文件 |
| [x] TB-02-03 | 独立高程骨架：SDF 台面、分离过渡带、两入口、噪声保护、统一地表定稿 | 02 | 台缘真实禁行；两个完整走廊可走；台面 ≥3 个完整可建候选；旧模板不变 |
| [x] TB-02-04 | 坡脚泉源候选、生态落位和双入口生产路网接入 | 03 | 两条实际入口分支接通台面与实际水源；所有车道拟合后合法；不改变资源账本语义 |
| [x] TB-02-05 | 专属门禁和有界失败：默认/极端配置、入口退化、无水源、预算 0/1/N、降级实际 profile | 04 | 失败在发布前被捕获；无无限重试；降级不会计作台地通过；正常种子首试成功 |
| [x] TB-02-06 | 显式 profile 全链路：存读档白名单、快照审查、前端配置与缓存、应用/生成器版本、WASM 双副本 | 05 | M1：显式创建→保存→加载→回溯→换世界完整可用；仍保持原 random 映射 |
| [x] TB-02-07 | 完整物理与确定性矩阵、长期稳定、旧模板差分和性能测量；归档失败种子及指标 | 06 | §6 全部内核门禁通过，证据可复现 |
| [x] TB-02-08 | Chrome 场景验收与 TA-10 交接：固定 seed/存档、四方位/远中近/低高俯角；修复影响台地使用的表现缺陷 | 07；必要 TA-10 修复 | M2：入口能辨、实体接地、行走不穿崖、交互正常，性能预算通过；TA-10 剩余美术项独立登记 |
| [x] TB-02-09 | 随机池准入：注册第六张模板、单独记录映射变化、按当前生成器版本递增并回归 | 07、08 | M3：随机抽取可得到真实台地，所有实际 profile 可存读档，基线不入池；不要求旧 random 映射不变 |
| [x] TB-02-10 | 文档收口：06 的模板状态/机制、07 的 TB-02 状态、14 现状、配置参考生成、局部 AGENTS/代码地图/changelog、TODO | 09，或止于 M1/M2 明确登记 | 完成状态与证据匹配；未准入则明确保留 09 待办；删除临时测试脚本 |

建议工作量：内核与全链路约 10～15 人日，TA-10 表现修复另计。该估算需在 01 完成后按入口离散化结果调整；不能以到期替代验收。

## 6. 验收矩阵与通过条件

### 6.1 物理与运行门禁

| 项目 | 样本与方法 | 通过条件 |
| --- | --- | --- |
| 台地几何 | 默认配置 seed 0～59，外加台心相对栅格偏移、固定多方位原型 | 每个样本台缘存在稳定 ≥34° 阻路带；非入口区域无跨缘漏路；两入口都真实可用 |
| 入口 | 沿每个入口全长扫描完整道路走廊，含坡顶/坡脚/肩部 | 最大坡度 ≤ 当前 `terrain_max_walk_slope`，无硬禁行交叉；不把中心线检测当全宽检测 |
| 可建台面 | 用生产房屋占地参数枚举并验证候选 | ≥3 个不重叠候选，完整占地通过并能接路；台缘跨界候选被拒绝 |
| 路网 | `validate_terrain_world()`；入口 A/B 分别约束的路线检查 | 所有实际关键 POI 可达，两入口均存在生产分支；直线端点合法但曲线穿崖必须拒绝 |
| 水粮市场 | 对全部实际营地执行 `diagnose_survival()` | 水/粮/市场可达且成本在既有生存预算内，不放宽预算掩盖入口绕行 |
| 连通 | 地表通行分量与完整宽度路由分别统计 | 默认世界通行区保持单一连通分量；宽度路由门禁独立通过 |
| 配置边界 | 高度上下界、可支持网格/世界尺寸、极端噪声、非法值、重试 0/1/N | 支持配置通过；不支持配置明确拒绝或受控降级；无 NaN/越界/卡死 |
| 降级隔离 | 人为构造封堵入口/不足用地的临时候选 | 有稳定失败原因、预算受限、同种子结果一致；不发布半成品，不冒充台地 |

默认 60 种子台地验收要求 60/60 实际 profile 为台地且全部门禁通过。补充边界样本由 01 冻结清单；异常配置有界降级通过与台地几何成功分开统计。禁止挑选漂亮种子代替矩阵。

### 6.2 确定性与兼容性

- 新模板同 seed、配置、生成器版本重复生成：物理 cells/features、路网、POI 与模拟结果一致；覆盖分批 tick、暂停、存读档和回溯。
- 显式旧五模板及 `flat_baseline` 做跨构建对照。只排除明确变化的应用/生成器版本元数据；物理高程、地表、装饰抽样、路网和 POI 字段必须保持一致。
- `random` 在 09 前映射不变；09 后映射变化是预期，单独记录映射算法、候选顺序与新种子分布，避免误用全帧旧版本字节相等作准入条件。
- 保存实际生成 profile；降级世界保存/恢复实际基线。新旧不兼容档明确拒绝，不根据种子重新生成并拼回旧状态。
- READY/LOAD/REWIND/RESET 验证静态地形替换和缓存清理；跨世界字符串表仍按 `STR_TAB.start_index==0` 清除。

### 6.3 视觉与性能

必须使用 Chrome 并真正建立本地存档，验证模拟推进。至少选择一个典型与一个入口/台缘最不利种子，记录应用/生成器版本、源码与双副本 WASM 哈希、完整配置、seed/tick、存档哈希、Chrome/设备、窗口/DPR、镜头与季节。

验证四个方位、低高俯角、远中近距离：台面与坡脚高差可读，两个入口不被画成断崖，人物/道路/房屋接地；跟随人物完成上下坡取水；跨台缘道路被拒绝；选中和悬浮交互可用。TA-10 的精细台缘/材质不在内核步骤中提前勾选。

性能分别记录生成/布局耗时、路由耗时、静态快照字节、渲染/UI p50/p95 和相机旋转开销。同设备同配置同测量边界对照，绘制增量目标沿用 07 §11.3 的 ≤3ms，其他预算沿用现行性能指南；台地特有生成预算在 01 建基线时冻结。超标应优化复测，不自动借用旧任务的性能例外。

## 7. 实施交付检查与证据

后续代码交付执行与变更匹配的门禁（本次文档交付不构建、不升版）：

```bash
cargo build -p sim_wasm --target wasm32-unknown-unknown --release
cp target/wasm32-unknown-unknown/release/sim_wasm.wasm frontend/rust/sim_wasm.wasm
cp target/wasm32-unknown-unknown/release/sim_wasm.wasm frontend/sim_wasm.wasm
cargo test --lib
node tools/test-wasm.js
node tools/test-determinism.js
node tools/config-check.js
node tools/snapshot-check.js
node tools/frontend-check.js
node tools/diagnose.js --check all
node tools/code-map-check.js
node tools/doc-maintenance-check.js
node tools/doc-link-check.js
node tools/cross-doc-check.js
node tools/bump-version.js --check
```

每个实际代码交付使用 `node tools/bump-version.js --patch` 统一升版，并在最终版本常量确定后构建和同步 WASM；不要构建完后再升版导致二进制常量落后。生成器版本按物理算法/随机映射变化另外递增。新增参数后按现有工具生成配置参考文档。

台地专项优先通过现有 `terrain_probe` 临时扩展和一次性断言验证，执行接口在实现时确认，不在本方案伪造已存在的台地命令。按项目规则不提交持久化单元测试或新增专项测试脚本；验收报告保留输入、指标、错误码和结果，临时断言/脚本在收口前删除。

证据记录模板：

| 字段 | 待填写内容 |
| --- | --- |
| 构建与配置 | 提交、应用/生成器版本、WASM 双副本哈希、完整配置哈希 |
| 物理矩阵 | seed/profile/实际 profile、台缘与入口坡度、可建候选数、连通分量、双入口路径、生存成本 |
| 失败与降级 | requested/effective profile、attempt、失败码、预算、是否首试成功 |
| 生命周期 | 同 seed 重复、分批 tick、存读档、回溯、跨世界替换结果 |
| 视觉与性能 | 同版本存档、镜头/DPR/设备、截图、计时边界、p50/p95、预算结论 |
| 完成状态 | M1/M2/M3 分别是否通过、未完任务、临时验证文件清理情况 |

## 8. 主要风险与处置

| 风险 | 处置 |
| --- | --- |
| 台缘仅跨少量格子，旋转后漏崖或入口封口 | 01 先做离散相位/方位矩阵；以最终栅格和完整走廊校准带宽 |
| 入口中轴缓，但横向肩部造成超坡 | 独立肩部参数、扩大保护核心，扫描完整足迹；不降低通行阈值 |
| 两次最短路都走同一入口 | 显式入口锚点与生产图分支，诊断分别禁用另一入口验证 |
| 台面可建但居民无法取水或施工接路 | 同时验证完整占地、接路、实际 POI、生存成本；不强制派发 Agent 任务 |
| 规划中旧版本/枚举被当成现状 | 以第 2 节源码核对为起点，实施前重读分支；完成后同步对应权威文档 |
| 世界级降级掩盖台地失败 | 报告同时记录 requested/effective；矩阵只把真实台地首试成功计作通过 |
| 大文件继续膨胀，顺手重构改变旧地形 | 新模块承担算法，必要等价拆分独立差分验收；不夹带旧模板行为修改 |
| 高差暴露 Canvas 遮挡缺陷 | TB-02 保留物理证据，TA-10 修表现并复验；明显影响可读性的缺陷阻止 M2/M3 |
