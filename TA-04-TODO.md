# TA-04 TODO · 世界光向动态受光

> **任务定义**：[07-terrain-art.md](docs/plan/tech/07-terrain-art.md) §1.2 任务表 TA-04（原代号 P0，详见 §6.5）——**世界光向动态受光：枝干/叶簇/Boulder 法线点积，移除固定屏幕亮斑，阴影随树高与叶量变化**。本文件把该任务拆为可执行任务序列，编号即建议实施顺序；每项标注出处章节与验收方式。
> **状态**：TA-04-1 ✅ 已实现（2026-09-13，`lighting.js` 新增 `sunScreenDirFull()` 并通过 `frontend-check.js` 与 16 项冒烟断言）；TA-04-2~8 待实施。`render_accents.js` 头注「受光：本任务沿用既有左上柔光亮部；世界光向动态受光（法线点积）属 TA-04」仍为当前基线（消费在 TA-04-2~5）。升版与 WASM 双副本按本文件计划统一在 TA-04-8 收口执行。
> **难度**：中（单模块内有算法/设计含量，跨 2~3 个前端文件，约 3~8 人日）。
> **依赖**：TA-03 ✅ 已落地（v1.50.25 / v1.50.26 / v1.50.27 枝干骨架 + 椭球叶簇 + 两遍式树冠），本任务不再有前置未结项。
> **范围纪律**：纯前端视觉改动，不新增 FABS 字段、不消费共享模拟 RNG、不改变通行/资源/碰撞/建造事实；装饰仍走独立 `accent_rng`。**即使纯前端改动，统一升版仍会修改 Rust 应用版本常量，实施交付须重编译 WASM 并同步双副本**（07 号 §11.5）。
> **验收总入口**：07 号 §11.4「受光」行 + §11.3 性能预算（p95 新增绘制增量 ≤ 3 ms，待测目标非现有成绩）；浏览器验收必须用 Chrome（存档依赖 File System Access API，07 号 §11.5）。

## 任务序列总览

| 编号 | 任务 | 涉及文件 | 难度 | 依赖 |
| :--- | :--- | :--- | :--- | :--- |
| TA-04-1 ✅ | 照明读取与基础色受光帮助函数 + 世界光向完整屏幕投影 | `frontend/js/lighting.js` | 低 | — |
| TA-04-2 | 法线转换与点积管线：季节基础色 → 漫反射/环境光 → 光色温 | `render_accents.js` / `accent-model.js` | 中 | TA-04-1 |
| TA-04-3 | 枝干受光：少量侧面明暗表达圆柱体 | `render_accents.js` | 低 | TA-04-2 |
| TA-04-4 | 叶簇受光：低频分面/椭球体积，移除固定白色椭圆亮斑 | `render_accents.js` | 中 | TA-04-2 |
| TA-04-5 | Boulder 受光：带法线顶面与侧面，统一场景受光方向 | `render_accents.js` | 低 | TA-04-2 |
| TA-04-6 | 投影阴影随树高与叶量变化，地面图元单独排序 | `render_accents.js` | 中 | TA-04-2 |
| TA-04-7 | 视觉参数集中到 `config.render.js`，模型缓存键纳入风格版本 | `config.render.js` / `accent-model.js` | 低 | TA-04-3~6 |
| TA-04-8 | 收口：§11.4 受光验收 + 性能实测 + 门禁 + 升版与文档同步 | 全链路 | 中 | TA-04-3~7 |

## 任务明细

- [x] **TA-04-1 照明读取封装与基础色受光帮助函数（`lighting.js`）**
    - 内容：① 每帧统一读取 `SimLighting.lightDir()` / `ambient()` / `intensity()` / `tint()`（接口已暴露，§6.5）；② 在 `lighting.js` 增加面向**基础色**的独立受光帮助函数（合入柔和漫反射 + 环境光 + `intensity/tint` 光色温），保留房屋等既有调用行为——**不要在每种 accent 内复制光照公式**；③ 二维低画质路径：把世界光向**完整投影到屏幕**供渐变光心使用——`screenX = Lx*cosZ − Ly*sinZ`，`screenY = (Lx*sinZ + Ly*cosZ)*cosX − Lz*sinX`；现有 `sunScreenDir()` 只投影水平分量，不能原样作为立体树冠亮部位置；光源接近视线方向时投影趋零，应令亮部回到冠心，避免归一化抖动（§6.5 末两段）。
    - 现状核对：`shadeFace()` 是相对旧固定光的明暗校正且不直接合入 `intensity/tint`，**不能**当作新植被材质的完整照明函数；已有 `shadeRgb()` 雏形（注释明示不复用 `shadeFace` 旧光逻辑，合入 intensity/tint），可在此任务内评估扩展或替代。
    - 出处：07 号 §6.5、§3.3「颜色计算/季节光照」行、§4.1 共用受光约定。
    - 验收：房屋/地形/族人等既有调用行为逐项不变；新帮助函数可直接以「季节基础色 + 法线 + 相机」出最终颜色，且不复制光照公式。
    - 依赖：无（首个任务）。
    - ✅ 落地记录（2026-09-13）：**受光帮助函数评估结论——`shadeRgb(rgb, nx, ny, nz)` 已满足设计**（基础色 × (环境光 + wrap 漫反射) × intensity × tint，输出 [0,255] 数组，v1.50.27 起已导出），无需改动；**新增 `sunScreenDirFull()`**（导出）：含 z 分量的完整世界光向屏幕投影，`len < SUN_SCREEN_EPS(0.02)` 时方向按 `len/EPS` 平滑衰减回零（亮部回冠心，避免归一化抖动与边界跳变，调用方无需分支），`valid` 标志仅供参考；既有 `sunScreenDir`/`shadeFace`/`shadeRgb` 及其消费者行为零改动。验证：`node --check` + `node tools/frontend-check.js`（35 JS 语法 + DOM ID 全绿）+ 16 项 Node 冒烟断言（公式分量、归一化、精确退化角回冠心、衰减连续性、旧固定光对照路径）全过。`SUN_SCREEN_EPS` 按 TA-04-7 计划在收口时统一入 `config.render.js`。

- [ ] **TA-04-2 法线转换与点积管线：颜色管线重构**
    - 内容：先以局部旋转把**枝干面、叶簇面法线转换为世界空间**，再与光向求点积；颜色管线固定为「**季节基础色 → 柔和漫反射与环境光 → 光源色温**」，**不把受光明暗预先烘焙进季节色板**（§6.5 首段）。接入点：`render_accents.js` 的 `drawAccentTree` / `drawAccentBush` / `drawAccentBoulder` 内投影/着色段（v1.50.27 Pass B 现有「冠内体积明暗」tZ/tD 是相机空间固定明暗，保留作体积分档，但亮暗驱动改为世界光向）。法线若需预计算/缓存，放入 `accent-model.js` 个体模型缓存（`accent.id` 纯函数，遵守 §10.2 缓存键）。
    - 出处：07 号 §6.5 首段、§6.2 渲染路线、§6.7 三件套职责（模型投影与受光归 `render_accents.js`，光源真相归 `lighting.js`）。
    - 验收：同一季节固定光向下，转相机后亮部随世界光向移动、不黏在屏幕左上；帧耗时增量见 TA-04-8。
    - 依赖：TA-04-1。

- [ ] **TA-04-3 枝干受光：侧面明暗表达圆柱体**
    - 内容：枝干用**少量侧面明暗**表达圆柱体（正面亮、侧面渐暗），随光向变化；不引入逐段贴图或过量多边形。与 TA-04-2 的管线共用同一受光帮助函数。
    - 出处：07 号 §6.5 第 2 段、§6.1 动机行「树皮亮线固定在屏幕左上」。
    - 验收：枝干存在可辨认的受光方向，旋转镜头/光向后明暗随动。
    - 依赖：TA-04-2。

- [ ] **TA-04-4 叶簇受光：低频分面/椭球近似体积，移除固定屏幕亮斑**
    - 内容：叶簇用**低频分面或椭球近似**表达体积；叶面以**宽而弱的亮部**为主，**移除固定白色椭圆**，避免塑料反光（§6.5 第 2 段）。同步核对 v1.50.27 近景簇高光「只给冠层上半部」的既有逻辑——该高光属屏幕固定位置，须改为由世界光向投影驱动（亮部回冠心规则见 TA-04-1）。
    - 出处：07 号 §6.5 第 2 段、§6.1「树皮亮线、冠部径向渐变和亮斑固定在屏幕左上」、任务表 TA-04 行「移除固定屏幕亮斑」。
    - 验收：默认视角与旋转视角均无固定于屏幕的白色高亮斑；亮部宽而柔和，位置随光向与相机变化。
    - 依赖：TA-04-2。

- [ ] **TA-04-5 Boulder 受光：带法线的顶面与侧面**
    - 内容：`drawAccentBoulder` 同步改为**带法线的顶面与侧面**（顶面亮、侧面暗），与植被共用同一受光方向，否则场景仍有两套受光方向（§6.5 第 2 段）。
    - 现状核对：`drawAccentBoulder(sx, sy, scaled, rot, cosZ, sinZ)` 目前无受光参数、无法线分面。
    - 出处：07 号 §6.5 第 2 段、§11.4「受光」行（枝干/树冠/**灌木/岩面**亮部和地面投影协调）。
    - 验收：岩石亮暗方向与同场景树/灌木一致，随光向与相机协调变化。
    - 依赖：TA-04-2。

- [ ] **TA-04-6 投影阴影随树高与叶量变化 + 地面图元排序**
    - 内容：投影**继续复用世界光向与影长**（`lighting.js` 现有 `shadowOffset(height)` 路径），但随**树高、叶量**改变覆盖范围与强度：夏季叶冠影较完整，冬季以**稀疏枝影和弱接地影**为主；**阴影作为地面图元单独排序**；样板阶段可用簇影近似，无需逐叶阴影贴图（§6.5 末段）。
    - 现状核对：现有贴地影为 `shadowK = 0.55 + 0.45 * leaf` 的椭圆近似（已按叶量缩放），需补：树高对影长的贡献、光向对影偏移的驱动、冬枝稀疏影与「地面图元单独排序」语义。
    - 出处：07 号 §6.5 末段、§4.1「地形、房屋体块、族人投影与植被共用同一受光约定」、§11.4「受光」行。
    - 验收：夏影较完整、冬影稀疏减弱；光向变化时影偏移正确；阴影不遮挡拾取与选中信息（§11.2 最小遮挡样板口径）。
    - 依赖：TA-04-2。

- [ ] **TA-04-7 视觉参数集中与模型缓存键版本**
    - 内容：新增视觉参数（明暗系数、亮部宽度、影长/影强系数等）集中到 `frontend/js/config.render.js`（`window.RENDER_CONFIG`，不注入 WASM、不并入 SIM_CONFIG、不进 `config-check.js` 的 SimConfig 链路，07 号 §3.3/§10.4）；**严禁散落字面量**。法线/受光改动属素材风格版本变更：`accent-model.js` 缓存键含 `accentModelStyleVersion`，受光参数入缓存键（或确认不进入逐个体缓存），`accentModelStyleVersion` 递增；缓存键须含世界实例代次、`terrain_generator_version` + `terrain_profile`、素材/风格版本与季节档位；**不缓存相机×光向×季节组合精灵**，设内存上限（§10.2）。
    - 出处：07 号 §10.2 缓存失效契约、§10.4 工程纪律、§6.7 配置行。
    - 验收：`frontend/js/config.render.js` 无新增散落字面量；换世界/读档/REWIND/RESET 后装饰受光不残留旧世界形态（判据 `STR_TAB.start_index == 0`，根 AGENTS.md §4.5.1）。
    - 依赖：TA-04-3~6（参数在实现中收敛后集中）。

- [ ] **TA-04-8 收口：受光验收 + 性能实测 + 门禁 + 升版与文档同步**
    - 内容：① **受光验收**（07 号 §11.4）：固定季相改变光向时，枝干/树冠/灌木/岩面亮部和地面投影协调；固定世界光向转相机，亮部**不黏在屏幕左上**；四季 × 四方位 × 远中近 × 低高俯角在 Chrome 中实测（固定种子/存档/窗口/DPR，记录设备信息）；② **性能实测**（§11.3）：同设备同负载对照，新增绘制 p95 增量目标 ≤ 3 ms，预热后采样 ≥ 60 s；③ **门禁**：`node tools/frontend-check.js`、`node tools/test-wasm.js`、`node tools/config-check.js`、`node tools/test-snapshot-bin.js`（无快照字段变更也应跑通）、`node tools/cross-doc-check.js`、`node tools/doc-link-check.js`、`node tools/doc-maintenance-check.js`；④ **升版与文档同步**：`node tools/bump-version.js --patch`（严禁手改，根 AGENTS.md §4.9）→ 重编译 WASM 同步双副本（`frontend/rust/` + `frontend/`，§4.1）→ 更新 07 号 §1.2 任务表 TA-04 行 ✅、§6.5 状态注、`docs/current/01-changelog.md`、roadmap 21 行（07 号 §11.5）；**不提交临时单元测试**（根 AGENTS.md §4.10）。
    - 出处：07 号 §11.4 受光行、§11.3 性能表、§11.5 实施门禁、根 AGENTS.md §4.0/§4.1/§4.9。
    - 验收：受光验收逐条通过；性能增量达标或记录超标结论并回填方案（调整分块/缓存/剔除/细节分级）；全部门禁绿灯后方标记完成。
    - 依赖：TA-04-3~7。

## 验收口径速查（07 号 §11.4「受光」行）

| 验收项目 | 通过标准 |
|---|---|
| 受光 | 固定季相改变光向时，枝干/树冠/灌木/岩面亮部和地面投影协调；固定世界光向转相机，亮部不黏在屏幕左上 |
| 性能 | 同设备同负载对照，新增绘制 p95 增量目标 ≤ 3 ms（待测目标，非现有成绩） |
| 生命周期 | 暂停/读档/回溯可重建；换世界相同 ID 不复用旧形态；高倍速不频闪 |
