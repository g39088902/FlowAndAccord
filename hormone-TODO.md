# HORMONE TODO · 四轴十一激素神经内分泌调制层

> **任务定义**：[04-hormone-system.md](docs/plan/design/04-hormone-system.md)——在生理层与马斯洛决策层之间引入化学基调调制层（奖赏-情绪轴 / 应激轴 / 生殖轴 / 代谢轴，共 11 激素）。本文件按设计稿 §8「分阶段落地」（P0 只读模型 → P1 最小行为切片 → P2 全量接入 → P3 气质与深化）拆为可执行任务序列，编号即建议实施顺序；每项标注出处章节与验收方式。
> **状态**：未实施（截至 v1.50.32），设计稿尚未排期。
> **本文档已按当前代码基线（v1.50.32）校订**：修正了配置落地口径、快照版本策略、B18 受孕机制、b8/b13 门禁形态等与实现不符之处，校订点以 `【已校订】` 标注。
> **三大红线（全程有效，出处 §1.3）**：① **调制不指挥**——严禁写 `agent.state` / 派发任务 / 越过 `transition.rs`，一切影响以「分支自包含条件内的阈值 / 成本 / 速率调制」表达（根 AGENTS.md §4.11.1）；b1/b2 求生守卫与 b18 生育硬门槛不入调制域；② **确定性**——动力学为纯确定性函数（脉冲 + 线性衰减 + 钳制，周期相位由 tick 派生），**不消耗 WorldRng**；③ **单一真相源**——不建第二份健康 / 关系 / 储备，效果总开关关闭时行为与 RNG 消费与旧版**逐字节等价**。
> **上游依赖策略**：部分脉冲源 / 消费方属未落地规划（05 狩猎防御的战斗、23 记忆、24 撮合市场、20③ 作息、A2 熟人互惠）。对应任务以**既有世界事实替身先行**（断流危机、丧亲、成婚、分娩、登基、卸货、饱食休整等已存在），上游落地后补接完整通道；补接点在各任务内标注。
> **【已校订】外部引用编号口径**：04 号设计稿中的「29-A2 / 29-B5 / 29-B6 / 29-B4」并非 29 号文档（`docs/current/tech/29-impact-matrix.md`，内容为影响矩阵）的章节，而是 **[03 号人际依赖与敌对关系](docs/plan/design/03-social-relations.md) §3/§4 的 A2 / B4 / B5 / B6 编号**（A2 熟人互惠与求助、B4 税政对抗、B5 宗族世仇、B6 偷窃与侵占）。本文档统一改用 **A2 / B5 / B6** 短编号并指向 03 号文档；**A2「熟人互惠」本身尚在 03 号 §7 优先级第 2 位、未落地**，故以其为硬前置的 H-23 暂不排期（见 H-23 修订）。
> **范围纪律**：含 Rust 改动任务完成后重编译 WASM 并同步双副本（根 AGENTS.md §4.1）；快照变更走四处同步（§4.5）；超参四处同步且必须有真实消费点（§4.12，`config-check.js` 第 5 条「空转参数」拒绝零读取字段——**调制类参数随各自消费点任务分批落地**，勿一次全量铺开）；临时断言验证后提交前删除（§4.10）；升版一律 `node tools/bump-version.js --patch`（§4.9）。

## 任务序列总览

| 编号 | 阶段 | 任务 | 难度 | 依赖 |
| :--- | :--- | :--- | :--- | :--- |
| H-01 | P0 | 配置骨架：全局组超参 + 动力学必需参数首批（★ 总开关移至 H-08） | 中 | — |
| H-02 | P0 | `AgentHormones` 定长结构 + 性别 / 年龄基线合成 | 中 | H-01 |
| H-03 | P0 | tick 动力学内核：脉冲 / 衰减 / 钳制 + 既有事件脉冲源接线 | 高 | H-02 |
| H-04 | P0 | 耦合矩阵（动力学侧）+ `crash_timers` 计时器 | 中 | H-03 |
| H-05 | P0 | 快照四处同步 + 存档全量序列化 | 中 | H-04 |
| H-06 | P0 | Inspector「🧬 激素基调」面板 + 状态标签 | 中 | H-05 |
| H-07 | P0 | 收口：长程稳定性 + 与开工前基线等价 + 门禁升版 | 中 | H-01~06 |
| H-08 | P1 | 决策接入骨架（作用点①③）+ **总开关落地** + DA 驱动力与阈值漂移 | 高 | H-07 |
| H-09 | P1 | CORT 危机聚焦：b8/b13 成本调制 + 体力动员 | 中 | H-08 |
| H-10 | P1 | 收口：行为对照 + 消沉死锁冒烟 + 门禁升版 | 中 | H-08~09 |
| H-11 | P2 | §5④ 生理速率单点合成管道（先行基建） | 中 | H-10 |
| H-11b | P2 | ★ 长程对照实验脚手架（30 种子 × 3 组 + 统计口径） | 中 | H-10 |
| H-12 | P2 | 血清素 5-HT：情绪基调 + SAD 冬季 + 低谷效应 | 中 | H-10 |
| H-13 | P2 | 内啡肽 EP：冲刺减免 + 跑者高潮 + 崩解窗口 | 中 | H-11 |
| H-14 | P2 | 催产素 OT：信任折扣 + 协作意愿 + 应激缓冲 + 悲恸 | 中 | H-10 |
| H-15 | P2 | 肾上腺素 ADR + 去甲 NE：爆发 / 警觉 / 焦虑囤积 | 中 | H-11 |
| H-16 | P2 | 甲状腺素 THY：BMR 代谢乘子 + 营养自适应节流 | 中 | H-11 |
| H-17 | P2 | 慢性代价包：折寿 / 禁孕 / 雄激素压制 / 消沉级联 | 高 | H-12~16 |
| H-18 | P2 | 生殖轴行为效果（默认关闭）：AND / EST / PROG 调制 | 高 | H-17 |
| H-18b | P2 | ★ 生殖轴专项长程实验（婚配率 / 出生率不塌方判据） | 高 | H-18 + H-11b |
| H-19 | P2 | 收口：30 种子三组长程对照 + 性能基准 + 门禁升版 | 高 | H-11~18 + H-11b |
| H-20 | P3 | 气质禀赋遗传（含代谢气质，复用 10 号混合规则） | 中 | H-19 |
| H-21 | P3 | 受孕窗口周期（改为**门槛调制**，非成功率） | 中 | H-19 |
| H-22 | P3 | DA × 记忆编码 + CORT × 记忆衰减（待 23 号落地）**+ P3 总收口** | 低 | H-19 |
| H-23 | P3 | OT 分对象联结（待 A2 熟人互惠落地）**暂不排期** | 中 | H-19 + A2 |

## 任务明细

### 阶段 P0 · 只读模型（骨架线）

- [ ] **H-01 配置骨架：全局组超参 + 动力学参数首批（★ 不含总开关）**
    - 内容：`config.rs` 新增激素配置——**【已校订】落地口径**：按根 AGENTS.md §4.12，v1.44.9 起 **Rust 侧无命名 CONST、无手写 `impl Default`**（`config.rs` 为 `#[derive(Default)]` + `#[serde(default)]`，**默认值唯一真相源是 `frontend/js/config.js`**）。故新增超参走**四处**：① `crates/sim_core/src/config.rs` 加 `SimConfig` 字段（含 doc 注释）→ ② `frontend/js/config.js` 加同名 camelCase 键**并写入默认值** → ③ `crates/sim_core/examples/config.json`（探针用）→ ④ `tools/config-check.js` IMPACT_OVERRIDES 映射。**不要**去找「命名 CONST」或手写 Default，二者在基线中不存在。
    - 本任务落地：全局组（分支调制钳制区间 `[0.75, 1.35]`、速率合成总钳制 `[0.8, 1.25]`、全局生育抑制总量上限、通用衰减参数）+ 各激素基线 / 衰减速率 / 事件脉冲表 / 慢性阈值的**动力学必需首批**。
    - **【已校订】总开关 `hormone_effects_enabled` 不在 P0 落地**：原方案称「P0 阶段真实消费点 = H-11 速率乘子占位」，但 H-11 依赖链为 H-11←H-10←H-09←H-08←H-07，P0 收口时 H-11 尚未开始 → 开关在内核**零读取点**，必然触发 `config-check.js` 第 5 条「空转参数」拒绝，与 H-07「config-check 通过」自相矛盾。**修正**：总开关随 **H-08**（第一个真实消费点）一并落地；P0 阶段的「只读」由「代码内没有任何调制接线」这一事实本身保证，不需要开关。
    - **分批纪律**：行为调制类阈值参数（b8/b13/b16/b17 等消费点专属）留到 H-08/H-09/H-18 随消费点落地，避免「空转参数」门禁报错。
    - 出处：04 号 §7 配置集中化、§5 效果总开关、§2 数值域（0~100、基线 50）；根 AGENTS.md §4.12。
    - 验收：`node tools/config-check.js` 通过（含空转参数第 5 条）；`cargo test --lib` 编译校验。
    - 依赖：无（首个任务）。

- [ ] **H-02 `AgentHormones` 定长结构 + 基线合成**
    - 内容：按 §3.1 定义 12×f32 激素 + `chronic_stress` 累计器 + `crash_timers: [u32; 3]`（零堆分配，`harvest_queue` 先例）；基线合成（§3.3）：性别二态（男性雄激素入调制域、女性雄激素与男性雌孕激素低水平不入域）+ 女性雌激素 / 孕激素年龄曲线（青春期抬升 → 育龄平台 → 更年期衰减）+ 雄激素高龄下滑；**气质维度留 P3（H-20）**；周期相位不存字段、由 tick 重算（§4.9）。挂载到 agent 结构，旧存档兼容用 `#[serde(default)]`。
    - 出处：04 号 §3.1、§3.3（1/2 两维度）、§4.9 周期确定性。
    - 验收：`cargo test --lib`；开关关闭下不改变任何既有行为与 RNG 消费。
    - 依赖：H-01。

- [ ] **H-03 tick 动力学内核：脉冲 / 衰减 / 钳制 + 脉冲源接线**
    - 内容：实现 §3.2 更新模型 `level' = clamp(level + Σ pulses − decay_rate × dt × (level − baseline), 0, 100)`，挂载 `tick_phase_metabolism_and_child` **末尾**（遗物归户 2.5 先例之后），决策阶段读取当拍更新后数值；衰减为向基线线性回归、各激素独立速率（ADR 秒级 / EP 分钟级 / DA 小时级 / 阈值游戏日级 / 生殖轴慢曲线）；脉冲源接线**只用既有世界结算事实**：任务完成 / 升级竣工 / 成婚 / 分娩 / 登基 / 卸货装满 / 现场自食 / 淘金丰收 / 断流危机 / 饥渴警戒 / 丧亲丧偶 / 饱食休整 / 在宅团聚 / 威望映射等；战斗（流寇 / 猛兽）、税政、市场成交类脉冲源待上游落地后补接（预留脉冲注册点）。全链不消耗 `WorldRng`。
    - **【已校订】脉冲注入时序**：`tick_phase_metabolism_and_child`（`world_tick.rs:161-206`）内部实际顺序为「代谢循环 → **`execute_pending_childcare()` 受孕结算**（`:198`）→ 2.3 胎儿建号 → 遗物归户」。**受孕 / 分娩 / 成婚等最关键的生殖轴脉冲源发生在阶段中段**，若一律等到阶段末尾统一结算，当拍受孕产生的 PROG 脉冲会延迟一拍生效。**决策**：① 受孕 / 分娩 / 成婚 / 登基类**即时脉冲**在事件执行点（`execute_pending_childcare` 等）内**立即注入**；② 其余脉冲（卸货、自食、休整、团聚等）在阶段末尾统一结算。二者共用同一注入函数，避免双写。
    - 出处：04 号 §3.2、§4.1~4.11 各「来源」行、§1.3 红线 2。
    - 验收：同种子逐字节一致（test-wasm + test-determinism）；事件脉冲可追溯（临时诊断输出验证后删除，§4.10）。
    - 依赖：H-02。

- [ ] **H-04 耦合矩阵（动力学侧）+ 计时器**
    - 内容：落地 §3.4 耦合表中**动力学侧**条目：CORT→5-HT 基线下调（chronic_stress 高位）、OT⇢CORT 脉冲缓冲（× (1−OT 缓冲系数)）、EP/ADR→`crash_timers` 事后窗口（内啡肽崩解 / 肾上腺素深度疲劳 / 产后情绪脆弱三个计时器）、NE⇢5-HT 互抑（双低 →「焦虑性消沉」判定数据准备）、CORT→AND 抑制（HPG）、胜利→DA+AND 双脉冲 / 败绩反向小幅、EST⇢5-HT 敏感度乘子、PROG⇢CORT/NE 镇静、营养 / 供暖→THY 节流；各耦合量全部配置化。**行为侧消费**（调制乘子进入分支 / 速率）不在本任务，随 P1/P2 各自任务接线。
    - 出处：04 号 §3.4 全表、§3.1 crash_timers、§4.3 崩解窗口。
    - 验收：耦合只改激素读数不改行为（开关关闭逐字节等价）；计时器随存档持久化不重置。
    - 依赖：H-03。

- [ ] **H-05 快照四处同步 + 存档**
    - 内容：`AgentSnapshot` 新增 `hormones` 定长子结构（12×f32 + chronic_stress + crash_timers，纯数值无新枚举字符串、不触 STR_TAB），完整走 ① `spatial/snapshot.rs` 定义 → ② `world_snapshot.rs::generate_snapshot()` 赋值 → ③ `snapshot_bin/encode.rs` FABS 编码 → ④ `snapshot-bin.js` 解码 + `rustworld.js::_applySnapshot()` 映射（根 AGENTS.md §4.5 四处同步铁律）。
    - **【已校订】`FORMAT_VERSION` 必须 +1，不是「评估」选项**：`snapshot_bin/layout.rs:13` 明确「各 Section 内部为**顺序流（非定长 stride）**」，AGENT section 追加 12×f32 后既有解码器按旧偏移读取会**全部错位**（静默产生 NaN / 乱值，`snapshot-check.js` 静态核对未必能捕获）。**决策**：`layout.rs:31` `FORMAT_VERSION` **3 → 4（强制）**，并同步 `frontend/js/snapshot-bin.js:27` 的 `FORMAT_VERSION = 3` → `4`（该处为硬判据 `dv.getUint16(OFF_VERSION) !== FORMAT_VERSION → return null`）。
    - **【已校订】存档 `SAVE_FORMAT_VERSION` 保持不变 = 7**：H-02 已定「旧存档兼容用 `#[serde(default)]`」，而存档走 **serde JSON 全量**（`world_save.rs:78` `agents: Vec<Agent3D>`），非二进制定长——`#[serde(default)]` 天然向后兼容，**不得**同时 +1（原文档「结构不兼容时手工 +1」与 H-02 的 serde(default) 方案互斥）。先例见 `geo/terrain.rs:256/303`：`#[serde(default)]` 保证旧档加载默认空，`SAVE_FORMAT_VERSION` **不递增**。**注意**：旧档本无激素数据，读出即默认零值，这与「读档不得重置」不矛盾（新档才需保证不重置），措辞以本条为准。
    - 出处：04 号 §7 快照 / 存档行、根 AGENTS.md §4.5 / §4.5.1 / §4.9。
    - 验收：`node tools/snapshot-check.js` + `test-wasm.js` 全绿；**新增**「旧 wasm + 新 JS」与「新 wasm + 旧 JS」两种混搭组合均返回 null（版本不符安全降级）而非静默错读；存读档回归（含换世界后 `STR_TAB.start_index == 0` 缓存判据不受影响）。
    - 依赖：H-04。

- [ ] **H-06 前端 Inspector「🧬 激素基调」面板**
    - 内容：按四轴分组的十一激素横条（当前值 / 基线刻度 / 趋势箭头）+ 状态标签（消沉 / 悲恸 / 焦虑 / 冲刺中 / 警觉 / 孕期安稳 / 节流代谢）+ 代谢气质显示（P3 前可先占位隐藏）；世界事件留痕走既有事件模板管线（LLM 不参与事实生成）。**红线**：Inspector 属高频刷新容器，必须套内容快照缓存（根 AGENTS.md §4.15）；**不做性别对比叙事，只显示个体状态**（04 号 §9 风险 7）。
    - **【已校订】趋势箭头的跨帧状态管理**：快照下发为 30Hz 节流且**按人口自适应降频**（`sim_worker.js:235` `snapshotIntervalMs = 33.3`，M5-0.4 起降频），而趋势箭头需前后帧差分。**决策**：趋势缓存需处理三种**状态跳变**——① rewind / 读档（tick 回退）；② 换世界（`strtab_epoch` 递增）；③ 存档加载。**检测到 tick 不连续或回退时清空趋势缓存**，否则会显示错误的剧烈涨跌。
    - 出处：04 号 §7 前端行、§9 风险 7；根 AGENTS.md §4.15。
    - 验收：`node tools/frontend-check.js` 通过；Chrome 实测面板随快照刷新、标签切换正确；**新增** rewind / 换世界 / 读档三种跳变下趋势箭头不出现假剧烈波动。
    - 依赖：H-05（数据通道就位）。

- [ ] **H-07 P0 收口：稳定性 + 等价性 + 门禁升版**
    - 内容：① 长程运行激素曲线无 NaN / 无越界（test-wasm 长程套件 + 临时钳制断言，验证后删除）；② **等价性验证**：test-determinism 6 套件（多种子 / 分批独立性 / 快照无副作用 / 存读档）；③ 全量门禁：test-wasm / test-determinism / snapshot-check / config-check / frontend-check / cross-doc-check / doc-link-check / doc-maintenance-check；④ `node tools/bump-version.js --patch` → 重编译 WASM 同步双副本；⑤ 文档同步：04 号状态注、[01-changelog.md](docs/current/01-changelog.md)、`docs/current/README.md` 模块登记、相关局部 AGENTS.md（agent 结构与 tick 顺序变化涉及 `spatial/AGENTS.md`）。
    - 出处：04 号 §8 P0 完成条件、§9 验收、根 AGENTS.md §4.0 / §4.1 / §4.9 / §4.10。
    - 验收：P0 完成条件逐条复核（任意长程无 NaN / 与开工前基线逐字节等价 / 脉冲可追溯）后标记 P0 完成。
    - 依赖：H-01~06。

### 阶段 P1 · 最小行为切片

- [ ] **H-08 决策接入骨架（作用点①）+ DA 驱动力与阈值漂移**
    - 内容：建立作用点①「分支阈值调制」的应用框架——乘子一律钳制 `[0.75, 1.35]`、只写在**分支自包含条件内部**；实现 DA（§4.1）：`drive = clamp(dopamine / dopamine_threshold, 下限, 上限)` → 劳动启动门槛与连续作业意愿调制乘子；★ 阈值漂移（每次奖赏 `threshold += 脉冲 × 漂移系数`，游戏日级慢回落）→ 淘金变率强化与「醉心淘金」人格涌现（与 b13 的 4 级庄园纪律守卫不冲突——只调制意愿不绕过门槛）；空虚态：drive 低于下限持续数游戏日 →「消沉 · 提不起劲」标签 + b8/b13 阈值上浮；**不阻断 b1~b5 生存链路**。b8/b13 调制参数在本任务随消费点落地。
    - **【已校订】总开关 `hormone_effects_enabled` 在本任务落地**（自 H-01 移入）：作为本框架唯一的全局门控，关闭时所有调制乘子恒为 1.0。**这是开关的第一个真实消费点**，保证 `config-check.js` 第 5 条「空转参数」不报错。默认关。
    - 出处：04 号 §5 ①、§4.1、§9 风险 1/2。
    - 验收：相同处境不同激素背景下选择可解释；**【已校订】求生守卫验证方式**——原写「`node tools/diagnose.js --check all`」不准确：该工具的 `--check` 取值为 `all | anomalies | starvation | deaths | none`（`diagnose.js:76`），是**异常嗅探**（饿死 / 死亡统计），**不能断言「饿死非激素所致」**。修正为：① `diagnose.js --check starvation` 观察饥饿死亡数为 0；② **新增临时断言冒烟脚本**（在分支调制出口处断言 b1/b2 分支命中不受调制影响），验证后按 §4.10 删除；③ 开关关闭行为与本阶段起始态逐字节等价。
    - 依赖：H-07。

- [ ] **H-09 CORT 危机聚焦（作用点③）**
    - 内容：实现皮质醇急性效应（§4.5）。
    - **【已校订】作用对象与调制载体的实际形态**：原写「④⑤ 层阈值上浮」有两处与代码不符：
      1. **「④⑤ 层」在代码中无数字编号**——实际层级是 `MaslowLevel::{Physiological, Safety, Belonging, Esteem, SelfActualization, Instantaneous}` 枚举，且**层级由分支内部动态计算**（如 b8 在 `at_home` 时为 `Instantaneous`、Tier0 时为 `Safety`、否则 `Esteem`，见 `branches.rs:268-274`），同一分支可能落在不同层级，「④ 层」指代不清。**改为按分支 ID 表述**：b8 改善住宅 / b13 积累财富。
      2. **b8 与 b13 根本没有可乘的「阈值」字段**——b13 是**硬门禁** `house.tier != Tier4Manor || need_repair || !all_stocked || !bag_has_space`（`branches.rs:357-360`），b8 是 `upgrade_ready_by_cost` **材料成本**门禁。**当前代码无乘子挂载点**。
      **修正方案**：调制作用于 **b8 的 `upgrade_ready_by_cost` 材料成本**与 **b13 的 `all_stocked` 储备下限**（`FAMILY_STOCK_ORDER` 各资源 trigger 下限），即「危机中需要更多材料/储备才肯动工动念」，而非「阈值上浮」。需**新增**配置化乘子挂载点（属新增代码，非复用）。
      表达为**阈值调制而非层级重排**，决策编排（用户拖动顺序）权威不变（根 AGENTS.md §4.14）；体力动员小窗口（消耗减免，代价为事后疲劳，接 crash_timers）；与马斯洛「低层级绝对优先」同向但机制独立。
    - 出处：04 号 §5 ③、§4.5 急性效应。
    - 验收：危机中 b8/b13 意愿显著抑制、危机源消失后自然恢复；决策编排热注入功能不受影响；**新增**：确认调制未改变任一分支的 `MaslowLevel` 归属（层级不被重排）。
    - 依赖：H-08。

- [ ] **H-10 P1 收口**
    - 内容：① 同种子开关对照实验（行为差异可解释、无异常状态卡死）；② 消沉死锁冒烟：人为压低 DA/5-HT 长程运行，验证基线回归 + 求生守卫保证不饿死（§9 风险 1）；③ 门禁全绿 + bump-version --patch + WASM 双副本 + changelog；④ 更新 04 号 §8 P1 行状态；⑤ **【已校订】重新生成本阶段基线**（存档 + 快照哈希）并在 changelog 记录，供 P2 收口对照（判据见 H-07 修订）。
    - 出处：04 号 §8 P1 完成条件、§9 风险 1。
    - 验收：「相同处境不同激素背景选择可解释；求生守卫零违反」；开关关闭 = **本阶段起始态**逐字节等价。
    - 依赖：H-08~09。

### 阶段 P2 · 全量接入

- [ ] **H-11 §5④ 生理速率单点合成管道（先行基建）**
    - 内容：实现速率乘子**单点合成函数**：THY（代谢）、EP（镇痛减免）、ADR（爆发消耗）等分来源记账、统一进入一个乘子合成函数、总乘子钳制 `[0.8, 1.25]`；**严禁各处散落乘**（§4.11 红线）。此管道是 H-13/H-15/H-16 的共同前置。
    - **【已校订】既有散落乘子必须一并收编**：`agent.rs:502-523` 当前已有三处代谢乘子在散落——`is_pregnant → ×agent_pregnant_metabolism_mult`、`RepairingHouse/GatheringWood/MiningStone/MiningGold → ×agent_work_metabolism_mult`、`/dig_ratio`（`agent_digestion_ratio_min/max` 钳制）。若只把 THY/EP/ADR 三个新来源收进合成函数而放任既有三处在外，「单点合成」名不副实，仍是两套乘子逻辑。**决策**：合成函数**同时收编既有三来源**（作为「既有来源」分账记账），新管道成为代谢乘子的唯一出口。
    - 出处：04 号 §5 ④、§4.11、§9 风险 6。
    - 验收：极端组合（THY 高 + EP 高 + ADR 高）下无人饿死 / 渴死于自身代谢（临时断言用例，验证后删除）；**新增**：开关关闭时合成结果与旧版**逐位一致**（确认收编既有三来源未改变原数值，含浮点结合律影响需实测）。
    - 依赖：H-10。

- [ ] **H-11b ★ 长程对照实验脚手架（新增任务）**
    - 内容：**【新增】**原 H-19 把「≥30 种子 × 3 组长程对照」当作验收项，但项目内**没有**多种子批量长程对照工具（现有 `gold_mining_analysis.js` / `test-personalization.js` 均为一次性分析脚本）。**本任务先建脚手架**：复用 `test-determinism.js` 的种子驱动与 `diagnose.js` 的统计口径，产出可复跑的实验脚本，固定输出指标：人口与灭绝（灭绝样本单列）、家族存续、劳动时间分配、财富集中度、婚配率与出生率、饥荒存活结构、冬季行为差异、职业结构分布。支持 `{无激素 / 只读 / 全量}` 三组开关与 ≥30 种子批量。
    - 出处：04 号 §9 长程对照实验纪律。
    - 验收：`node tools/<新脚本> --seeds 30 --groups 3` 可跑通并输出结构化对照报告；同种子结果可复现。
    - 依赖：H-10（可与 H-11 并行）。

- [ ] **H-12 血清素 5-HT：情绪基调 + SAD + 低谷效应**
    - 内容：来源接线（休整至满体力 / 长期饱食 / 威望 prestige 映射基线上浮 / 在宅团聚正耦合 / 雌激素敏感度乘子）；★ 季节性情感：冬季档位（接 15 四季，**当前已落地**）血清素基线临时 − 配置百分比；低谷效应：高阶需求阈值上浮（兴趣缺失）、熔断耐受下降（挫折更早折返）、与高 CORT 叠加进入「消沉」；**与 23 号边界**：M01/M02 挨饿记忆（情节经验）与血清素（当下基调）分通道叠加、共用同一钳制上限不重复计权（23 号未落地前先写边界注释与预留通道）。
    - 出处：04 号 §4.2、§3.4 EST⇢5-HT、§6 接口表。
    - 验收：入冬全村情绪可观察下落、开春恢复；消沉可自然恢复不死锁。
    - 依赖：H-10（H-04 耦合已备数据）。

- [ ] **H-13 内啡肽 EP：冲刺与崩解**
    - 内容：来源（连续重体力满 N 游戏小时 / 长途跋涉跑者高潮 / 欢庆事件）；效应经 H-11 管道：体力消耗与伤势移速惩罚临时减免——**只掩盖不治疗**，伤势与疲劳照常积累；★ 崩解窗口：峰值回落后 `crash_timers` 体力恢复减半疲偿期（「连干三天 → 倒头睡两天」劳动节律）。
    - 出处：04 号 §4.3、§5 ④。
    - 验收：冲刺—崩解节律可观察；实际伤势数值不被 EP 改写（只改感知/减免）。
    - 依赖：H-11。

- [ ] **H-14 催产素 OT：信任、联结与悲恸**
    - 内容：来源（配偶在宅共处 / 育儿照料 / 并肩民兵与换工帮工可先接既有协作事实 / 赠予往来）；效应：**信任折扣**——交易对手方候选成本 × 信任系数（首期边界：家户 / 宗族成员；24 号撮合市场未落地前先在既有榷场交易 / 换工分支的候选评估内生效）；协作 / 赠予 / 借工具分支条件阈值下调；应激缓冲（H-04 已接动力学侧）；OT 低位的共处关系衰减更快（20④ 关系维持的量化载体，20 号未落地则预留）；★ 悲恸：丧偶 / 丧子 → OT/5-HT 大额崩落 + CORT 大额脉冲 → 数游戏日「悲恸」标签，与既有丧偶封账 / 产后休养冷却并行。
    - 出处：04 号 §4.4、§3.4 OT⇢CORT、§9 首期边界。
    - 验收：有家可归者皮质醇反应更小可观察；丧偶后悲恸标签出现并按期恢复；OT 为单 Agent 全局水平（不建全人口两两矩阵）。
    - 依赖：H-10。

- [ ] **H-15 肾上腺素 ADR + 去甲肾上腺素 NE**
    - 内容：ADR（§4.6）：脉冲（遭遇威胁 / 罪案目击 / 临界求生——战斗系统属 05 号未落地，先接临界求生与既有威胁事件，战斗落地后补接流寇 / 猛兽源）；效应：移速爆发倍率、战力倍率（18 号对抗公式未落地则预留乘子接入点）、侦测半径扩大、体力消耗经 H-11 管道上浮、事后 crash_timers 深度疲劳、长期高频 → vitality 上限临时折损。NE（§4.7）：来源（近期遇袭频度 / 亲历暴力 / 资源紧张，中速累积）；效应：侦测阈值下调、**焦虑囤积**（备货触发阈值有效值上浮，与 23 记忆 M01/M02 分通道）、熟路偏好（L2 成本修正——统一选策未落地前可先作用于既有重路由偏好）、易惊中断、双低「焦虑性消沉」标签（H-04 数据已备）。
    - 出处：04 号 §4.6、§4.7、§3.4 NE⇢5-HT、§6 接口表 18 号行。
    - 验收：威胁临近时警觉行为可观察；焦虑囤积与记忆备货修正分通道可区分；慢性 ADR 不致 vitality 塌方（钳制）。
    - 依赖：H-11（速率管道）、H-10。

- [ ] **H-16 甲状腺素 THY：代谢乘子 + 营养节流**
    - 内容：BMR 乘子**只经 H-11 单点管道**生效：调制饥渴消耗 / 体力消耗与恢复 /（微弱）健康衰减；★ 禀赋代谢气质的基线漂移位预留（遗传在 H-20）；★ 营养自适应节流：家户粮 / 木低于生存线持续 N 游戏日 → THY 下调 → 全消耗 × 节流系数（钳制下限），节流代价 = 迟钝（劳动速率↓）、恢复慢；供暖不足判定与 15 号共享**同一有效供暖需求读数**（「同一物资同一事实」纪律）。
    - 出处：04 号 §4.11、§3.4 营养→THY、§6 接口表 15 号行。
    - 验收：荒年群体自适应节流可观察（消耗下降但迟钝）；无 anyone 被自身代谢烧死（H-11 用例复跑）。
    - 依赖：H-11。

- [ ] **H-17 慢性代价包（chronic_stress 越阈）**
    - 内容：§4.5 慢性五条：① 健康衰减速率上浮（**战乱 / 苛税折寿**的生理证据链）；② 记忆衰减 × 慢性系数（23 号未落地则留接口）；③ **禁孕**（慢性高位应激性不孕）；④ 雄激素压制（H-04 动力学已备，此处接行为侧：求偶与竞争意愿下调）；⑤ 消沉级联（拖低血清素）。★ **全局生育抑制总量上限**在此任务生效：禁孕（③）与后续孕激素失守（H-18）共享总量上限，防止战乱营地出生率被压至零（§9 风险 5）；解除条件 = 危机源消失 + 衰减 + OT 缓冲。
    - 出处：04 号 §4.5 慢性代价、§4.10 钳制纪律、§9 风险 5。
    - 验收：慢性高压营地出生率下降但不归零（长程观察）；压力解除后生育能力恢复。
    - 依赖：H-12~16（耦合与速率通道齐备）。

- [ ] **H-18 生殖轴行为效果（★ 默认关闭，实验通过后才开启）**
    - 内容：AND（§4.8）：Winner/Loser effect（竞争获胜双脉冲已备，接行为侧）、重体力装载速率乘子（与力量禀赋、DA 三通道独立叠加共用全局钳制）、b16 求偶积极性调制、b14 夺位 / b17 出价 / 狩猎倾向、冒险偏好；Pair-bonding 成婚温和下调；EST（§4.9）：受孕窗口**不在本任务**（H-21），此处接情绪敏感行为侧 / 产后崩落 crash_timers / 更年期标记（Inspector「已过生育年纪」，不改变终止门槛）；PROG（§4.10）：孕期镇静（冒险成本上浮、应激钝化）、**流产风险因子**（确定性阈值累积，与既有流产规则合流，不新增随机掷骰）、哺乳期缓降。★ **开关纪律**：生殖轴行为效果默认 P2 关闭（v1.19.0 b16 前科——人口自我更替是项目敏感红线），专项实验（H-18b）通过后才开启；女性社会制度（不立户 / 不夺位）与激素无关，**严禁用激素解释社会规则**（风险 7）。b16/b14/b17 调制参数随本任务消费点落地。
    - **【已校订】b16 求偶调制的形态约束（重演 v1.19.0 的高风险点）**：`11-decision-engine.md:199` 记载 v1.19.0 曾因「单身男性被安全/备料分支长期占满、求偶极少触发导致人口无法自我更替」而**特意把 b16 提升到 b5/b6/b7/b9/b10 之前**（`branches.rs:56-66` 兜底序中 `B16Courtship` 亦确实排在 B8/B9/B10/B13/B14 之前）。本任务的「低雄激素 → 不积极求偶」**正是在重演同一条人口塌方路径**，而原文档只写「钳制下限保证不塌方」却**未给量化判据、未限定调制落点**。**决策**：
      1. **调制只作用于 b16 的「目标选择偏好」（魅力 / 距离 / ID 的选择权重），不调制 b16 的触发门槛**——这样能表达「积极性差异」而不触碰人口红线（触发频率不变，只是选谁不同）；
      2. 若实施中确实需要调门槛，则**调制乘子上限硬性设为 1.10**（即门槛最多抬高 10%），并写入配置注释；
      3. 「不塌方」的量化判据交由 **H-18b** 专项实验裁定。
    - 出处：04 号 §4.8、§4.9、§4.10、§9 风险 5/7；`11-decision-engine.md` v1.19.0 条目。
    - 验收：开关关闭 = 全部生殖轴行为与 H-17 收口态逐字节等价；**新增**：确认 b16 触发频次在开关开启前后无统计显著下降（仅目标分布改变）。
    - 依赖：H-17。

- [ ] **H-18b ★ 生殖轴专项长程实验（新增任务）**
    - 内容：**【新增】**原 H-18 验收写「开关开启后婚配率 / 出生率在钳制与总量上限内不塌方（专项长程实验）」，但**未定义「不塌方」的量化判据、也未安排执行载体**。**本任务补上**：基于 H-11b 脚手架，跑 ≥30 种子 × {生殖轴关闭 / 开启} 两组，裁定 H-18 与 H-17 的量化红线——建议判据（实施时据实测回填具体阈值并写入 04 号 §9）：① 长程婚配率相对关闭组下降 ≤X%；② 出生率相对关闭组下降 ≤Y%；③ 任一种子**不出现人口归零**；④ 战乱 / 苛税高压场景下出生率**下降但不归零**（验证全局生育抑制总量上限有效）。
    - 出处：04 号 §9 风险 5。
    - 验收：得出明确结论（通过 / 不通过 + 实测数据）；通过后**方可**将生殖轴开关默认置为开启，结论回填 04 号 §9 风险 5。
    - 依赖：H-18 + H-11b。

- [ ] **H-19 P2 收口：长程对照 + 性能 + 门禁**
    - 内容：① **≥30 种子 × {无激素 / 只读 / 全量} 三组**长程对照（**载体 = H-11b 脚手架**）：人口与灭绝（灭绝样本单列）、家族存续、劳动时间分配、财富集中度、婚配率与出生率（生殖轴专项，结论并入 H-18b）、饥荒存活结构（代谢气质专项，注：代谢气质在 H-20 才落地，本轮仅记录基线）、冬季行为差异、职业结构是否单调坍缩（成瘾风险 2）；② 性能：`node tools/profile-benchmark.js` 决策与内核耗时增长 ≤5%（更新 O(agents) 定长运算零堆分配复核）；③ 钳制内无死锁验证（消沉、节流可自然恢复）；④ 门禁全绿 + bump-version --patch + WASM 双副本 + changelog + 04 号 §8 P2 行状态更新。
    - 出处：04 号 §8 P2 完成条件、§9 验收与风险 1/2。
    - 验收：三组对照结论落档；每激素有触发 / 效应 / 观察证据；性能达标或记录超标结论并回填优化方案。
    - 依赖：H-11~18 + H-11b + H-18b。

### 阶段 P3 · 气质与深化

- [ ] **H-20 气质禀赋遗传（含代谢气质）**
    - 内容：五维气质（多巴胺反应度 / 皮质醇易感性 / 催产素依恋倾向 / 血清素稳定性 / ★ 代谢气质 THY 基线漂移），复用 10 号先天禀赋的遗传混合规则接入出生流程；**生殖轴不设气质维度**（避免把生育力做成可育种属性）；气质作为脉冲反应度与基线漂移的乘子（钳制内）进入 H-03/H-16 既有通道；与力量 / 智力 / 魅力禀赋并列进入择偶与叙事；涌现观察：工作狂家族 / 将门虎子 / 商贾世家 / 荒年饿不死的慢代谢宗族。
    - **【已校订】RNG 取舍必须预先定案，不得留作开放项**：原写「不消耗 WorldRng **或** 严格按既有禀赋混合的 RNG 消费顺序（二者取一）」，把确定性红线的判断甩给实施者。实际二难为：`birth.rs:122-133` 的既有混合 `inherit` 闭包**每个性状消耗一次 `rng.gen_range(-delta, delta)`**——
      - 选「不消耗 RNG」（纯父母均值派生）→ 气质**无变异、无遗传漂变**，「家族气质谱系」涌现效果大打折扣；
      - 选「按既有顺序」→ 5 个新维度追加 5 次 `gen_range`，**改变 RNG 消费序列**，违反红线②（同种子逐字节一致）。
      **决策（采纳前者为默认，理由：不动 RNG 序列即不动任何既有行为）**：气质**复用既有 `inherit` 闭包、追加在既有 6 个禀赋（intelligence / strength / digestion_efficiency / libido / sleep_efficiency / life_expectancy）之后**，消费顺序固定为 6 项 → 11 项，**一次性约定并写入 `birth.rs` 注释**。这会改变 RNG 序列，故**必然**产生基线变更，须：① 重跑 `test-determinism` 与旧基线比对；② 在 changelog 显式记录「本版本起 RNG 消费序列变更，旧存档模拟结果不可复现」；③ 评估是否需要 `SAVE_FORMAT_VERSION` 7→8（若旧档续跑会产生与旧版不同的未来，建议 +1 以阻断静默偏差）。
      **备选（仅在不愿动 RNG 时采用）**：气质纯确定性无变异，并明确接受「代际无漂变」的代价——**须在 04 号 §3.3 写明该取舍**。
    - 出处：04 号 §3.3 第 3 维、§8 P3、§6 接口表 04 号行；`crates/sim_core/src/spatial/birth.rs:115-133`。
    - 验收：家族气质谱系数代可观察；RNG 消费顺序变更已记录并获基线比对确认；`test-determinism` 全绿（以新基线为准）。
    - 依赖：H-19。

- [ ] **H-21 受孕窗口周期（可选开关，默认启用）**
    - 内容：育龄女性雌激素固定周期波动，相位由出生 / 到达 tick **确定性派生**（`sin` 类纯函数，不消耗 RNG、不存相位字段）。
    - **【已校订】调制载体由「成功率」改为「受孕门槛」**：原写「B18 受孕判定成功率调制（峰段高、谷段趋零）」，但 `world_tick.rs:271-321` `execute_pending_childcare` 的实际逻辑是——只要 `eligible`（成年 / 非孕 / 冷却结束 / 饥渴体力达标）且 `at_home`，就**无条件** `is_pregnant = true`，**全链路零随机、零概率**。因此「成功率」在当前代码中**不是一个存在的概念**，无从调制：
      - 若新增概率判定 → 必须消耗 `WorldRng`（违反红线②）或另发明确定性判据（新增机制）；
      - 若谷段直接硬禁孕 → 与 H-17 慢性禁孕叠加，直接撞上风险 5 的出生率归零。
      **决策**：改为「**受孕条件门槛调制**」——峰段放宽 `agent_conception_hunger_min` / `thirst_min` / `stamina_min` 三项阈值，谷段收紧。这样：① 天然确定性、零 RNG；② 关闭时阈值乘子恒为 1.0，天然逐字节等价；③ 不引入概率概念；④ 与 H-17 禁孕机制正交（一个是门槛，一个是硬禁）。
      独立开关，关闭时退化为现行判定、行为逐字节等价；与 H-18 生殖轴总开关、H-17 生育抑制总量上限共同约束。
    - 出处：04 号 §4.9 周期节律、§8 P3；`world_tick.rs:271-321`。
    - 验收：同种子开关关闭逐字节等价；开启后「何时怀上」呈周期结构且出生率不塌方（并入 H-11b 脚手架复跑）。
    - 依赖：H-19。

- [ ] **H-22 DA × 记忆编码 + CORT × 记忆衰减（待 23 号落地）**
    - 内容：23 号记忆系统落地后接线：`MemoryEntry` 强化增量 × DA 因子（钳制）；慢性应激 × 记忆衰减速率（H-17 预留接口转正）；与 5-HT / NE 的分通道叠加共用钳制上限不双计权；文档与代码注释双向声明「记忆 = 情节经验 / 激素 = 当下基调」边界。
    - **【已校订】承接 P3 总收口**（自 H-23 移入，避免被未落地的 A2 阻塞）：本任务完成后执行 **P3 总收口**——04 号 §8 P3 完成条件复核（家族气质谱系可观察 / 与 23 联动无双计权 / 人口长程不塌方）、全量门禁、升版、文档收尾（含**本 TODO 归档勾选**）。
    - 出处：04 号 §3.4 多巴胺→记忆、§4.2 边界、§8 P3、§9 风险 4。
    - 验收：「同一记忆不同基调」「同一基调不同记忆」行为区分用例通过；P3 完成条件复核通过并完成归档。
    - 依赖：H-19（+23 号记忆系统落地为硬前置）。

- [ ] **H-23 OT 分对象联结 + 总收口**
    - 内容：**【已校订】依赖澄清与排期调整**：原文的「29-A2 关系网络」编号有误（29 号 `docs/current/tech/29-impact-matrix.md` 是影响矩阵，无 A2 章节）。实际所指是 **[03 号人际依赖与敌对关系](docs/plan/design/03-social-relations.md) §3.2 的 A2「熟人互惠与求助」**，而 A2 在 03 号 §7 落地优先级中列第 2 位、**目前尚未落地**。故本任务**标记为暂不排期**，待 A2 落地后启动；在 A2 落地前，OT 维持 H-14 定义的「单 Agent 全局水平」形态。
      本体内容（A2 落地后执行）：OT 从单 Agent 全局水平分化为按人 / 按对的联结强度（**严禁提前建全人口两两矩阵**，23 号同款性能纪律——按需惰性建对）；信任折扣细化到人、世仇反向加价（[03 号 §4.5 B5 宗族世仇](docs/plan/design/03-social-relations.md) 恩怨记忆构成负极）；OT 低位的共处关系衰减更快转正（20④）；最终收口：04 号 §8 P3 完成条件复核、全量门禁、升版、文档收尾（含本 TODO 归档勾选）。
    - **【已校订】总收口与 H-23 解耦**：原「最终收口」挂在以 A2 为前置的任务上，会导致 P3 收口被无限期阻塞。**修正**：P3 的**总收口动作**（04 号 §8 P3 完成条件复核、全量门禁、升版、文档收尾）**移出本任务**，改为在 H-22 完成后即执行；H-23 仅保留 OT 分对象联结本体，作为 A2 落地后的可选增强。
    - 出处：04 号 §4.4 首期边界、§8 P3、§9 风险 3；[03 号](docs/plan/design/03-social-relations.md) §3.2 / §4.5 / §7。
    - 验收：家族气质谱系可观察 + 与 23 联动无双计权 + 人口长程不塌方（P3 完成条件，由 H-22 后的总收口执行）；关系矩阵内存有上限。
    - 依赖：H-19 + **A2 熟人互惠落地（硬前置，当前未满足 → 暂不排期）**。

## 验收口径速查（04 号 §8 完成条件 + §9）

| 阶段 | 完成条件 | **【已校订】判据口径** |
|---|---|---|
| P0 | 任意长程运行激素曲线无 NaN / 无越界；等价性；事件脉冲可追溯 | 等价性 = **与 H-01 开工前基线逐字节一致**（P0 是唯一一次可用「改动前基线」的窗口，因开关尚未引入、tick 与快照改动在此阶段一次性完成）；快照 `FORMAT_VERSION` 3→4、存档 `SAVE_FORMAT_VERSION` 保持 7 |
| P1 | 相同处境不同激素背景下选择可解释；求生守卫零违反 | 等价性 = 与本阶段起始态逐字节一致；求生守卫验证见 H-08 修订（`diagnose.js --check starvation` + 临时断言，非 `--check all`） |
| P2 | 每激素有触发 / 效应 / 观察证据；钳制内无死锁（消沉、节流可自然恢复） | 长程对照载体 = **H-11b 脚手架**；生殖轴开启与否由 **H-18b** 专项实验裁定 |
| P3 | 家族气质谱系可观察；与 23 联动无双计权；人口长程不塌方 | **总收口在 H-22 后执行**（已从 H-23 解耦，避免被未落地的 A2 阻塞）；H-20 会带来 RNG 消费序列变更，须重立基线 |
| 全程 | 确定性（不消耗 WorldRng、同种子逐字节一致）；≥30 种子三组长程对照；性能增长 ≤5%（`profile-benchmark.js`）；生殖轴行为效果默认关闭、实验通过后开启 | ★ **唯一例外**：H-20 气质遗传**会**改变 RNG 消费序列（6 项 → 11 项），须显式记录基线变更并评估 `SAVE_FORMAT_VERSION` +1；除 H-20 外全链零 RNG |

## 重点风险对策速查（04 号 §9，实施时逐条对照）

1. **消沉死锁** → 求生守卫绝对性 + 调制钳制 + 基线回归（H-08/H-10 冒烟用例）；
2. **成瘾人口停滞** → 阈值慢衰减 + 金矿储量约束方差 + 职业结构长程观察（H-19）；
3. **调制叠加爆炸** → 全局钳制上限 + 分层分来源记账 + UI 修正分解（H-11/H-17）；
4. **与 23 边界混淆** → 分通道 + 共用钳制 + 双向声明 + 区分用例（H-12/H-22）；
5. **★ 生殖轴塌方** → 各通道独立钳制 + 全局生育抑制总量上限 + **H-18b 专项实验（量化判据）** + 默认关闭；**且 H-18 的 b16 调制只作用于目标选择偏好、不调触发门槛**（避免重演 v1.19.0，见 H-18 修订）（H-17/H-18/H-18b）；
6. **★ 代谢乘子失控** → §5④ 单点合成管道（**须一并收编既有 pregnant/work/digestion 三处散落乘子**）+ 总乘子 `[0.8, 1.25]` + 极端组合用例（H-11/H-16）；
7. **★ 性别叙事边界** → 激素只写生理与情绪，严禁解释社会制度；面板不做性别对比（H-06/H-18 全程）。

### 【新增】实施前必读：本 TODO 与代码基线的 5 处关键差异

> 开工前务必确认，否则 H-01 即会走不通。

| # | 原 TODO / 设计稿表述 | 代码基线实际 | 影响 |
|---|---|---|---|
| 1 | 「命名 CONST + 字段 + Default 三处」 | v1.44.9 起**无命名 CONST、无手写 Default**；`config.rs` 为 `#[derive(Default)]`，默认值唯一真相源 = `frontend/js/config.js`（根 AGENTS.md §4.12） | 照原表述找不着落点 |
| 2 | 「评估 `FORMAT_VERSION` 是否 +1」 | Section 是**顺序流非定长 stride**（`layout.rs:13`），追加字段必错位 | 必须 +1（3→4），且同步 `snapshot-bin.js:27` |
| 3 | 「B18 受孕成功率调制」 | `execute_pending_childcare` 是**零随机、无条件**受孕（`world_tick.rs:271-321`） | 「成功率」不存在，改为门槛调制 |
| 4 | 「④⑤ 层阈值上浮」 | 层级是 `MaslowLevel` 枚举且**分支内动态计算**；b8/b13 是**成本 / 硬门禁**，无阈值字段 | 改为调制材料成本与储备下限 |
| 5 | 「待 29-A2 关系网络」 | 29 号是影响矩阵；实为 [03 号](docs/plan/design/03-social-relations.md) §3.2 **A2 熟人互惠**，**尚未落地** | H-23 暂不排期；P3 总收口移至 H-22 后 |
