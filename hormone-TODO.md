# HORMONE TODO · 四轴十一激素神经内分泌调制层

> **状态**：未实施、未排期。2026-09-13 按当前 v1.50.35 源码复核。
> **设计来源**：[04 激素系统设计](docs/plan/design/04-hormone-system.md)。本文负责实施顺序、边界与验收；设计稿中关于事件时序、跨版本等价、遗传 RNG、代谢钳制、受孕门槛及外部依赖的旧校订，以本文的修订契约为准。实施对应任务时同步修正设计稿相关章节，不沿用相互矛盾的表述。
> **范围**：这是游戏模拟的抽象参数模型；激素名称不代表医学因果或真实浓度。只使用已存在的结算事实；缺少战斗、记忆、撮合市场或关系机制时，相关接入项明确延期，不虚构“既有替身”。

## 1. 全程契约

1. **调制不指挥**：任务仍由 `arbitrate_sustained_task` → `dispatch_task` 分派；激素不写行动状态、不替代 `transition.rs`、不改变用户配置的分支顺序与层级。b1/b2 求生守卫、住宅成本矩阵、b13 庄园与储备触发器门禁、b18 原有生育条件均保留。保护范围按用途判断，不能把 b8 的 Tier0→Tier1 安居需求一并当作高阶欲望压制。
2. **确定性与等价性分开验证**：同一构建、配置、种子与操作序列必须可复现；跨版本关闭效果时比较既有世界状态与 RNG 状态的投影，不能比较增加字段后的原始快照字节。激素动力学及 H-20 均不额外消费共享 `WorldRng`。
3. **单一真相源**：不增加第二份健康、家户余额或人际关系。气质是固定禀赋，激素是当前状态，关系强度仍由关系系统维护。
4. **开关覆盖全部行为路径**：P0 无行为接线。H-08 引入默认关闭的 `hormone_effects_enabled`，关闭时保持原公式与运算顺序。H-17 引入默认关闭的生殖效果开关；生殖相关跨轴效果（CORT 生育抑制、AND 求偶偏好、EST 情绪敏感、PROG 效应及周期）都同时受总开关与生殖开关控制，禁止通过耦合其他激素绕过关闭状态。只读动力学仍可显示。
5. **按真实消费点落配置**：`config.rs` 字段及注释 + `frontend/js/config.js` 默认值 + `examples/config.json` + `config-check.js` 的影响映射同步。Rust 不写默认数值常量或手写 `SimConfig::default`。参数与首个读取点同批完成，禁止占位读取骗过空转参数检查。
6. **时间与数值**：统一以游戏小时和 tick 表达，`simulationDt` 不变。每 tick 当前为 1/60 游戏小时，不能许诺解析小于一 tick 的“秒级”过程。所有除数有正下界；衰减系数、输入与输出必须有限；所有持续时间、阈值和调制范围配置化。
7. **交付纪律**：按根 [AGENTS.md](AGENTS.md) 与 [开发流程](docs/current/tech/30-workflow.md) 执行。代码批次统一升版后构建并同步 WASM 双副本，再验证最终产物；纯文档修订不升版。临时实验脚本和断言验证后删除，不提交新的持久化测试。新增复杂模块同步局部指南、代码地图与现状文档。

## 2. 任务依赖总览

编号保留以便追踪；依赖列决定实施顺序。同一批中的配置与消费点必须一起达到可验证状态。

| 编号 | 阶段 | 任务 | 依赖 |
|---|---|---|---|
| H-01 | P0 | 动力学配置首批（与 H-02/H-03 同批） | — |
| H-02 | P0 | 定长状态与初始化 | H-01 |
| H-03 | P0 | 动力学与结算事件接线 | H-02 |
| H-04 | P0 | 耦合、累计器与余韵窗口 | H-03 |
| H-05 | P0 | 快照与存档 | H-04 |
| H-06 | P0 | Inspector 观察 | H-05 |
| H-07 | P0 | 基线等价与稳定性收口 | H-01～H-06 |
| H-08 | P1 | 总开关、DA 与分支意愿接入 | H-07 |
| H-09 | P1 | CORT 危机聚焦 | H-08 |
| H-10 | P1 | 最小行为切片收口 | H-09 |
| H-11 | P2 | 分通道生理速率合成 | H-10 |
| H-11b | P2 | 临时长程实验与指标口径 | H-10 |
| H-12 | P2 | 5-HT 基调与季节 | H-10 |
| H-13 | P2 | EP 消耗减免与崩解 | H-11 |
| H-14 | P2 | OT 缓冲与悲恸 | H-10 |
| H-15 | P2 | ADR 爆发与 NE 备货调制 | H-11 |
| H-16 | P2 | THY 代谢与节流 | H-11 |
| H-17 | P2 | 慢性代价、生殖开关与抑制合成 | H-12～H-16 |
| H-18 | P2 | 生殖轴行为 | H-17 |
| H-18b | P2 | 生殖专项长程实验 | H-18、H-11b |
| H-19 | P2 | 可用通道收口与性能 | H-11～H-18、H-11b、H-18b |
| H-20 | P3 | 不额外消耗 RNG 的气质遗传 | H-19 |
| H-21 | P3 | 可选受孕周期（默认关闭） | H-19、H-18b |
| H-22 | 扩展 | DA/CORT 与记忆接入 | H-19、记忆系统落地 |
| H-23 | 扩展 | OT 与按对象关系接入 | H-19、A2 关系系统及对应消费方落地 |
| H-24 | P3 | 气质与周期收口 | H-20、H-21；不依赖延期扩展 |

## 3. P0 · 只读模型

- [ ] **H-01 动力学配置首批**
  - 只增加 H-02/H-03 当批使用的基线、衰减和已接事件脉冲参数。耦合参数在 H-04、行为钳制在 H-08、速率钳制在 H-11、生育抑制上限在 H-17 引入。
  - H-01 不是可单独发布的空配置提交；与消费者同批通过 `config-check.js`，不得提前铺满所有全局参数。
  - 来源：设计稿 §7、根 AGENTS.md §4.12。

- [ ] **H-02 定长状态、基线与初始化**
  - 结构为 **11 个激素水平 + 1 个 DA 奖赏阈值，共 12 个 f32**，另有 `chronic_stress` 和三个余韵计时器。奖赏阈值不是第十二种激素，必须保持正值。
  - 按性别、年龄及孕程合成基线；创建始祖、胎儿与出生的入口逐一核对。胎儿不执行成人动力学，出生初始化只能发生一次。
  - `#[serde(default)]` 只解决缺字段反序列化，零值不等于合理初始基线。若保留缺字段容错路径，必须有可辨识的缺失状态，在配置注入后初始化；禁止把合法的零激素水平误判成“未初始化”。正常新档恢复不得重置。
  - 持续低驱动力、营养不足、连续重体力、悲恸等若依赖历史，须增加有界、定长、可存档的累计器或上一拍标记，不能只列 12 个浮点数便声称实现全部持续状态。字段随首次消费落地并同步 H-05 契约。
  - 验收：数值有限、阈值非零，创建/出生/存读档初始化语义一致；无额外 RNG 消费。
  - 来源：设计稿 §3.1、§3.3。

- [ ] **H-03 动力学与结算事件接线**
  - 保留既有 tick 顺序：代谢/受孕 → POI 交互/分娩 → 房屋 → 运动 → 决策及提交 → 账本。`world_tick.rs` 的代谢阶段末尾不是所有事件的共同尾部，卸货、自食、分娩、成婚等不能在那里按“本拍已发生”集中补算。
  - 每拍在代谢阶段末尾执行一次回归与持续输入更新；离散脉冲在事实成功结算的位置注入一次，使用同一纯数值入口。回归前产生的脉冲参与本拍回归，回归后的脉冲保留到下拍回归，顺序固定且可追踪。
  - 后续决策读到此前已结算的值；决策期间/之后的事件只影响后续读取，不重跑已完成的代谢或决策。受孕阶段末尾更新并不会自动造成“延迟一拍”，必须按实际读点解释延迟。
  - 回归采用 `level += alpha * (baseline - level)`，`alpha = clamp(decay_per_game_hour * dt, 0, 1)`；脉冲后钳制水平到配置域。这是离散线性回归，不等于恒速减法。不依赖帧率或快照次数。
  - 离散事件以结构化结算结果触发，不解析 `last_event` 文案。装满/满体力只在跨越边界时奖励一次；饱食、共处、资源短缺按 `dt` 累积，不能每 tick 重复发整笔事件奖励。
  - 先接已存在的升级、成婚、出生、登基、采收/卸货、死亡与生理警戒；登记每个事件的生产阶段、受影响对象、注入次数、读取时点。战斗胜败与撮合成交延期。
  - 验收：一件事实一次脉冲；分批推进与逐 tick 推进一致；快照读取不产生脉冲。
  - 来源：设计稿 §3.2、§4；`world_tick.rs`、`ecology/tick.rs`、`decisions/scheduler.rs`。

- [ ] **H-04 耦合与余韵窗口**
  - 耦合以本次更新前的一份激素值计算全部增量，再统一提交，避免按字段写入顺序产生隐式级联。固定 CORT→5-HT、OT→压力缓冲、营养→THY 等公式与符号。
  - NE 焦虑标签使用“高 NE + 低 5-HT”，删除原稿与“焦虑警觉”定义矛盾的“双低”条件；标签只是游戏规则。
  - 慢性压力与持续状态必须有恢复/衰减规则。EP/ADR 峰后窗口采用越阈边沿触发，计时器饱和递减，禁止低位每拍重新续满；产后窗口仅由分娩/流产等明确事件开启。
  - EST/PROG/AND 的跨轴贡献独立保留来源，避免后续生殖开关无法阻断间接行为效应。
  - 验收：同输入同输出，无回路数值爆炸，无永久续期；关闭效果时只改变激素观察数据。
  - 来源：设计稿 §3.4。

- [ ] **H-05 快照与存档**
  - 快照走 `snapshot.rs` → `world_snapshot.rs` → `snapshot_bin/encode.rs` → `snapshot-bin.js` + `rustworld.js` 四处同步；内部累计器完整存档，观察快照只传 UI 必需项，并说明字段布局。
  - 当前 FABS `FORMAT_VERSION = 3`；修改 AGENT 顺序流布局须递增版本并同步解码器，实施时读取当时版本，不机械写死 3→4。验证两个方向的新旧混搭均拒绝解码，前端能明确呈现不兼容而非继续推进陈旧画面。
  - 当前 `SAVE_FORMAT_VERSION = 7`、`SAVE_APP_VERSION = 1.50.35`，加载器检查两者。应用升版已经拒绝旧应用存档；`serde(default)` 不代表用户可以跨版本读档。不改应用版本门禁，不为行为/RNG 差异单独递增结构版本；仅不兼容序列化结构变化才调整结构版本。
  - 存读档与 rewind 保留所有累计器、气质和余韵窗口；工具换世界后 `resetCaches()`，STR_TAB 仍只以 `start_index == 0` 判断新驻留表。
  - 验收：`snapshot-check.js`、WASM 与确定性存读档回归；同构建连续推进与存读档后续跑一致。
  - 来源：根 AGENTS.md §4.5/§4.5.1/§4.9、[快照与存档](docs/current/tech/06-snapshot-and-save.md)。

- [ ] **H-06 Inspector 激素面板**
  - 按四轴显示十一激素、有效基线与状态标签；DA 奖赏阈值单独标示。趋势用相同世界/Agent 的两个样本除以实际 tick 差，不把快照间隔视为固定。
  - 在 `READY / LOAD_RESULT / REWIND_RESULT / RESET_DONE` 生命周期消息和选中对象变化时清理趋势缓存，tick 回退作额外保护；同 tick 不做差分。正常倍速/降频导致 tick 跳跃，不应因此清空缓存。
  - 不用 `strtab_epoch` 判断换世界，它在新世界可重复。字符串缓存与 UI 趋势缓存各按自己的契约处理。
  - 高频 HTML 刷新遵守内容快照缓存；只展示个体状态，气质功能未实现时隐藏相关项。
  - 验收：Chrome 下测试换世界（含相同种子）、同 tick 读档、rewind、倍速、选人切换；无假趋势、无交互失效。
  - 来源：根 AGENTS.md §4.15、`rustworld.js::_invalidateWorldStaticCaches`。

- [ ] **H-07 P0 收口**
  - 开工前保存基准构建、配置、种子和固定采样 tick。以各构建匹配的快照解码器生成**既有状态投影**，比对 Agent、POI、房屋、账本、任务、事件及 RNG 状态；仅排除新增激素字段和明确的版本/协议元数据，不能排除行为字段来获得通过。
  - 同构建确定性回归与跨构建基线对照分别执行。`test-determinism.js` 证明重复运行/存读档一致，不能独自证明相对旧构建无行为变化。浮点状态按位比较，RNG 可由完整存档或临时探针核对。
  - 长程验证所有新增数值有限、无越界，脉冲可追溯；临时断言删除。执行第 6 节适用门禁，完成现状文档、局部指南、模块登记及 changelog。

## 4. P1 · 最小行为切片

- [ ] **H-08 总开关与 DA 意愿调制**
  - 引入默认关闭的总开关。DA `raw_drive = dopamine / max(dopamine_threshold, epsilon)`；作用乘子使用钳制后的值，消沉判定使用 **raw_drive 与独立阈值**，不能判断“已经钳制到下限的 drive 小于下限”。累计低谷时长持久化，恢复后回落/清零。
  - 奖赏阈值随事件上调并慢回归，明确上限、正下限。各来源合成后只钳制一次，分支意愿乘子初始候选范围为 `[0.75, 1.35]`，以配置为准。
  - b8/b13 缺少通用意愿阈值，须新增有界的意愿等待/冷却条件，保留原硬门禁，不能伪称直接乘现有阈值。b8 只调高阶升级、豁免 Tier0→Tier1；b13 可在原金币采集冷却之上接意愿条件。不得每次评估都重置等待计时形成永久不启动。
  - 既有资源准备、临界自救与修缮规则不因 DA 降低而失效。改变持续作业终止意愿时同样保护求生边界。
  - 验收：隔离相同输入验证分支结果；极低 DA 仍能命中 b1/b2，等待有界且可恢复；关闭效果与 H-07 基线投影一致。
  - 来源：设计稿 §4.1、§5①。

- [ ] **H-09 CORT 危机聚焦**
  - 高 CORT 通过 H-08 同一意愿通道调制高阶 b8/b13，不修改实际升级扣账成本，不提高 `all_stocked` 判据，不绕过 `family_stock_on` 的施密特触发器。
  - 原提案把成本与储备上调后，可能超出仓储/备货目标且没有补货分支响应，造成永久卡死；因此不采用。不得以“④⑤层”笼统改写动态层级。
  - CORT 体力动员与事后疲劳延期到 H-11 管道内接入，P1 不提前创建第二条速率路径。
  - 验收：危机抑制可解释，危机解除后恢复；库存触发器、扣账、编排与 Tier0 安居均维持原语义。
  - 来源：设计稿 §4.5、§5③；`branches.rs` b8/b13。

- [ ] **H-10 P1 收口**
  - 固定场景验证低 DA、高 CORT、危机解除与在宅升级。验证任意合法分支编排下守卫自包含，不要求关闭用户编排或重新排序。
  - `diagnose.js --check starvation` 仅提供死亡观察，不能证明因果；以隔离输入的临时断言证明求生守卫不变。资源耗尽本身允许死亡，不把“所有场景零饿死”当成可实现承诺。
  - 关闭效果持续对照 H-07 的开工前基线投影，不通过每阶段更换参考来掩盖退化；开启效果的统计参考另行记录。执行适用门禁并同步文档。

## 5. P2/P3 · 扩展与收口

- [ ] **H-11 分通道生理速率合成**
  - 为饥饿、水分、体力消耗、体力恢复、健康消耗、装载与移速分别合成激素修正；同一来源只计一次。H-09 延期的体力动员在此接入并加入真实配置消费。
  - `[0.8, 1.25]` 仅是新增激素合成修正的初始钳制范围，**不钳制既有妊娠/劳动/消化乘子的最终乘积**。保持原公式顺序：当前消化效率只影响饥饿，不影响水分；不能收编后统一除到所有通道。
  - 总开关关闭直接走原计算路径，确保按位不变。诊断可以统一列示既有来源与激素来源，无须为展示重排浮点表达式。
  - EP “恢复减半”与总下限 0.8 冲突，首期按总钳制收敛，删除减半承诺；若需要更强代价须先另行设计该通道范围再验证。
  - 验收：各通道极端组合均有限、在指定范围，关闭效果按位一致；检查资源充足时是否出现新增自救阻断。

- [ ] **H-11b 临时长程实验与统计口径**
  - 复用现有工具编写临时批量驱动，至少 30 个固定种子。三组为：A 开工前基准构建；B 当前构建只读（总开关关）；C 当前构建效果开启，明确生殖开关值。A/B 做投影等价与开销对照，B/C 做行为效果对照；“无激素”不是尚不存在的第三个运行开关。
  - 固定配置、地图、初始人口、总游戏时长、采样间隔、构建哈希和运行平台；比较相同种子，保留灭绝样本及删失时间。
  - 先定义分母：婚配事件/合格未婚人口时间、活产/育龄女性人口时间；另记总人口、灭绝、家族存续、时间加权任务分布、财富集中度、冬季差异。无暴露量的比例记缺失，不记零；尚无“职业”实体时用任务时间占比。
  - 先定判据再跑效果实验。脚本/断言验证后删除；报告保留参数、指标定义、命令和重建步骤，放入 `docs/` 适当位置并登记，不提交持久化测试脚本。

- [ ] **H-12 血清素基调与季节**
  - 复用 H-03/H-04 的来源与耦合，补充季节性基线和高阶意愿调制，避免把脉冲再接一遍。熔断耐受只改允许降级的边界，不抢占高优先级生理需求。
  - 记忆系统未落地，只记录未来分通道合成契约，不增加占位参数。EST 敏感度行为效果等待 H-18。
  - 验收：冬季/开春可观察，低谷可恢复，求生守卫不变。

- [ ] **H-13 内啡肽冲刺与崩解**
  - 连续劳动/路程累计触发奖励，消费与恢复修正只经 H-11。峰后窗口沿用 H-04，不重复累计。
  - 伤势移速减免等待 [狩猎防御](docs/plan/tech/05-hunting-defense.md) 的伤势机制落地，不新增替代健康量。
  - 验收：冲刺与有界恢复窗口可观察，停工后能恢复，无永久续期。

- [ ] **H-14 催产素缓冲与悲恸**
  - 本期仅使用已存在的配偶共处、出生、丧偶/丧子事实与压力缓冲；标签持续时间由明确累计器支持。复用已有脉冲，不双计。
  - 交易对象信任、协作、赠予、借工具及关系衰减消费方延期到撮合市场/A2；既有榷场采购没有可供选择的熟人卖方，不能在其中伪造“信任折扣”或改写价格/账本。
  - 验收：相同危机下 OT 缓冲可观察、悲恸可消退；全局激素不建人口两两矩阵。

- [ ] **H-15 ADR 爆发与 NE 备货调制**
  - 本期用已存在的临界生理警戒/资源紧张接入，ADR 移速和消耗经 H-11；移动仍由 `current_lane_id` 驱动。NE 只在现有资源准备条件上有限调制。
  - 备货开启/关闭阈值、储备目标与实际容量一起检查，保留滞回方向和可达性；不得仅抬高一个阈值而使库存永久无法满足。
  - 侦测、战力、vitality 上限、暴力目击、熟路记忆、统一 L2 候选成本均依赖对应系统，延期；不修改路由来伪造记忆或威胁偏好。
  - 验收：爆发后疲劳有界，NE 备货可完成、可退出，保护临界自救。

- [ ] **H-16 THY 代谢与营养节流**
  - 调制仅经 H-11。营养不足依据实际饥饿等身体读数及既有供暖结算事实累计；家户缺粮可作辅助，不能把无家户余额误认成所有无家者长期挨饿。
  - 供暖需求与住房系统一致，温暖季节缺木不应被解释为受冻。营养/供暖改善后基线恢复；代价以装载减慢、恢复减慢表达，发展速率不存在时不宣称已经接入。
  - 验收：短暂缺货不触发慢性节流，持续危机及解除可观察，消耗与恢复范围有效。

- [ ] **H-17 慢性代价与生殖抑制合成**
  - 健康速率和消沉消费已有累计器，雄激素压制动力学沿用 H-04。记忆衰减延期 H-22。
  - 此处首次引入默认关闭的生殖开关，早于 H-18；生育抑制不能先于开关上线。废除“硬禁孕但靠乘子上限保证出生率非零”的矛盾方案。
  - 生育抑制首期采用有界的附加等待窗口，累计压力只能在完成/明确周期边界设置窗口，不允许每拍推迟截止点。原始合格条件始终是下限，窗口结束提供重新评估机会，不保证受孕。
  - CORT、周期等生育等待进入同一合成出口和总上限；流产风险属于独立通道，不与等待时间相加冒充“统一抑制总量”。首期不新增激素导致流产的规则，待单独规则与人口实验通过后再设计。
  - 决策候选与 `execute_pending_childcare` 结算复核读取同一纯条件函数及同一组参数；保留 pending 意图边界，不在结算中生成任务。
  - 验收：压力解除可恢复；关闭任一总门控后无额外生育抑制；受孕等待有上限，不宣称战乱一定有出生。

- [ ] **H-18 生殖轴行为（默认关闭）**
  - AND 可调装载与求偶目标偏好，EST 可调情绪敏感，PROG 可调压力反应；均遵守第 1 节双开关，含跨轴间接效应。已有婚姻、孕程与产后冷却为事实来源；哺乳机制未实现时不声称已有哺乳期状态。
  - b16 保留原合格候选集与触发条件，偏好只作用于实际择偶排序键，ID 保持稳定最终平局规则。删除“必要时允许上调门槛 10%”例外。
  - 改变对象也会改变路程、成婚率与未来触发次数，不能承诺“只改目标就不影响人口”。b14/b17 候选偏好需先核实消费点，狩猎/胜败等依赖项延期。
  - 验收：隔离输入下原资格守卫不变，开关无旁路；人口影响交 H-18b，不用一次短跑作结论。

- [ ] **H-18b 生殖专项实验**
  - 基于 H-11b，固定非生殖效果开启，对照生殖关/开两组。覆盖资源充足、持续短缺及恢复场景；战乱/苛税未落地时不用其名义制造世界事实，可用临时注入压力验证局部通道并明确场景性质。
  - 去掉 X%/Y% 占位及“任一种子不灭绝”硬断言。先用独立基线/试验种子制定婚配、出生和新增灭绝风险的数值容忍界限，记录后再运行正式配对种子；结果出来后不得放宽阈值以取得通过。
  - 报告配对变化、区间估计、异常种子与恢复时间；样本不足或区间跨越失败界限，结论为不确定，保持默认关闭。出生率非零不是局部乘子钳制能够保证的性质。
  - 通过只提供后续默认值变更的依据，本任务不自动开启。调整默认值作为单独代码批次走门禁与文档更新。

- [ ] **H-19 P2 可用通道收口**
  - 汇总三组长程报告、生殖专项与恢复冒烟；每个本期消费通道都有来源/效果/观察证据。延期项逐项保留，不能标成“十一激素全量完成”。
  - 性能区分 A→B 只读开销与 B→C 效果开销；同平台、人口规模、配置和采样窗口重复测量并报告波动，内核/决策耗时增长目标 ≤5%。超过目标即性能项未通过，优化或明确修改预算后再收口，不能只写一句超标说明便勾选完成。
  - 气质到 H-20 才落地，此处不验收代谢气质的遗传结果。执行第 6 节适用门禁。

- [ ] **H-20 气质遗传：不额外消费共享 RNG**
  - 五维气质为奖赏反应、压力易感、联结敏感、情绪稳定、代谢基线。首期采用固定规则从既有禀赋派生始祖气质，后代按父母均值混合，缺失父母按统一基线处理；不追加随机变异。允许家族差异被继承，不承诺遗传漂变或特定家族类型必然涌现。
  - 当前 `birth.rs` 的六项遗传各消费一次随机数；追加五次会改变共享 RNG 序列。它仍可满足同构建确定性，却违反关闭效果的旧状态等价，因此不采用原稿的追加方案。
  - 不因气质新增改变现有择偶排序；新的气质择偶规则属于独立扩展。气质持久化并接 H-03/H-16 的真实消费点；关闭效果时既有行为和 RNG 投影仍相同。
  - 验收：父母/子代混合可追溯，创始差异可继承，关闭效果不改变既有 RNG 序列；不为行为差异递增存档结构版本。

- [ ] **H-21 可选受孕周期（默认关闭）**
  - 相位用稳定的个体标识与 tick 的整数周期派生，周期长度转 tick 时规定舍入和正下限；不依赖浮点三角函数跨平台逐位一致的假设。周期不存独立可漂移相位，必要累计状态依 H-05 存档。
  - `execute_pending_childcare` 是通过资格与在宅检查后确定性受孕，没有“受孕成功率”。周期仅通过 H-17 的有界附加等待生效，**不放宽原饥饿/水分/体力门槛**，不用谷段永久禁孕。
  - 总开关、生殖开关、周期开关三者同时开启才生效；候选筛选与结算复核同源。与慢性压力共用等待上限。
  - 验收：关闭退化为原判定，开启后有周期机会且等待有界；复跑 H-18b 的生育影响与恢复场景。

- [ ] **H-22 记忆接入（延期，不承担 P3 收口）**
  - 硬前置：[记忆系统](docs/plan/tech/02-memory-system.md) 的对应字段与消费通道实际落地。届时接 DA 编码/CORT 衰减，并核对 5-HT、NE 与经验修正不重复计权。
  - 验收：同一记忆不同基调、同一基调不同记忆的隔离用例；关闭激素不改变记忆通道。上游缺失时保持未勾选。

- [ ] **H-23 OT 与按对象关系接入（延期）**
  - 硬前置：[人际关系方案](docs/plan/design/03-social-relations.md) 的 A2 及具体消费方；市场偏好另依赖 [内部市场](docs/plan/tech/03-internal-market.md)，世仇效应另依赖 B5，关系维护另依赖 [日常生活](docs/plan/design/02-everyday-life.md)。不能只等 A2 就声称全部可接。
  - OT 保持 Agent 全局化学水平，按对象的关系强度读取关系系统；不把 OT 拆成另一份关系网络。关系端容量、淘汰和序列化复用上游规则。
  - 验收：只有已存在的关系事实参与偏好，单对象影响可解释，无 O(人口²) 全表扫描或双份关系存储。

- [ ] **H-24 P3 基础收口**
  - H-20/H-21 完成后复核继承、周期、关闭效果等价、长程人口与性能，执行第 6 节门禁；同步设计稿阶段状态、现状文档及 changelog。
  - H-22/H-23 和战斗/撮合等延期接口独立跟踪，不阻塞本期收口，也不得随本期完成一起勾选或把整份 TODO 归档为已完成。

## 6. 验收与交付速查

| 项目 | 可执行判据 |
|---|---|
| 同构建确定性 | 相同种子/配置/操作序列，逐 tick、批量推进、存读档、rewind 后状态一致；快照无副作用 |
| 关闭效果等价 | 各阶段都与 H-07 开工前构建比较既有状态和 RNG 投影；新增字段/协议版本不同不比较原始整帧字节 |
| 求生边界 | 临时隔离断言验证原始守卫；死亡统计仅作观察，不承诺资源枯竭时零死亡 |
| 生殖安全 | 原条件不放宽，等待上限可达，开关完整；正式配对实验前冻结数值判据，不确定则保持默认关闭 |
| 数值与时序 | 有限值、正除数、固定更新顺序、事件一次注入、窗口可退出；持续状态完整存档 |
| 代码交付 | 按适用范围运行构建、`cargo test --lib`、`test-wasm.js`、`test-determinism.js`、`config-check.js`、`snapshot-check.js`、`frontend-check.js`；修改移动/行为时追加诊断 |
| 文档与版本 | `doc-maintenance-check.js`、`cross-doc-check.js`、`doc-link-check.js`、`code-map-check.js`（模块变动时）、`bump-version.js --check`、`git diff --check`；纯文档不升版、不重跑模拟测试 |
| 最终产物 | 代码批次升版后构建 WASM 并同步双副本，验证最终版本；删除临时测试，报告保留实验口径与未完成依赖 |

现有静态门禁不能自动证明本文全部语义；事件时序、开关覆盖、阈值可达性与依赖是否实际落地必须人工复核。
