# TA-05 · P0 植被样板闭环技术方案与任务序列

> 状态：待实施。本文交付技术方案，不代表样板、浏览器验收或性能门禁已经通过。
> 编制日期：2026-09-14。源码核对基线：`77fffc7`；实施时重新记录实际工作区与产物身份。
> 按本次用户要求放在仓库根目录；任务总台账与验收权威仍为 [07 地形美术规划](docs/plan/tech/07-terrain-art.md) §1、§11.3、§11.4。本文细化 TA-05，不改写其他任务的完成状态。

## 1. 目标、范围与完成条件

将现有渲染能力收敛成“一株落叶树、一株落叶灌木、一块 Boulder 岩石”的可复现 P0 样板，交付四季形态、四方位旋转、世界受光、生命周期与帧耗时的完整证据。执行路线是：固定基线 → 建立样板 → 分离季相与受光变量 → 验证真实世界 → 测量 → 修复复验 → 归档。

TA-03/TA-04 为前置能力，已有实现直接复用。TA-05 可以修复本次验收暴露的局部缺陷；三类树形/灌木变体属于 TA-06，带滞回的完整 LOD 与包围体剔除属于 TA-07，跨实体深度拆分属于 TA-08，完整山口/岩壁构图属于 TA-09。不能为了样板通过，提前把这些任务标为完成。

**完成条件**：三个样板身份可复现；视觉矩阵、真实生命周期、性能门禁均有证据；阻断问题已修复并复验；临时夹具与断言已清理；结论能由归档配置、存档与记录重建。若性能超标或真实 LOAD 尚未验证，只能记录部分通过，TA-05 保持未完成。

不新增持久化字段、FABS section、模拟参数或物理规则。不增加花灌木、积雪、落叶地被、飘叶和可采资源；这些能力不属于本次闭环。

## 2. 已核对的实现与技术入口

下表为当前源码事实。07 号文中带旧版本标记的“当前精灵仅消费叶色”“Bush 不消费季节”等历史描述，不作为重新实现的依据。

| 职责 | 当前入口 | TA-05 使用方式 |
| :--- | :--- | :--- |
| 季相 | [accent-season.js](frontend/js/accent-season.js)：`SimTreeTint.sample(accent, sim, profile)`、`yearPhase(sim)` | 读取原始快照季节，观察连续叶色、叶量和芽量；不用平滑光相控制落叶 |
| 稳定模型 | [accent-model.js](frontend/js/accent-model.js)：`AccentModel.get()`、`getByKey()`、`resetCache()` | 固定 kind/id 复用骨架；通过返回模型的 `evergreen` 筛选真正的落叶个体 |
| 个体绘制 | [render_accents.js](frontend/js/render_accents.js)：`drawAccentEntity()`、`drawAccentTree()`、`drawAccentBush()`、`drawStoneBody()` | 沿用局部三维枝干、叶簇与岩面；`accentClusterVisibility()` 控制逐簇收缩隐藏 |
| 光照 | [lighting.js](frontend/js/lighting.js)：`SimLighting` | 复用世界光、材质受光与完整屏幕光向投影；配置仍归 [config.lighting.js](frontend/js/config.lighting.js) |
| 地面阴影 | [render_shadows.js](frontend/js/render_shadows.js)：`drawAccentShadowGround()`、`drawAccentShadowFor()` | 与实体一起验证，不能只截树冠；阴影保持地面接收与独立入队 |
| 世界队列 | [render_world.js](frontend/js/render_world.js)、[render_depth_queue.js](frontend/js/render_depth_queue.js) | 复用地形、道路、实体、阴影统一排序；不另加整层植被 pass |
| 帧循环与 UI | [render_canvas.js](frontend/js/render_canvas.js)：`render(now)` | 记录真正执行绘制的帧；区分整帧、渲染/UI、统一队列三类耗时 |
| 生命周期 | [rustworld.js](frontend/js/rustworld.js)：`_invalidateWorldStaticCaches()` | 核对 READY / LOAD_RESULT / REWIND_RESULT / RESET_DONE，验证模型冷重建一致性 |
| 视觉参数 | [config.render.js](frontend/js/config.render.js)：`RENDER_CONFIG` | 叶簇数、季相曲线、常绿比例、细节阈值等集中配置；不注入 WASM |

已有近/中/远细节阈值与模型缓存，不能把 TA-07 描述成从零添加 LOD；该任务补齐的是滞回、完整投影包围体及相关优化。当前模型缓存也被资源景观通过 `getByKey()` 复用，改公共模型时须抽查资源景观，避免只顾三个 accent 样板。

## 3. 样板与复现设计

### 3.1 两条验证路径

**受控视觉路径**：从真实快照选取三个 accent，记录原始字段；必要时在临时夹具中复制到独立的只读渲染输入，排列成留有冠幅和阴影间距的三对象样板。沿用生产绘制、投影、地形及队列，不复制一套“专用美术算法”。夹具移动位置时必须重新取真实地表高程，并记录变换；标记为“视觉夹具”，不能当作真实世界截图。

**真实世界路径**：保留三个对象原始坐标，用实际页面与 WASM 快照推进，分别拍摄各对象及上下文。三个对象不必在同一镜头内；固定对象 ID 比强求同框更重要。这条路径验证建档/读档、暂停、回溯、重置、倍速、选择与镜头跟随。

临时夹具仅在开发会话中使用，退出时恢复函数、相机、配置与输入引用，刷新后复验生产入口。不得修改 Worker、`_applySnapshot()` 或存档季节字段来伪造端到端通过，不增加永久入口、服务器写文件接口或持久化测试脚本。

### 3.2 三个样板的选择

1. 以 seed 42 作为首个候选，读取实际生成的 profile 和 accents；它只是搜索起点，不保证有合适构图。
2. 在真实 accents 中分别筛选 `kind === 'Tree'`、`kind === 'Bush'` 且 `AccentModel.get(accent).evergreen === false` 的对象。岩石必须为 `Boulder`，不能用 RockCluster 代替。
3. 优先选枝形清楚、无遮挡、非边界的普通地表个体。另选真实坡地/视口边缘位置做补充；没有合适对象时记录并换种子，不编造对象 ID。
4. 保存 `kind/id/x/y/z/scale/rotation/tint`、模型风格版本、实际季相 profile 与配置。所有季节和镜头对照保持同一对象身份。
5. 不通过全局将 `accentEvergreenChance` 置零筛选样板。模型会缓存 `evergreen`，修改该配置而不清理缓存会让观察失真。常绿保护检查另外选一株实际常绿对象，不计入三对象主样板。

### 3.3 基线清单

每次正式取证记录：源码提交及未提交补丁、应用/生成器/存档版本、两份 WASM 的 SHA256、完整模拟/渲染/光照配置、seed、实际 profile、tick、同版本存档及 SHA256、三个对象身份；设备/操作系统/Chrome 版本、GPU 或软件光栅模式、显示刷新率、CSS 视口/画布尺寸/实际 DPR、人口/房屋/可见装饰和景观子图元数量、倍速、相机参数、原始季相和实际世界光向。

建议首轮统一 1600×900 CSS 视口，DPR 固定为设备可稳定复现的值并如实记录。不要把 CSS 尺寸当作画布像素尺寸，也不要把其他任务的虚拟显示设备成绩当作本机绝对值基线。

## 4. 任务序列

按以下顺序实施；每项结束填写证据路径与结论，不能只勾选代码完成。预计 4–7 人日，不含跨任务性能问题的大规模修复。

| 编号 | 任务与具体动作 | 依赖 | 交付物与退出条件 | 估时 |
| :--- | :--- | :--- | :--- | :--- |
| TA-05-1 | 固化工作区、版本、配置、WASM 与运行设备；核对已有受光路径；建立修改前性能基线 | TA-03、TA-04 | 基线清单完整；记录既有高密拖动超标，未将其豁免 | 0.5 日 |
| TA-05-2 | 筛选落叶 Tree、落叶 Bush、Boulder，建立临时受控样板与真实场景镜头 | 05-1 | 三对象身份表、基准截图、恢复方法；确认真实生产绘制被调用 | 0.5 日 |
| TA-05-3 | 跑四季中心、初春/深秋过渡、四个季节交界与年度相位回环；记录叶量及可见簇 | 05-2 | 季相对照、连续过程证据；冬枝/春芽/秋色可辨，无整冠透明替代落叶 | 0.5–1 日 |
| TA-05-4 | 四方位×俯角×缩放矩阵；固定季相变光向与固定世界光转相机；检查坡地和视口边缘 | 05-3 | 视觉矩阵与缺陷清单；树/灌木/岩石受光、接地和基础遮挡均通过 | 0.5–1 日 |
| TA-05-5 | 真实快照推进、暂停、LOAD、REWIND、RESET、光照开关和高倍速；抽查景观复用模型 | 05-4 | 生命周期记录；新世界/读档结果与同输入冷重建一致 | 0.5–1 日 |
| TA-05-6 | 初始/高密×固定/移动四场景正式 A/B；采集帧耗时、吞吐、缓存、重建首帧 | 05-1、05-5 | 原始样本与 p95 报告，所有门禁有结论；缺陷修复后重测受影响场景 | 1–1.5 日 |
| TA-05-7 | 关闭缺陷、复验、删除夹具与临时断言；执行适用门禁；归档并回填任务台账 | 05-3～6 全通过 | 可复现证据包、最终报告；仅满足全部完成条件时标记 TA-05 完成 | 0.5–1 日 |

任何阶段发现代码问题，进入“最小修复 → 受影响矩阵复验 → 性能必要复测”，不是继续截图后将缺陷附在完成报告里。若必须依赖 TA-07/TA-08 的架构工作，登记明确依赖和解除条件，保留 TA-05 未完成状态。

## 5. 四季、镜头与受光技术指引

### 5.1 季相采样

统一使用 `SimTreeTint.sample()`；年度相位由 `yearPhase()` 计算。当前季分箱中心分别对应春 0、夏 0.25、秋 0.5、冬 0.75，`seasonProgress=0.5` 对应各季中心。**冬→春的季节边界不是年度相位 1→0**：两者都要测，不能只覆盖其中一个。

临时数值采样可使用独立只读对象 `{currentSeason, seasonProgress}`，不写回真实 `sim`。四季中心之外，补充初春、晚春、初秋、深秋；在每个季节交界前后取 `progress=1−ε` 与下一季 `progress=ε`（建议 ε=0.001），同时扫过春季中部的年度相位回环。记录个体哈希造成的季相偏移，不能要求不同 ID 同时落叶。

| 观察项 | 判定方法 |
| :--- | :--- |
| 春夏秋冬 | 同株春芽、夏冠、秋色、冬枝可辨；Bush 保留基生多茎形态，不能是缩小乔木 |
| 连续性 | 连续播放加边界数值采样；颜色、叶量、芽量无非预期跳变、NaN 或越界；数值容差按采样步长/曲线斜率说明 |
| 落叶 | 先变色后减叶；按稳定 shed 次序收缩/隐藏，枝条保留，叶簇仍挂真实枝条；无完整半透明冠云 |
| 冬季 | 普通落叶树 `leafDensity` 约 0–0.05，并人工确认冬枝观感；同时记录可见簇数量及收缩程度，不能把叶量参数直接当作像素覆盖率 |
| 岩石 | 固定光照时材质不随植被季相变色；真实四季光照改变导致的岩面明暗/色温变化允许存在 |
| 常绿保护 | 额外常绿个体保持既有冬季轮廓，不因修复落叶样板而被套入落叶曲线 |

### 5.2 镜头矩阵

主矩阵为 **四季 × 四方位 × 两档俯角 × 三档缩放 = 96 个视图/样板布局**。三对象同框时一套 96 图可共同评分；真实对象分镜头时分别覆盖并记录，不能以同框对象被遮住的截图算该对象通过。

建议初值：`rotZ = 0 / π/2 / π / 3π/2`，`rotX = 0.45 / 1.25`，`zoom = 0.6 / 1.5 / 3.5`。这些均在当前相机允许范围内，但只是取景初值；须按对象实际屏幕尺寸核对 `accentDetailLevels()` 的分档，必要时调整缩放并冻结记录。不能仅凭 zoom 数字宣称覆盖全部细节分档。

每组记录 `panX/panY/rotZ/rotX/zoom`。另做完整一圈连续旋转、细节阈值两侧往返缩放、对象逐步移入/移出四边视口。检查簇间跳层、冠顶裁断、锚点悬空/入土、阴影方向和真实坡地接地。基础场景保留一处真实道路、房屋或人物上下文检查误覆盖；复杂树屋人穿插拆分仍由 TA-08 负责。

### 5.3 受光变量分离

1. **固定季相与相机，改变世界光向**：可沿用临时热调 `SIM_LIGHTING.azimuthOffsetDeg` 的方式取四方位，记录实际 `lightDir`；等待光照稳定再截图。检查枝干、叶簇、Bush、Boulder 各面与地面投影是否协调。
2. **固定季相与世界光，旋转相机**：使用暂停且稳定的光照输入或临时固定光输入，不让 `SimLighting.update()` 在采样时覆盖设定。对同一世界面核对漫反射，不要求不同可见面的屏幕像素完全相同。
3. **光投影退化位置**：补充世界光接近视线方向，确认簇亮部平滑回到中心，无归一化抖动；动态光开→关→开后关闭态使用既有固定世界光。

局部修复继续遵守“季相反照率 → 世界法线受光 → 光源色温”；倾干剪切/非均匀缩放后的法线须正确变换。不能恢复固定屏幕左上亮斑、按相机深度决定漫反射或在几何缓存中烘焙季节/光向。

## 6. 生命周期与缓存验证

| 场景 | 操作 | 必须记录的结果 |
| :--- | :--- | :--- |
| 真实四季推进 | 正常 Worker 快照跨季，保留 tick 和原始季相 | 植被随真实快照变化；光照允许既有限速，叶量不读取平滑光相 |
| 暂停/继续 | 暂停后间隔取样，再恢复 | 季相与模型不漂移；暂停前尚未收敛的光照单列观察，不把光照收敛误报成落叶漂移 |
| LOAD | 用实际 Chrome 存档入口载入同版本样板档 | 记录 LOAD_RESULT、对象/季相/模型重建、选择与镜头状态；截图对应所加载的真实档 |
| REWIND | 推进后回溯到记录 tick，再继续 | 对象几何、季相及可见簇回到对应状态，无未来缓存残留 |
| RESET / READY | 换种子、重新建世界、重新打开页面 | 新世界身份生效；不按相同 profile 或 epoch 误复用旧模型 |
| 冷暖缓存 | 同世界同配置同相机下，暖缓存结果与清理后重建对照 | 几何摘要一致、画面无可见差异；光照先稳定，不能混入不同光相 |
| 高倍速 | 普通倍速与最高可用倍速往返 | 无非预期频闪、错误缓存或输入失效；记录 tick/s，不用降低 tick 掩盖渲染成本 |
| 交互/共享模型 | 选择真实人物/房屋、跟随与关闭 Inspector；观察使用共享模型的资源景观 | 点击位置正确；关闭 Inspector 同时停止跟随；公共模型修复无景观回退 |

Chrome 自动化若无法完成原生文件对话框，保留该项为待验证并安排真实交互补测；REWIND 同路径证据、`test-determinism.js` 存读档结果只能作为补充，不能写成真实 LOAD 已通过。`?nogate=1` 只适用于受控视觉/性能会话，不能证明文件存档链路。

静态通道继续遵循 `null` 表示未发送、明确静态帧整组替换的契约。模型清理由四个世界生命周期消息负责；字符串表的 `start_index==0` 判据仅管字符串缓存，不能代替模型清理。调几何或常绿相关配置时显式重建模型；真正改变模型生成规则才评估模型风格版本递增。

## 7. 性能测量方案与超标处置

### 7.1 对照关系

以 TA-05 开始前的实际代码为 A，最终候选为 B。相同设备、浏览器、配置、负载起点与镜头轨迹，预热建议至少 12 秒，**每场景每端正式采样至少 60 秒**，建议至少三轮并交替 A/B 顺序。测试时不并行跑其他基准，截图/录屏与性能采样分开。

若仅交付验收材料、未改渲染代码，不伪造“新增耗时为零”的成绩：报告现状绝对耗时、重复测量波动与历史未决问题。若升版使存档不兼容，两端分别以相同配置和种子建档到同 tick，对比排除版本元数据后的世界负载；不得篡改存档版本绕过门禁。负载无法对齐时说明限制，不能给出严格差分结论。

小样板只用于归因，完整场景才用于性能门禁：

| 场景 | 负载与相机 |
| :--- | :--- |
| P1 | 初始世界，固定镜头 |
| P2 | 同一初始世界，连续平移/旋转轨迹 |
| P3 | 真实演化产生的高密世界，固定镜头 |
| P4 | 同一高密世界，连续移动镜头，包含最差近景与方向 |

高密档记录人口、房屋、实体和可见数量，不凭“跑了若干秒”命名高密。两端运行测量从同 tick 重新加载，各轮固定起点和运动轨迹；另做暂停同帧比较以隔离模拟演化影响。

### 7.2 计时边界与统计

- **整帧主线程**：实际执行帧从 `frameStart` 到帧末；另外记录 rAF 间隔/FPS。`render()` 有帧率门控，提前返回回调不是绘制帧，不能放入绘制分位数稀释成本。
- **渲染与 UI**：从 `sim.tick()` 返回后的 `tickEnd` 到帧末，与现有调试计时含义一致；不使用 HUD 指数平滑均值冒充 p95。
- **统一队列**：完整包裹 `drawWorldEntities()`，包含收集、投影阴影入队、排序、分发和绘制。新增阴影不能从候选端计时边界剔除。
- **装饰归因**：分别累计单帧 accent、资源景观及阴影成本，必要时细分 Tree/Bush/Boulder；每帧总和形成样本。不能对每次 draw 调用求 p95 后当作每帧成本，也不能相加各模块 p95 代替整体 p95。
- **吞吐**：真实 tick 增量/墙钟秒数，固定倍速和负载；补充既有无头工具以定位内核问题，但无头成绩不能替代浏览器主线程争用测量。
- **缓存/重建**：记录模型/视觉缓存保留量，浏览器堆快照作辅助。总 JS 堆不等于新增视觉缓存；单次冷启动也不足以计算 p95。LOAD/RESET/相机跳变各重复采集，建议每类至少 20 次，报告次数与原始首帧值。

每组保存原始毫秒序列、样本数、p50/p95/p99、均值、最差值及运行顺序。p95 建议固定 nearest-rank 口径：升序样本下标 `ceil(0.95×N)−1`；增量为 `p95(B)−p95(A)`，同时列 A/B 原值。计时封装保持两端一致，预分配采样存储，采样时不逐帧打印日志；用无插桩短对照检查观测开销。

### 7.3 门禁与已有风险

预算引用 07 号 §11.3：参考桌面设备渲染/UI p95 ≤16 ms；新增地表/装饰绘制 p95 增量 ≤3 ms；同设备同负载仿真吞吐回退不超过约 5%；新增视觉缓存建议上限 64 MiB；生命周期首帧单列与基线比较，不隐藏于稳态平均值。

**已有风险必须继承**：07 号已有 TA-04-8 首轮成绩，且高密拖动最差视角存在超标；文中“至今没有实测成绩”与后续实测段落不一致，应以后者的实际记录为依据，不能据前一句省略历史结果。此前测量横跨其他地形/景观变更，不能直接归因于 TA-05，也不能把现有超标当作默认豁免。

超标处理顺序：确认样本/负载/计时边界 → 分解地形、景观、装饰、阴影和 UI → 修复本项引入的多余计算或分配 → 同场景复测。需要 TA-07 的 LOD/包围体或其他任务优化时明确依赖；不得减少模拟步长、只展示三株低负载结果、删掉最差镜头或直接调宽预算。确需调整预算，先提供完整实测与取舍，取得用户明确接受后再改权威预算与结论。

## 8. 交付物与证据组织

建议证据包名 `TA05-evidence-<日期>-<提交>`。大型截图、存档和采样文件放仓库外的固定位置；精简报告在收口时按文档分层归档，记录证据实际位置与校验和。下列为待生成目录约定，不是已存在文件：

```text
manifest.json          # 环境、版本、配置、世界、对象、镜头、文件哈希
saves/                 # 初始与高密同版本真实存档
visual/fixture/        # 受控季相/光向与主镜头矩阵
visual/world/          # 真实快照、上下文与生命周期
metrics/               # A/B 原始帧数据、缓存/吞吐/首帧记录
report.md              # 结论、缺陷、修复复验及门禁输出摘要
```

截图名包含路径类型、对象/布局、季节与进度、相机方位/俯角/缩放、光向、tick，细节过长时用唯一 case ID 对应 manifest。每个 case 标注 PASS / FAIL / NOT_RUN，并记录期望、实际、证据路径、缺陷归属和复验状态；NOT_RUN 不算通过。

最终报告必须能回答：三个对象是谁、在哪个版本重建、哪些截图是夹具、真实 LOAD 是否验证、最差性能场景是否通过、还存在哪些阻断。视觉主矩阵全绿但性能或生命周期不完整时，只能声明对应部分通过。

## 9. 实施纪律与文档收口

实施前阅读 [根 AGENTS.md](AGENTS.md)、[frontend/AGENTS.md](frontend/AGENTS.md)、[工作流](docs/current/tech/30-workflow.md)、[浏览器指南](docs/current/tech/27-browser-automation.md)、[性能指南](docs/current/tech/25-benchmarking.md)。测试优先 Chrome；3004 已有服务时复用；临时浏览器会话退出时只清理自己的会话。

| 变更类型 | 必做事项 |
| :--- | :--- |
| 本文方案或纯验收文档 | `git diff --check`、`doc-maintenance-check.js`、`doc-link-check.js`、`cross-doc-check.js`、`bump-version.js --check`；不升版、不构建、不新增版本 changelog |
| 前端修复/视觉配置调整 | `frontend-check.js` 与对应视觉/性能复验；按全局规则用升版器，版本常量改动后构建 WASM、同步双副本并跑必要门禁 |
| 升版或 Rust 改动 | 按构建指南运行 `cargo build` / `cargo test --lib`、`test-wasm.js`、`config-check.js` 及适用确定性门禁，核对 WASM 双副本哈希 |
| 意外涉及快照/模拟配置 | 先重新审视是否超出 TA-05 范围；确需修改时遵守四处同步、`snapshot-check.js`、完整配置契约及确定性回归 |

纯视觉常量继续放 `config.render.js`，公共光源常量放 `config.lighting.js`。文件不得超过 800 行；不消费模拟 RNG，不新增持久化测试，不把调试采样写进正常玩家 UI。

收口时，07 号 §1.2 回填 TA-05 完成状态和证据入口，§11 补充实际验收与性能结果；若实现改变机制，同步对应 `docs/current/` 中层文档与受影响局部指南，升版时再写 changelog。TODO 仅更新本项相关状态，不清空其他在办事项。本文完成记录未来并入权威文档后，再决定是否删除根目录临时方案，避免长期形成第二份任务台账。
