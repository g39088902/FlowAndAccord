# Keep crates.io as the default source.  The repository does not track a
# `.vendor/` directory, so a global source replacement would make fresh CI
# checkouts fail before Cargo can download dependencies.
#
# Developers who have a complete local vendor directory and need an offline
# build may opt in locally (for example with `cargo --config ...`), rather
# than committing that machine-specific source replacement.
