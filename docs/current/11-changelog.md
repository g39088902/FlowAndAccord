# 📜 版本演进记录 (Changelog)

> **模块索引**：[← 返回 CURRENT.md 全景索引](../CURRENT.md)
> 本文件为里程碑级变更记录，按版本号正序排列。最新版本：**v1.13.0**。
> 实现细节与验证数据已精简，如需追溯请查阅 git 历史。

---

## 核心机制变更

| 版本 | 核心变更 | 影响模块 |
| :--- | :--- | :--- |
| **v0.9.24** | 随身金币遗产继承：族人故去后 `carried_gold` 平分给在世子一代子女，无子女则清零 | agent / ecology |
| **v0.9.34** | 房屋系统与世界环境模块化解耦：`housing_system` 拆为 5 个单一职责子模块，四季回归 `world.rs` | housing_system / world |
| **v0.9.35** | 简化四季参数模型：废除 `season_quarter_length`，单季长度由 `year_length × 0.25` 派生 | config / world |
| **v0.9.37** | 婚姻系统与建房事件解耦：移除升级竣工即时迎娶钩子，成婚统一由 `marriage.rs` 每 tick 扫描匹配 | housing_system/marriage |
| **v0.9.39** | 混沌系统测试策略落地：不再持久化保存单元测试脚本，长期确定性验证由 `test-wasm.js` 承担 | 工程规范 |
| **v0.9.42** | 开局人口 12→20（10男10女），百家姓库 60→150 | ecology / agent / frontend |
| **v0.9.43** | 建房/升级/修缮全流程回归 Agent 自主决策：废除系统发房扫描器，新增 `FoundHome` 需求 | decisions / housing_system |
| **v0.9.44** | 决策概率全部收敛为确定性执行（无掷骰），立宅选址参数全部入 `SimConfig` | decisions / config |
| **v0.9.45** | 决策模块子目录化拆分：`evaluator.rs` 拆为 7 个子模块（needs/routing/evaluate/harvest/seeking/scheduler/mod） | decisions |
| **v0.9.47** | 超高倍速支持（256x/512x/1024x），建房最小间距 14→28m | frontend / config |
| **v0.9.48** | 立宅优先复用空置路网节点：候选宅址 20m 半径内若存在空置节点则直接复用，防止代际更替后节点膨胀 | housing_system/settlement / graph |
| **v0.9.49** | 调试模式监视器：Tick/FPS/内核耗时/快照耗时/CPU/内存/WASM 内存九项指标，200ms 刷新 | frontend |
| **v0.9.50** | 道路衰减速率翻倍（线性模型阶段参数调整，后于 v0.9.66 改为比例模型） | graph / config |
| **v0.9.51** | 步行速度受力量禀赋加成（后于 v0.9.65 重构为力量直接乘率） | agent / config |
| **v0.9.52** | 视图显隐改造：新增隐藏部落民/隐藏路网开关，移除 POI 指示环开关（改为恒显） | frontend |
| **v0.9.53** | 死亡数区分自然死亡（寿终正寝）与非自然死亡（饿死/渴死），顶栏分列统计 | agent / snapshot / frontend |
| **v0.9.56** | 无头模式顶栏常驻更新 + 每秒 Tick 监视 + 空格键全局暂停（不受控件焦点残留影响）+ 族谱拖拽修复 | frontend |
| **v0.9.57 ~ v0.9.62** | 族谱系统迭代：全量血脉单图 → 力导向布局 → 纯力学布局 → 出生时序纵向重力 → 无惯性收敛 → 直系血脉裁剪（仅焦点祖先+后代链）→ 焦点暗红卡片配色（最终于 v0.9.64 被时间轴布局取代） | frontend/dag |
| **v0.9.63** | 全量消除 magic number：161 个超参收口 `config.js`，新增 `config-check.js` 前后端一致性校验与 `config-reference.md` 自动生成速查表 | config / frontend / tools |
| **v0.9.64** | 族谱时间轴布局重构：彻底废除力导向，Y 严格线性映射出生 tick + X 冲突横向扩展 + 视口虚拟化 LOD + 时间刻度尺，拆分为 `dag-layout/view/standalone/dag` 四模块 | frontend/dag |
| **v0.9.65** | 行走速度重构：所有 agent 共用默认速度 + 力量直接乘率（无 clamp），速度不再受体力影响，清理 7 个废弃超参，`SimConfig` 161→154 | agent / config |
| **v0.9.66** | 道路衰减改比例模型（`wear × (1 - rate×dt)`），清理越野惩罚死机制，前端道路 tooltip 全部去硬编码读 `SIM_CONFIG` | graph / frontend / config |
| **v0.9.68 ~ v0.9.70** | CI/CD 自动部署流水线：GitHub Actions 编译 WASM → `test-wasm.js` 门禁 → 腾讯云 COS 增量上传，含 `.wasm` MIME 覆写、Secrets 格式预检与 DNS 预解析 | .github / docs |
| **v0.9.71** | 3 级木石庄舍图标去重：🏛️→🏯，与营地县级行政区图标区分 | frontend / housing_system |
| **v0.9.72** | **账本与婚姻登记系统 M1 奠基**：新增 `ledger/` 模块（Ledger 双环形流水 / Group 团体基类 / MarriageRegistry 婚姻登记簿 / HouseholdRegistry 家户体系），胎儿预分配 AgentId，与物理仓库完全分离 | ledger（新模块） |
| **v0.9.73** | 账本前端 UI 展示：快照扩展 `HouseholdSnapshot`/`MarriageSnapshot`/`LedgerBalanceSnapshot`，顶栏存续家户/婚姻统计 + Inspector 家户归属与婚姻登记卡片 + 家户与账本大盘可折叠面板 | snapshot / frontend |
| **v0.9.74** | 家户与账本大盘面板可点击性修复：补齐 `pointer-events:auto`，CSS 重写对齐全局均值大盘/图例窗口风格 | frontend |
| **v1.0.0** | **里程碑：账本与家户/婚姻系统 M1 完成**，版本策略升级（M 里程碑递增次版本号，Bug 修复/文档更新递增修订号） | 全项目 |
| **v1.0.1** | 全量文档重构精简：修复版本号/POI 数量/字段数等失同步，压缩 changelog（58KB→里程碑级）与规划文档，新增账本模块文档，统一文档分层放置策略 | docs |
| **v1.0.2** | UI 全景剖析与新功能设计规范文档：编写 `docs/UI_SPEC_AND_LEDGER_DESIGN.md`，深度解剖当前 Canvas 渲染/顶栏/大盘/Inspector/族谱时间轴，设计 M2-M4 4标签页制度大盘（家户/婚姻/宗族/地区）原型与 ASCII 原型图，建立快照三处同步清单与前端开发指南 | docs / frontend |
| **v1.1.0** | **M2 账本内核完成**：旁路记账 `tick_bookkeeping`（Deposit/Consume/Heating 增量观测法，只读物理事件只追加流水）、分家抽资（成年/丧父男子分走 1/(2+n) 资源立新户，Split 流水）、父亲死亡继承（资源平分在世子一代/绝嗣入公仓 `public_granary`，Inheritance 流水后解散）、快照扩展（`TransferRecordSnapshot`/`recent_journal`/`marriage_history_count`/`household_id`/`household_role`/`public_granary_balances`）、出生入家户钩子、Construction/Maintenance 记账钩子 | ledger / world / bookkeeping（新模块） / snapshot / birth / housing_system |
| **v1.2.0** | **M3 宗族内核完成**：宗族聚合 `ClanRegistry`（按姓氏自动聚合，始祖播撒即入族/新生儿随父姓入族）、族长顺位（同姓在世最年长男性，并列按 id 取小，无男性则无主账本冻结，Succession 审计事件）、族税 `Tribute`（每 1800 tick 全局统一征收，存续家户按账面余额×5% 缴纳，只记账不扣物理库存）、族内互助 `MutualAid`（族库总余额>50 时对水+粮<10 的极贫家户拨付 min(族库×20%, 缺口×2)，每家户 900 tick 冷却，族长签字）、快照扩展 `ClanSnapshot`（surname/leader_id/member_ids/balances/recent_journal/recent_events）、`LedgerRef::Clan(String)` 变体、5 个新超参（clanTributeRate/clanTributeIntervalTicks/clanMutualAidMinBalance/clanMutualAidFamilyThreshold/clanMutualAidCooldownTicks），超参总数 153→158 | ledger/clan（新模块） / ledger/group / ledger/journal / world / snapshot / ecology / birth / config |
| **v1.3.0** | **M4 地区与王国内核完成**：地区团体 Region（GroupKind::Region(u32)，每营地一册，国王=leader/公仓=ledger/政体=Kingdom/继承制=Primogeniture）、到达时序 arrival_tick（始祖=0/新生儿=出生tick，arrival_order按(arrival_tick,agent_id)升序）、初王顺位（arrival_order最早到达在世男性，无男性则王位空悬账本冻结）、夺位远征 SeekingThrone（决策树最高优先级，男性非国王冲向最近无主营地登基，走现有寻路+运动系统坐标连续不闪现，中断施工build_timer冻结不回滚，行囊保留不卸货，途中目标易主则重定向/放弃，抵达即登基全图播报）、长子继承制（国王死亡→在世最年长儿子→孙子→arrival_order下一男性→绝嗣空悬，胎儿不计入继承）、公仓税 Tax（每2400tick全局统一征收，存续家户按账面余额×3%缴纳，只记账不扣物理库存，有国王地区才征税）、救济 Relief（公仓总余额>30时对水+粮<8的极贫家户拨付min(公仓×15%,缺口×2)，每家户1200tick冷却，国王签字）、快照扩展 RegionSnapshot（camp_id/camp_name/king_id/regime/succession/member_count/arrival_order前10/heir_candidates前3/balances/recent_journal/recent_events/active_expedition_agents）+ AgentSnapshot.arrival_tick/is_on_expedition、LedgerRef::Region(u32)变体、5个新超参（ledgerTaxRate/ledgerTaxIntervalTicks/ledgerReliefMinBalance/ledgerReliefFamilyThreshold/ledgerReliefCooldownTicks），超参总数158→163 | ledger/region（新模块） / ledger/group / ledger/journal / agent / world / snapshot / ecology / birth / decisions / config |
| **v1.4.0** | **M6 房屋去仓储化 · 家户账本 = 家庭物资唯一真相源（分阶段平滑重构）**：① 写侧账本化——生态 `RestingAtCamp` 卸货/吃喝与 `maintenance` 冬季烧柴全部改为从**家户账本**真实收付（Deposit/Consume/Heating 流水），删除 `bookkeeping` 的 Deposit/Consume/Heating 旁路观测（仅保留继承/分家清算）；② 读侧账本化——决策层（branches/needs/harvest/seeking）与 `current_need` 状态标签全面改读**家户账本余额**（`Decisioner` 注入 `&HouseholdRegistry`），新增 `needs::stock_goal/upgrade_ready` 以「等级目标基准」（原 house_capacity_tier×ratio，数值不变）取代 `House::is_pantry_full/is_fertility_active`，**取消仓储容量上限**；③ **婚姻/生育去房屋化**——自动成婚由「遍历私宅找单身户主」改为「遍历存续家户户主就近配对」（无房可婚、女方随夫入家户并共享男方住所），受孕移除 `fertility_active`（房屋等级/仓储备货）门禁，成年已婚女指标达标即可受孕（无房可育）；④ **房屋升级瞬时化**——删除施工工时/体力链（`house_build_time_*`、`agent_construct_stamina_burn`、ConstructingHouse 计时与体力燃烧），`BuildHouse` 决策就绪后由房屋系统**一次性扣账（Construction）+ 瞬时晋升 + 事件播报**，无体力/时间投入；⑤ **威望（prestige）落地**——`Agent3D.prestige` 改为持久综合分值（所有影响因子集合体：子嗣活产父母各 +1、宅邸每晋升一级户主 +1，子女死亡不回减、不随房屋/家户转移），快照透传取代 `children_ids.len()` 派生；全 UI/文档废弃「宗族声望」称谓改称「威望」；⑥ 拆除物理层——`House` 删除 `pantry_*/max_pantry_*` 十字段及 `HouseSnapshot` 对应字段与 fertility/full 标记，前端房屋标签/Inspector「库存」改展示**户主家户账本五类余额**，`config.js`/`config.rs` 删除 houseBuildTime*/agentConstructStaminaBurn（超参 165→160），`config-check.js` 双绿 | house / ecology / maintenance / construction / marriage / bookkeeping / agent / birth / world / snapshot / decisions/branches / decisions/needs / decisions/harvest / decisions/seeking / decisions/evaluate / decisions/scheduler / config / rustworld.js / render.js / index.html / config.js / decision-viz-data.js |
| **v1.5.0** | **M7 家庭库存施密特触发器：去采货与房屋等级彻底脱钩**：① 五类资源（水/粮/木/石/金）"是否去 POI 采货并存入家户账本"改为家庭账本余额驱动的**施密特触发器**（有房含 0 级非废墟即可采）：余额 < `decisionFamilyStockTriggerOn`(100) → ON 去采；一旦 ON 须补到 ≥ `decisionFamilyStockTriggerOff`(200) 才 OFF；新增两个超参（config 162 字段）；② 触发/停止判定替换：branches b5/b6/b7/b9/b10 与 harvest.rs 现场去停（decide_drinking/foraging/harvest/mining_gold）全部改读 `Agent3D.family_stock_active`（每 agent 五布尔，`refresh_family_stock` 于 decide 开头每拍更新），`seeking.rs` 金中断冷却区分同步，删 `stock_goal/stock_needs/household_capacity/upgrade_ready(旧)/gold_is_stock/HouseStockNeeds` 全套等级目标判定；③ **升级就绪改按材料成本**：抽出共用 `needs::upgrade_material_cost(tier)`（0→1 无材料、1→2 木 34、2→3 石 68、3→4 金+石各 102，cap×ratio）供 `construction.rs::try_instant_upgrade` 与 b8/b11 `upgrade_ready_by_cost` 共用（余额≥cost 即就绪，0→1 恒就绪）；④ b13 GoldWealth 门禁再锚为「4 级庄园 + 五类储备全 ≥200（trigger 全 OFF）+ 无修缮 + 冷却 180s」，b9/b10 层级降为 family_level（Safety 语义，不再升级导向）；⑤ 无房者维持不可采、现场只自用不装袋；⑥ 前端 decision-viz-data b5~b13 条件文案、render NEED_KIND_REASON 更新，版本号 v1.5.0 | decisions/branches / decisions/needs / decisions/harvest / decisions/seeking / decisions/evaluate / housing_system/construction / agent / config / config.js / decision-viz-data.js / render.js / index.html / docs |
| **v1.5.1** | **Agent 启动加速文档体系（P0）**：① 新增 `docs/current/13-impact-matrix.md` 跨模块影响矩阵——9 大机制域（行囊/快照/决策/账本/房屋/生态POI/Agent生理/配置/构建部署）的"改 X 牵动哪些文件"速查表 + world.rs tick 实际调用顺序（含胎儿协调/金币遗产/bookkeeping/clan/region 全步骤）+ Rust→前端数据流向图 + 14 脚本加载顺序 + 改动前自检清单；② 新增 `crates/sim_core/src/spatial/AGENTS.md`——spatial 核心层 14 散文件+3 子目录职责边界表、agent↔ecology 装载卸货契约、bookkeeping 与 ledger 分工边界、快照映射责任、5 个局部易踩坑（agent_index 同步/胎儿特殊处理/WorldRng 确定性/world.rs 行数预警/生态重置全量清空）；③ 新增 `frontend/AGENTS.md`——14 JS 文件+server/index/style 职责边界表、脚本加载顺序约束（决策三件套须早于 rustworld.js）、渲染管线数据流、DOM ID 共享契约表、7 个局部易踩坑（render.js 2130 行超标/快照三处同步/配置热注入时序/agentArchive 全量档案/镜头跟随联动/server.js 写盘端点/wasm 双副本）、与 Rust 内核的 wasm 接口对照表；④ 根 AGENTS.md §0.1 新增 spatial/ 与 frontend/ 两行索引，CURRENT.md 模块导航新增 13 号条目，版本号 v1.5.0→v1.5.1 | docs / crates/sim_core/src/spatial / frontend / AGENTS.md |
| **v1.6.0** | **M8 房屋升级材料成本矩阵化（固定值）**：① `needs::upgrade_material_cost` 由「容量×比率推导」（1→2 木 34、2→3 石 68、3→4 金+石 102）改为**每级固定数值矩阵**——升到 1 级水/粮各 50、2 级木/粮/水各 75、3 级石/木/粮/水各 100、4 级金/石/木/粮/水各 125（同级同值、品类随等级递增、该级不消耗品类填 0：扣账 `amt>0.001` 守卫自动跳过、就绪 `balance≥amt-1e-3` 恒真不阻塞）；② **20 个新超参** `house_upgrade_cost_tier{1..4}_{water,food,wood,stone,gold}`（config 162→168 字段）：Rust `config.rs` 保持平铺（const/字段/Default 三处分区集中），前端**独立拆分配置文件** `frontend/js/config.house-upgrade-cost.js`（`window.SIM_HOUSE_UPGRADE_COST`，index.html 加载于 config.decision-order.js 之后、rustworld.js 首次 applyConfig 前经 `Object.assign` 合并注入）；③ **工具链纳入拆分文件**：`tools/config-check.js` 把拆分配置并入前端字段集比对（防"Rust 有/前端无"误报）、`tools/test-wasm.js` 合并注入真实矩阵（防 WASM 拿 0 成本默认）；④ **删除 14 个废弃字段** `houseCapacityTier0..4`、`houseUpgradeTier{0..3}*Ratio`、`houseFertilityStockRatio`（config.rs 与 config.js 四处同步清理，render.js 不再引用 capacity 分母）；⑤ **行为变化**：0→1 从"无材料恒就绪"变为需账本水≥50 且粮≥50（有房含 0 级可采货囤积，可达性不受影响）；⑥ 房屋 Inspector 水/粮/木/石/金五行储备改**纯数值显示**（移除进度条 DOM 与百分比分母，耐久度条保留），升级条件/图例/b8-b11 文案同步新矩阵；⑦ 版本号 v1.5.1→v1.6.0 | needs / construction / branches / config / config.js / config.house-upgrade-cost.js / rustworld.js / render.js / decision-viz-data.js / index.html / style.css / tools / docs |
| **v1.6.1** | **P1 工程校验工具链（代码地图 + 快照三处同步）**：① 新增 `tools/code-map-check.js`——扫描实际文件树（crates/frontend/tools/.github/docs）与 `docs/current/09-code-map.md` 登记做交叉对比，捕获「文档缺失/文档过时/描述关键词漂移（如仍写"仓储"但已删 pantry）/嵌套 AGENTS.md 缺失」四类问题，括号深度匹配解析 Rust 构造块避免 `format!("{:?}")` 截断，构建配置文件（Cargo.toml/package.json）不强制登记；首次运行发现并修复代码地图 10 处缺失（branches.rs/config.decision-order.js/config.house-upgrade-cost.js/decision-viz 三件套/13-impact-matrix.md/code-map-check.js）+ 2 处描述漂移（house.rs"仓储"过时、SimConfig 字段数 163→169），修复后 0 错误 0 警告；② 新增 `tools/snapshot-check.js`——校验快照字段三处一致性（snapshot.rs/house.rs 结构体定义 vs world.rs generate_snapshot() 赋值 vs rustworld.js _applySnapshot() 映射），捕获「Rust 定义了但未赋值/赋值了但未定义（拼写错误）/前端读取了但未定义（拼写错误）/前端未读取（可能遗漏展示）」，支持 Rust 结构体简写语法（`balances,`）、WorldSnapshot3D 命名、JS map/reduce/for-of/直接索引四种映射模式，前端自添加派生字段（如 reverseId）白名单排除；首次运行 13 个结构体 0 错误（19 警告均为前端合理未读取字段）；③ 代码地图同步登记两个新工具，版本号 v1.6.0→v1.6.1 | tools / docs/current/09-code-map.md / docs/current/11-changelog.md |
| **v1.8.0** | **M9 读档/存档系统：世界全量状态 JSON 持久化**：① 内核新增 `spatial/world_save.rs`——`WorldSave` 契约 + `serialize_save`/`deserialize_save`（格式版本门禁 + `grid_res`/`world_size` 参数校验 + agent id 唯一性校验 + 按 seed 重建地形 + `rebuild_agent_index()` 重建派生索引）；② 序列化能力补齐——`WorldRng` 补 serde 持久化内部 `state`（确定性核心）、`LaneGraph3D` 手写 serde（节点/车道按插入顺序扁平化、反序列化同序重建，保证 A* 邻接表边序一致）、`PrimitivePoi` 的 `current_stock`/`max_stock`/`regen_rate` 走 `finite_f32` 助手（营地储量恒为 `INFINITY`，serde_json 默认序列化为 `null`，会导致「存档能写、读不回来」）；③ 显式排除可重建字段（`terrain` 按种子重建、`agent_index` 读档重建），实测存档体积约 392 KB；④ WASM 新增 6 个导出：`world_save_ptr/len`、`world_save_buf_ptr`、`world_load`（0 成功 / -1 越界 / -2 非 UTF-8 / -3 解析或校验失败）、`world_last_error_ptr/len`；⑤ 前端 `rustworld.js` 新增 `saveWorld()`/`loadWorld()`/`readSaveError()`（读档后清空轨迹/先祖档案/地形缓存/选中态并强制重建快照，且**不**重注入 `SIM_CONFIG` 以免覆盖存档配置破坏续演语义），新增 `js/save-ui.js`（三槽位：自动槽 60 秒覆盖 + 手动槽 1/2，正文键 `flowaccord.save.v1.<slot>` + 索引键存元信息避免双倍占用配额，顶栏「💾 存档 / 📂 读档」按钮打开保存/读取双标签面板，支持覆盖保存/读取/删除/导出/导入，读档后自动暂停并同步暂停按钮文案）；⑥ `tools/test-wasm.js` 新增 Test 3 存档读档确定性（存档点续演与不中断连续运行快照逐字符串相等 + tick 复原）与 Test 4 版本不兼容拒绝加载且不污染当前世界 | world_save.rs（新文件） / rng / graph / poi / spatial/mod / sim_wasm/lib.rs / rustworld.js / save-ui.js（新文件） / index.html / style.css / tools/test-wasm.js / docs |
| **v1.8.1** | 修复 v1.7.1 render 拆分回归：`render_canvas.js` 中 `const w/h` 因块级作用域仅存在于 `render()` 内，拆分出的 `drawTerrain()` 等绘制函数访问不到导致 `ReferenceError: w is not defined`、地形无法渲染；将 `w/h` 提升为该文件全局共享状态（每帧在 `render()` 内更新），恢复地形网格/视口裁剪正常绘制 | render_canvas.js / docs || **v1.7.1** | **P2 代码体量治理与文档可发现性提升（4 项）**：① **world.rs 拆分**（881→169 行，拆为 5 文件）：`world_tick.rs`（tick 管线调度+胎儿对账+金币继承，275 行）/ `world_snapshot.rs`（generate_snapshot 快照生成，413 行）/ `world_config.rs`（配置注入与反序列化，47 行）/ `world_season.rs`（四季温度计算，37 行）/ `world.rs`（结构体定义+构造+工具，169 行），Rust 多文件 impl 块分散，mod.rs 注册 4 个新模块，cargo build + test-wasm.js 全绿；② **render.js 拆分**（2128→5 文件，单文件均 <800 行）：`render_canvas.js`（共享状态+主循环调度，226 行）/ `render_hud.js`（顶栏/调试/资源大盘/均值大盘/账本面板，491 行）/ `render_world.js`（地形/路网/POI/房屋绘制，480 行）/ `render_agents.js`（族人绘制+登基礼花，206 行）/ `render_inspector.js`（Inspector 面板+点击拾取，786 行），index.html 脚本加载顺序更新，原 render.js 备份为 render.js.bak；③ **配置字段→影响模块映射**：`tools/config-check.js` 新增 `IMPACT_OVERRIDES`（12 个特殊字段）+ `IMPACT_PREFIX_RULES`（60+ 前缀规则）+ `getImpactModule()` 函数，`config-reference.md` 参数表新增「影响模块」列，168 个字段全部匹配（0 个未匹配），示例：`carryCapacityResource`→agent.rs/ecology.rs/decisions/、`decisionPoiSeekMinStockRatio`→decisions/routing.rs/harvest.rs、`houseWinterWoodBurnRate`→housing_system/maintenance.rs；④ **核心不变量集中清单**：新建 `docs/current/14-invariants.md`，将散落在 AGENTS.md §4 各小节的硬约束提炼为 6 大类（确定性 D1-D7 / 数据一致性 C1-C8 / 行为语义 B1-B13 + tick 顺序 / 构建部署 P1-P6 / 代码组织 O1-O6 / 前端 DOM F1-F4），每条标注来源章节与违反后果，末尾附 10 秒快速自检清单；⑤ 版本号 v1.7.0→v1.7.1，cargo build + test-wasm.js + config-check.js 三门禁全绿 | world（拆分） / render（拆分） / config-check / docs/current/14-invariants.md / docs/config-reference.md / index.html / AGENTS.md |
| **v1.8.2** | 修复控制台面板样式溢出：`.control-panel` 固定高度 264px 无法容纳实际内容（约 330px）导致溢出顶出窗口，改为 `height:auto` + `justify-content:flex-start` 自适应内容高度；简化「重演生态」按钮文案为一行（原「重演生态 (20人·10男10女)」过长换行），详情移入悬停提示 | frontend/index.html / style.css |
| **v1.8.3** | **产后休养冷却（分娩后 900s 禁孕）**：新增 `Agent3D.postpartum_cooldown_timer` 与超参 `agentPostpartumCooldown`(900s)——女性完成正常分娩（`pregnancy_progress≥1.0`）即进入 900s 产后休养冷却，`tick_metabolism` 受孕判定短路禁孕、冷却每秒递减归零后可再孕；与既有流产冷却（450s）按发生场景独立设置、互不干扰；快照三处同步新增 `postpartum_cooldown`，前端 Inspector 冷却框优先展示「产后休养剩余 Xs 可受孕」（无则退回流产「调养」文案）；config 字段 168→169，`test-wasm.js`/`config-check.js` 双绿 | agent / config / config.js / snapshot / world_snapshot / rustworld.js / render_inspector.js / config-check.js |
| **v1.8.4** | 修复产后冷却提示文案：Inspector 冷却框前缀原为 HTML 写死的「🥀 流产调养中」，正常生育（产后休养 900s）后也会误显示为流产调养；改为前缀随冷却类型动态切换——正常生育显示「🤱 产后休养中」，流产显示「🥀 流产调养中」，纯前端改动无需重编译 WASM | frontend/index.html / render_inspector.js |
| **v1.8.5** | 移除建筑卡片冗余状态标签：删除 Inspector 标题栏「⚠️ 建筑磨损折旧 (XX% 待修缮)」分支（耐久<85% 时展示），该信息与下方耐久度条（颜色渐变 +「需修缮」文字）重复；删除后耐久<85% 的非修缮房屋状态回落为常规「安居宅邸/起步营地」，耐久度条仍正常提示待修缮，纯前端改动 | frontend/js/render_inspector.js |
| **v1.8.7** | **内核死亡/流产墓碑（recent_deaths）+ 前端档案库死因补记与流产胎儿入族谱**：修复高倍速（≥512x）下死亡族人档案库滞留"健在"、年龄冻结的陈旧 bug（死因丢失）——内核新增 `RecentDeathSnapshot`（id/cause/is_natural/is_fetus/father_id/mother_id/tick），`world_tick.rs` 在死亡结算处与 `tick_fetus_reconcile` 流产/随母亡故胎儿移除处写墓碑，`tick()` 末按 4096 tick 滑动窗口清理；快照三处同步（snapshot.rs / world_snapshot.rs / rustworld.js），墓碑随快照输出；前端 `_applySnapshot` 消费墓碑（`_consumedDeathIds` 幂等去重，init/load 时清空）：①对档案库滞留"存活"副本强制补记 `isAlive=false`+死因（修复 #249 饿死但卡片/族谱显示健在的根因）；②流产/随母亡故胎儿以"已故子嗣"身份入档（isFetus=false，血缘 fatherId/motherId 随墓碑携带——高倍速下胎儿整个生命周期可在单帧内，prevAgents 取不到，必须由墓碑保血缘），族谱可画"💀 死因: 流产"节点；recent_deaths 为瞬态不入存档（world_save.rs 读档补空 Vec，ecology.rs 世界重置清空）；版本号 v1.8.6→v1.8.7 | world_tick / snapshot / world_snapshot / world.rs / world_save.rs / ecology.rs / rustworld.js / dag.js / frontend/AGENTS.md / docs |
| **v1.8.6** | **房屋新增「修建/升级者」历史确权字段（不随继承改变）**：① `House` 新增 `builder_id`（修建者=立宅人，`materialize_founded_houses` 立宅时以户主 ID 固定）与 `last_upgrader_id`（最近升级者，`try_instant_upgrade` 每次升级成功时更新，从未升级为 None）；**父系继承仅改 `owner_id`/`generation`/`spouse_id`，二者天然不受影响**——历史确权与现户主解耦；② 快照三处同步：`house.rs` HouseSnapshot、`world_snapshot.rs` 赋值、`rustworld.js` `_applySnapshot` 映射（`builderId`/`lastUpgraderId`）；③ 前端房屋 Inspector 新增「🧱 修建/升级者」行（修建者 Agent #X · 最近升级 Agent #Y / —）；④ 存档格式升级 `SAVE_FORMAT_VERSION` 1→2（Rust `world_save.rs` + 前端 `save-ui.js` 同步）：新增必填 `builder_id` 使 v1 旧档无法反序列化，按设计红线「版本不兼容即拒绝」干净拦截而非报缺字段；版本号 v1.8.5→v1.8.6 | house / world_snapshot / housing_system/construction / rustworld.js / render_inspector.js / index.html / world_save.rs / save-ui.js / docs |
| **v1.9.0** | **决策引擎夺位远征 + 王国情报 / 宗族绝嗣 / 出生地与 UI 十项改造**：① **夺位远征决策引擎化（Task5/6）**：删除旧远征系统（`tick_conquest_expedition`/`start_expedition`/`expedition_targets`），新增第 14 条决策分支 `B14SeekThrone`（`NeedKind::SeekThrone`，生理层最高档、策展序置首 b14），守卫全部内联（在世成年男性、非现任国王、有房者仅夺自家房屋所在营地空缺王位、无房/废墟可夺任意空缺王位营地），决策器自主选定最近可夺位营地写入 `expedition_target_camp`，途中状态机 `decide_seeking_throne`（体力告警折返 / 抵达王位仍空缺→写 `coronation_pending` / 目标易主→原地掉头重定向 / 无可夺→放弃恢复决策），世界仅保留物理规则执行器 `execute_pending_coronations()`（每拍校验王位仍空缺后登基）；② **无房国王盖房约束（Task7）**：`FoundHome` 候选点须落在其王国营地 `poi_min_distance` 半径内，实体化 `camp_id` 优先取国王营地；③ **出生地（Task4）**：始祖随机落在普通道路节点（`road_nodes`=地形过渡节点，非 POI），`home_camp_node` 取最近营地；④ **王国情报（Task3）**：`Region` 新增 `history_kings` 与 `set_king` 包装（离任/驾崩前任国王入档），`RegionSnapshot` 新增 `history_kings`/`member_ids`/`governed_households`，营地卡片展示国王/继承人/历史国王/王国账本/管辖家庭；⑤ **宗族改造（Task10/11）**：立宗需男性（始祖两遍入族：先男建宗再全员入族），绝嗣（无在世男性）标记 + 族产平分其他存续宗族（无则入公仓兜底），新增 `TransferReason::Legacy`，`ClanSnapshot` 新增 `is_extinct`/`living_females`，前端红色绝嗣标签 + 在世女性统计；⑥ **UI（Task1/2/8/9）**：进度条悬停显示每秒变化速度（饱食/口渴/体力/房屋耐久修缮/怀孕，按游戏秒计量）、无头模式按钮移入重演生态右侧空位（省一行）、agent 卡片/族谱弹窗点击房屋跳转房屋卡片、顶部数据栏窗口水平居中；⑦ **决策策展序 13→14（b14 置首）**：`config.decision-order.js`/`decision-viz-data.js`/`decision-viz.js`/`config-check.js` 同步；版本号 v1.8.7→v1.9.0，cargo build + test-wasm.js + config-check.js 三门禁全绿 | decisions/branches / decisions/evaluate / decisions/seeking / decisions/scheduler / decisions/needs / agent / ecology / settlement / ledger/region / ledger/clan / ledger/journal / snapshot / world_snapshot / world.rs / world_save.rs / rustworld.js / render_inspector.js / main.js / ledger-ui.js / decision-viz.js / decision-viz-data.js / config.decision-order.js / index.html / style.css / config-check.js / docs |
| **v1.9.1** | **宗族与女性彻底解耦（Task10/11 语义收紧）**：宗族回归纯父系男性团体——`ClanRegistry::add_member` 女性一律拒绝入族（此前仅「纯女性不立宗」，同姓女性仍随男入族）；始祖入族由两遍法简化为单遍男性入族，新生儿随父姓入族仅限男性子嗣；绝嗣结算（族产平分其他宗族 / 无则入公仓兜底，`TransferReason::Legacy`）与红色「⛩️ 绝嗣 · 无在世男性」标签不变，但**移除「在世女性统计」**——`ClanSnapshot.living_females` 字段删除（快照三处同步：snapshot.rs / world_snapshot.rs / rustworld.js），前端宗族页删除「♀ 在世女性」标签，绝嗣事件文案去掉「仍可统计在世女性」；版本号 v1.9.0→v1.9.1，cargo build + test-wasm.js + config-check.js 三门禁全绿 | ledger/clan / snapshot / world_snapshot / ecology / birth / rustworld.js / ledger-ui.js / index.html / AGENTS.md / docs |
| **v1.10.0** | **营地房屋上限 + 去绝嗣废弃 + 空置房屋登记（4项机制重构）**：① **营地房屋上限**：新增超参 `campMaxHouses`(30)，`SimConfig.camp_max_houses` 三处同步（const+字段+Default）；B12FoundHome 决策分支新增 `has_nonfull_camp()` 预检（至少一个营地未满才允许立宅），`materialize_founded_houses` 实体化阶段按距宅址距离排序营地、跳过已满营地（国王优先自己王国营地，满则按距离尝试其他），全部满则放弃本次建房；② **删除绝嗣废弃状态与加速风化**：`House.is_ruin` 字段删除，`tick_depreciation` 移除 15x 加速风化分支（无主房屋统一正常 2x 速率），`House.generation` 代际字段删除（继承机制移除后恒为1失去意义）；③ **删除房屋继承逻辑**：`tick_patrilineal_inheritance` 整体替换为 `tick_vacant_house_tracking`（事件驱动：户主死亡当拍检测→房屋标记无主 `owner_id=None`+`spouse_id=None`→所属营地 `vacant_houses` 列表登记 `VacantHouseEntry{house_id, beneficiary_ids}`），受益人为户主所有在世子女（father_id=户主，不分性别不分是否同住）+目前的妻子（house.spouse_id，若在世），按 agent.id 升序去重（确定性）；房屋坍塌时从营地空置列表移除；④ **owner_id 改为 Option**：`House.owner_id` 从 `AgentId` 改为 `Option<AgentId>`（None=无主空置房），全链条适配（winter_heating/repair/marriage/construction/branches/evaluate/seeking/harvest 共10+文件）；⑤ **快照三处同步**：`HouseSnapshot` 删除 `is_ruin`/`generation`、`owner_id` 改 Option；`PoiSnapshot` 新增 `vacant_houses: Vec<VacantHouseSnapshot>`；`world_snapshot.rs` 赋值同步；`rustworld.js` `_applySnapshot` 映射同步（house 删 isRuin/generation、poi 加 vacantHouses）；⑥ **前端 UI**：房屋 Inspector 无主房显示「🏚️ 无主空置房」灰色标签+受益人登记提示，删代际显示；营地 POI Inspector 状态徽章加空置计数、详情文本列出空置房屋+受益人；世界渲染无主房半透明+「空」标签；HUD 房屋统计删 `!isRuin` 过滤；版本号 v1.9.1→v1.10.0 | config / config.js / house / poi / housing_system（全部6文件） / decisions（branches/evaluate/seeking/harvest） / snapshot / world_snapshot / rustworld.js / render_inspector.js / render_world.js / render_hud.js / index.html / AGENTS.md / docs |
| **v1.11.0** | **本地文件存档（File System Access API）：存档直写用户磁盘，突破浏览器存储配额限制**：① `save-ui.js` 新增本地文件存档模块——`connectLocalFile()` 通过 `showSaveFilePicker` 让用户选择一个本地 .json 文件并获得 `FileSystemFileHandle`，后续 `saveToLocalFile()` 经 `createWritable()` 直写磁盘（无需重复弹窗），`loadFromLocalFile()` 从已连接文件读取，`loadFromLocalFilePicker()` 支持不先连接直接打开任意存档文件；② 自动保存智能切换——已连接本地文件时，`tickAutoSave()` 每 60 秒直写本地文件而非 localStorage，彻底规避大存档的 `QuotaExceededError`；③ 兼容性降级——不支持 File System Access API 的浏览器（Firefox 等）自动隐藏连接按钮，读取标签下提供「选择存档文件」降级到传统 `input[type=file]`，底部提示引导使用 Chrome/Edge；④ 权限失效处理——写入/读取时捕获 `NotAllowedError`，自动断开连接并提示用户重新授权；⑤ UI——存档面板顶部新增绿色「💻 本地文件存档」卡片（未连接/已连接/不支持三态渲染），已连接时显示文件名、最后写入时间、存档 Tick/人口/体积，底部存储提示同步切换为本地文件模式说明；⑥ 页面刷新后句柄失效需重新连接（浏览器安全策略），状态条明确提示 | save-ui.js / index.html / style.css / AGENTS.md / docs |
| **v1.12.0** | **营地辖区详情模态框 + 历史国王在位时长/死因 + 存档体系文件槽位化（4项UI/内核重构）**：① **营地卡片精简与详情模态框（TODO 1/2/3）**：一级营地卡片仅保留国王 + 辖区晋升进度条（等级+辖房数+空置数移入晋升条标题）+「📜 查看辖区详情」按钮；删除标题右侧 state-badge、底部 poi-info-badge（产出速率/地形/辖区）与描述文本（仅对营地生效，非营地 POI 不受影响）；新增 `camp-detail-modal` 模态框分区展示继承人、历史国王（含在位时长与死因）、管辖家庭（`🏠#id(N人·户主#X)` 紧凑格式）、空置房屋列表（房屋ID+受益人芯片）、王国公仓账本（5类余额+最近6笔流水）；模态框每帧实时刷新，Esc/遮罩/关闭按钮三种关闭方式；② **历史国王扩展在位时长与死因（TODO 1.5，内核快照改动）**：`Region.history_kings` 从 `Vec<AgentId>` 改为 `Vec<HistoryKing>{agent_id, reign_start_tick, reign_end_tick, death_cause}`，新增 `Region.current_reign_start` 追踪现任国王登基 tick；`set_king` 签名增加 `prev_death_cause` 参数，4 个调用点（初王登基/国王更替/长子继承/夺位远征）同步传入前任国王死因（从 `agent.death_cause` 读取，被废黜则为 None）；快照三处同步（`snapshot.rs` 新增 `HistoryKingSnapshot` + `current_reign_start`、`world_snapshot.rs` 映射、`rustworld.js` 适配对象数组并兼容旧档数字数组）；存档格式版本 v2→v3（不兼容旧档）；③ **存档体系文件槽位化（TODO 4）**：彻底删除 localStorage 三槽位体系（`loadIndex`/`saveIndex`/`readSlotData`/`doSave`/`doLoad`/`doDelete`/`doExport` 及自动保存 localStorage 分支全部移除），改为 3 个固定文件槽位（`flowaccord-save1/2/3.json`），每槽位独立 `FileSystemFileHandle` 经 **IndexedDB 持久化**（`flowaccord-save-handles` 数据库），页面刷新后自动恢复连接并从文件头读取元信息；自动保存每 60 秒写入槽位 1（UI 标注「🤖 自动保存」徽章）；仅支持 Chrome/Edge，不支持浏览器显示红色不兼容提示；隐藏旧「导入存档文件」按钮；④ 版本号 v1.11.0→v1.12.0，cargo build + test-wasm.js 双绿（确定性/长程稳定/存档读档/版本不兼容拒绝全通过） | ledger/region / decisions/scheduler / snapshot / world_snapshot / world_save / rustworld.js / render_inspector.js / render_canvas.js / save-ui.js / index.html / style.css / AGENTS.md / docs |
| **v1.13.0** | **外部市场（榷场互市）与幂律动态价格系统**：① **外部市场 POI 与双库存设计**：全图新增 `PoiType::Market`（榷场互市 #1，ID 60，全图共 24 处 POI），`PrimitivePoi` 增加次级库存三元组字段（`secondary_stock`/`max`/`regen`，`#[serde(default)]` 兼容旧档），主库存承载清水（100.0，产速 2.0/s）、次级库存承载粮食（100.0，产速 2.0/s）；隔离于常规 `NodePool` 与施密特触发器，不参与日常免费公地采集；② **幂律动态定价**：纯函数 `market_unit_price(stock, max_stock, config)` 实现 $P = P_0 \times (S_{max} / \max(S, S_{floor}))^k$，库存充盈时基准价 0.1 金，极端枯竭时安全钳制（单价最高 1000 金，杜绝除零与 NaN）；③ **B15 榷场商贸自包含决策分支**：生理层兜底档（置于 B2 觅食与 B3 休息之间，`str_id: "b15"`），仅限成年男性户主；严格 OR 生存急迫守卫（家户水或粮短缺且对应野外采集点对该 Agent 私有触发器全关闭），起步存金 ≥0.5、起步体力 ≥15.0；新建 `decisions/market.rs` 子模块，决策分支数组扩容联动 `[BranchId; 15]`（`BranchId::ALL`、`resolve_order`、`seen` 同步为 15）；④ **现场自救缓冲与黄金流失记账**：新增行动状态 `SeekingMarket` 与 `BuyingAtMarket`，到达市场后若自身饥渴 < 10.0 优先现场购入饮用/进食保命；随后连续装袋购入（行囊容量/市场库存/家户黄金三重约束），扣减家户账本黄金并计入 `TransferReason::Market` 流水指向 `LedgerRef::Void`，资金耗尽、行囊装满或力竭时平滑返航卸货；⑤ **快照三处同步与前端可视化**：快照新增 `secondary_stock/max/regen` 与 `water_price/food_price`（`snapshot.rs` / `world_snapshot.rs` / `rustworld.js`）；世界渲染绘制 🏪 图标与蓝/粉双库存环；Inspector 卡片双储备进度条与单价展示；决策引擎覆层支持拖动编排；⑥ **超参集中化与门禁**：新增 11 个外部市场超参（`countMarkets`、`marketPrice*` 等），`config.rs`（const/字段/Default）与 `config.js` 全链条同步，超参总数 170→181；`config-check.js` 与 `test-wasm.js`（逐字节一致性/防越界/防 NaN/存档读档）双绿通过 | poi / ecology / decisions/market（新模块） / decisions/branches / decisions/needs / decisions/evaluate / decisions/scheduler / decisions/routing / agent / ledger/journal / snapshot / world_snapshot / world_tick / config / config.js / config.decision-order.js / decision-viz-data.js / rustworld.js / render_world.js / render_inspector.js / server.js / tools/{config-check,test-wasm}.js / AGENTS.md / docs |
| **v1.14.0** | **二手房屋市场、双轨估价与营地中介麦穗 37% 拍卖系统（完成 TODO 全部 4 项需求）**：① **无头模式按钮文本精简（TODO 1）**：index.html 按钮与 title 精简为「🧠 无头模式」/「无头模式」，main.js 切换文案精简为「🧠 无头模式 (运行中)」，大幅减少文本量；② **外部价格等效黄金折算（TODO 2）**：依托榷场互市（POI #60）动态水粮市价纯函数与木/石基准价（`marketPriceBaseWood`/`Stone`），累加房屋 0 级至当前等级的基础物化建材，折算建设成本等效黄金 $C_{const}$（0级仓库设 5.0 保底地基价值）；③ **双轨估价与买方竞价模型（TODO 2/3）**：有闲置土地（辖区房屋 < `campMaxHouses`）时自建构成价格上限，房屋估价为 $C_{const}$，买方按 $\min(C_{const}, \text{家庭全部黄金})$ 理性出价；无闲置土地（达到上限）时二手房成为稀缺资产，房屋按供求比 $V = C_{const} \times \frac{D}{S}$ 估价，买方倾尽家庭账户全部黄金全力开价；④ **营地虚拟中介麦穗 37% 拍卖引擎（TODO 4）**：房屋空置起拍至 10% 修缮度为全拍卖周期，前 37% 耐久风化为观察期（只摸底不卖，树立最高出价标杆），后 63% 为决策期（出现高于标杆价立即成交）；最晚出售时限 10% 修缮度强制出清（只要有出价就必须选最高出价成交）；成交扣减买方家户账本黄金并记入地区公仓（`TransferReason::HousingPurchase`），新户主及家眷确权迁入并自然触发修缮闭环；⑤ **房屋档案持久化与快照展示（TODO 4）**：House 新增 `bids_history` / `deal_history`，报价历史与成交记录永久沉淀（含时间/买方/金额/修缮度/事由）；快照三处同步扩展估价与档案字段；前端房屋 Inspector 呈现市场估价、麦穗拍卖阶段指示器、标杆价、最高报价、最近成交及档案折叠展开面板；地图渲染添加 🏷️ 拍卖标识；⑥ **超参集中化与门禁**：新增 6 个超参，超参总数 181→187，`config-check.js` 与 `test-wasm.js`（逐字节确定性/防越界/防 NaN/存档读档）双绿通过 | house / housing_system/auction（新模块） / housing_system/mod / inheritance / snapshot / world_snapshot / world_save / config / config.js / rustworld.js / render_inspector.js / render_world.js / main.js / index.html / tools/config-check.js / AGENTS.md / docs |
| **v1.15.0** | **房屋拍卖交易所 UI 窗口、在售动态呼吸标牌与麦穗 37% 竞价窥视大盘**：① **大地图独特在售图标与悬浮标牌**：在售房屋周围新增金色径向呼吸脉冲光晕，上方悬浮金边深色亚克力立体标牌（`🔨 🌾摸底/🎯竞价/⚠️出清 · XX.XG`），带有向下箭头指针与阶段动态色标（观察期金色/决策期天蓝色/出清期珊瑚红）；② **拍卖交易所模态大盘 (`auction-ui.js` / `#house-auction-modal`)**：创建专属全景窗口，支持多房切换卡片条、正在拍卖与历史成交双 Tab 切换、资产基本面 Hero 卡片（动态建造成本折算与闲置土地充盈状态）；③ **🌾 麦穗 37% 博弈时间轴可视化**：按房屋起拍修缮度至 10% 绝境门槛，动态切分观察期（前 37% 仅树标杆不出售）、决策期（后 63% 击中更优麦穗即成交）与出清期（10% 选最高报价避免坍塌），配合随耐久风化实时平滑移动的耐久指针，直观呈现最优停止策略进程；④ **👥 辖区意向买家池**：实时扫描营地内单身/无房成年男性户主，展示其家户黄金余额、出资意向标签（资金充裕/尽力出资/微薄无金）与本地/邻境籍贯，支持点击一键镜头追踪聚焦买家视角；⑤ **⚡ 实时竞价过程流水**：按时间戳滚动呈现族人竞拍出价卡片、买方编号、金额、博弈阶段以及营地中介仲裁判定（确立新标杆/成交出圈/未达标杆要求等）；⑥ **全渠道便捷交互入口**：顶部栏常驻「🏛️ 拍卖 (N)」按钮（有活跃拍卖时呼吸高亮）、房屋 Inspector 专属按钮「🔨 窥视实时竞拍与麦穗博弈大盘」、双击画布在售房屋均可瞬时唤起大盘，支持 Esc/遮罩/关闭按钮退出与一键定位房屋视角；快照补充起拍耐久字段；版本号 v1.14.0→v1.15.0，cargo build + test-wasm.js + config-check.js 全绿 | house / world_snapshot / rustworld.js / auction-ui.js（新文件） / render_world.js / render_canvas.js / index.html / style.css / AGENTS.md / docs |
---

## 文档与工程规范维护

以下版本为纯文档/工具链维护，不涉及功能代码变更，合并记录：

| 版本 | 内容 |
| :--- | :--- |
| **v0.9.38** | 文档偏差修复：修正 AGENTS.md / CURRENT.md / BUILD_GUIDE.md 等与现状不符的描述 |
| **v0.9.46** | 嵌套 AGENTS.md：为 sim_core / sim_wasm / decisions / housing_system 四个目录新增局部操作指南 |
| **v0.9.54** | CURRENT.md 按功能模块拆分：根索引 + `docs/current/` 分模块文档两级结构 |
| **v0.9.55** | 文档目录整合：非 AGENTS.md 文档全部移入 `docs/` 目录，引用路径全量修正 |
| **v0.9.67** | 浏览器自动化使用指南落库：`docs/browser-guide.md`，playwright-cli 4 引擎实测通过 |
| **v0.9.75** | 前端服务器端口占用说明：AGENTS.md 补充 3000 端口已占用时无需重复启动 server.js |
| **v1.3.1** | 新增面向玩家的营销型 `README.md`：项目宣传定位、八大核心看点、第一局观察指引、三分钟上手、技术底座、路线图与文档地图（纯文档，无功能代码变更） |
| **v1.3.2** | 文档全量同步（对齐 v1.3.0 代码状态，纯文档无功能变更）：① AGENTS.md/`docs/CURRENT.md`/`08-config-system.md`/`09-code-map.md`/`crates/sim_core/AGENTS.md`/`DECISION_VIZ_DESIGN.md`/`UI_SPEC` 超参字段数 153→163、config 分区 10→12；② `12-ledger-system.md` 重写为 M1~M4 已实现态（旁路记账/分家继承/宗族/地区王国 + 快照 + 4 标签页）；③ `PLAN_LEDGER_REFACTOR.md` 状态 M1~M5 全部 ✅、`PLAN.md` M2/M3/M4 移入已完成、M5 起为规划；④ `UI_SPEC` 由「未实现设计稿」改为「已实现说明」（4 标签页枢纽/宗族页/王国页/夺位特效/快照实际结构）；⑤ 嵌套 `ledger/AGENTS.md` 重写为 7 子模块 + `decisions/AGENTS.md` 补夺位远征 + `housing_system/AGENTS.md` 补记账钩子；⑥ `06-motivation-ai.md`/`04-agent-life.md`/`05-house-system.md`/`07-frontend-ui.md`/`AGENT_AI_ANALYSIS.md` 补充 M2~M4 机制 |
| **v1.3.3** | `FoundHome`（立宅/建 0 级仓库）需求层级提升：由归属层（③）改为**生理层（①）最后一档**——`evaluate_needs` 中移至「解渴 → 觅食 → 体力休养」之后短路判定，`level` 由 `Belonging` 改为 `Physiological`，需求标签 `Belonging·FoundHome` → `Physiological·FoundHome`；触发条件（无家 + 成年男性 + 饥≥20/渴≥20/体力≥60 必然触发）与实体化链路不变，归属层仅保留 `BuildHouse`(0级) 升级成家。同步更新 `needs.rs`/`AGENT_AI_ANALYSIS.md`/`README.md`/`DECISION_VIZ_DESIGN.md`/`06-motivation-ai.md`/决策可视化原型（v1.3.3） |
| **v1.3.4** | **分家权重修复（核心规则）**：分家抽资 `tick_household_split` 中，父权重仅**在父亲在世时**计入——成年分家 `W = 2(父) + n(子一代，含胎儿)`；**丧父分家时亡父不占权重，`W = n`**，家户资源在子一代间平分（与继承清算 `total_amt/n` 语义一致），避免为已故父亲保留权重导致子代份额被稀释。同步更新 `bookkeeping.rs` 公式与注释、`ledger/family.rs` 模块文档、`ledger/AGENTS.md`、`docs/PLAN_LEDGER_REFACTOR.md` §3.2、`docs/current/12-ledger-system.md` 关键不变量 |
| **v1.3.5** | **受孕即建胎儿 Agent 身份（M1.7 核心规则）**：怀孕女性受孕瞬间即为其腹中胎儿**创建完整 Agent 实体**（新增 `Agent3D.is_fetus` 标记），而非仅占号——① 胎儿加入父母 `children_ids`、随父入父亲家户，因此**继承清算（账本/金币/房产）天然把胎儿计入**：父亡且仅有胎儿时不再误判"绝嗣"入公仓，而是为胎儿立户并转其份额（临时单测实测：胎儿继承 100 Food 全额、随身黄金 12.5 出生时转移）；② 胎儿**无需求消耗**（跳过代谢/年龄/死亡判定）、**无地图实体**（跳过运动/POI/渲染/点击拾取）、**跳过行动决策**（decisions/scheduler + 夺位远征），性别占位 Female 不被分家/婚姻/房产/王位继承当作男性处理；③ 分娩在 `resolve_newborns` **原位复用胎儿 ID** 替换为新生儿（不再是"出生才建实体"）；流产/母亡时 `tick_fetus_reconcile` 移除胎儿 agent 并清理 `children_ids`/家户成员；④ 分家 n 改为直接取父亲 `children_ids.len()`（胎儿已在其中，移除原"+1 if pregnant"避免重复计数）；⑤ 快照三处同步新增 `is_fetus` + `pregnancy_child_id`，前端母亲卡片妊娠框新增「👶 查看腹中胎儿 #X」按钮跳转胎儿卡片，胎儿卡片展示"腹中孕育中"、子嗣栏以 👶 标记，胎儿不入先祖档案库 | agent / world / birth / bookkeeping / decisions/scheduler / ecology / snapshot / rustworld.js / render.js / main.js / index.html |
| **v1.3.6** | **决策顺序可编排（数据驱动 evaluate_needs + 前端拖动热注入 + 落盘持久化）**：① 内核 13 条硬编码 if 级联抽为 `decisions/branches.rs` 分支注册表（`BranchId` ↔ `"b1".."b13"`，每条为自包含条件函数：无家守卫 / b13 的「4级庄园万事俱备」门禁 / b5·b6·b7 的 `family_level` 动态默认全部内建），`evaluate_needs` 改为按配置顺序迭代注册表、首个命中返回；② **Rust 层完全无顺序**——`SimConfig` 新增 `decision_eval_order: Vec<String>` 与 `decision_eval_levels: Vec<u8>` 默认空 Vec，空/非法时仅按 `BranchId::ALL` 声明序中性兜底，策展优先级的唯一真相源是新增的 `frontend/js/config.decision-order.js`（§4.12 三处规约的文档化例外）；③ 前端决策引擎视图落地（原 `docs/decision-viz-prototype.html` 原型迁入 `frontend/js/decision-viz-{data,view}.js` + 集成层 `decision-viz.js`，index.html 内嵌覆层「🧠 决策引擎」入口，与运行中的模拟**共用同一 WASM 引擎实例**）：拖动卡片换序 / 拖动分界线重划层级 → 写 `SIM_CONFIG` → `rustWorld.applyConfig()` 热注入即时生效，并 `POST /save-decision-order` 由 `server.js` 校验后原子写盘（静态 COS 部署无写能力时降级 localStorage 并提示）；④ 层级覆盖 `decision_eval_levels` 与顺序下标并行（`0` = 保留代码动态默认，`1-5` = 强制马斯洛层级），评估结论与 `state_need_label_with_agent` 的 `current_need` 标签共用同一覆盖表；⑤ 工具链扩展：`config-check.js` 支持 `Vec<String>`/`Vec<u8>` 数组类型逐元素校验 + 顺序文件 ID 词汇轻校验；`test-wasm.js` 改为合并 `config.js` + `config.decision-order.js` 后注入 WASM，使确定性门禁在生产真实配置下运行（超参总数 163→165）；⑥ 等价性验证：重构前后同种子 + 生产配置快照**逐字节一致**（seed 777/2026 两组对照） | decisions/branches（新文件） / decisions/evaluate / decisions/needs / decisions/scheduler / config / frontend/index.html / server.js / config.js / config.decision-order.js（新文件） / decision-viz-{data,view}.js（新文件） / decision-viz.js（新文件） / main.js / style.css / tools/{config-check,test-wasm}.js |
| **v1.3.7** | **决策引擎视图交互修复 + 左右栏 UI 重设计**：① 修复拖卡换位位移累加（换位后不再重写 `drag.startY`，卡片持续「手指跟随」、松手吸附最终槽位，原先每次越过相邻卡起点错位导致位移逐格累加、最终瞬移到顶端/底端）；② 修复拖分界线时部分卡片颜色不刷新（卡片显示层级恒为当前位置分区 `zoneOf(i+1)`，不再优先读取上一次 commit 的陈旧 `levels` 覆盖值——强制覆盖卡被分界移回原区时颜色立即回正）；③ 分界线热区加宽（`DIV_H` 24→40 / `GAP` 30→50），分界线改为整条横贯卡宽的可视横条（左右层级徽章 + 居中「拖动分界」抓取手柄）；④ 行动状态/需求目标增加中文语义（`decision-viz-data.js` 新增 `FSM_STATE_ZH` 15 态中文映射，状态机卡与检查器均显示「中文 + 英文码」）；⑤ 左右栏 UI 重设计：左栏改为「层级图例（含「全部层级」行、激活态、计数徽章）· 操作指引（图标化）· 数据来源」三面板（修复旧 `.dviz-lv-row` 类名与 JS 生成的 `.dv-lv-row` 不一致导致样式从未生效的问题），右栏检查器重设计（层级 chip + 副标题 + 分隔行）与空态图标化 | frontend/js/decision-viz-{data,view}.js / frontend/index.html / frontend/style.css / docs/current/07-frontend-ui.md |
