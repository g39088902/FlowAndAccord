# TA-09 技术实施方案：支脊山口与岩壁河谷实机样板交付（D-B1-8）

> **状态**：草案（待评审）｜ **日期**：2026-09-22 ｜ **任务**：[07 号 §1.2 TA-09](docs/plan/tech/07-terrain-art.md) / [06 号 D-B1-8](docs/plan/tech/06-terrain-templates.md)
> **基线**：应用 **v1.52.6** · `TERRAIN_GENERATOR_VERSION=14` · `SAVE_FORMAT_VERSION=7` / `SAVE_APP_VERSION=1.52` · FABS `FORMAT_VERSION=4` · `terrainGridRes=256`（步长 ≈2.996m）· random 池 8 路
> **前置结论（先读）**：TA-09 的两项硬前置均已落地——TB-01 支脊与多尺度地貌、阶段三 **RiverCliff 河谷峭壁注入器（v1.52.6）**；原前置 TA-08 已删除视为完成，遮挡判据改指 [31 号 §8.5](docs/plan/tech/31-canvas-to-webgl-migration.md)。**TA-09 当前已无代码前置阻塞，可直接开工**；07 号 §1.2 中“RiverCliff 当前空壳 `accepted=false`”一句已过时（写于 v1.50.50 基线），实施时以本方案 §2 复核为准，不以该句为准。
> **本文定位**：TA-09 的可执行技术方案（怎么做、做到什么算完）。任务台账权威仍是 [07 号 §1](docs/plan/tech/07-terrain-art.md)、构图交付权威仍是 [07 号 §4.4.1](docs/plan/tech/07-terrain-art.md)；本方案通过评审后按 §13 回写这两处，不另立口径。

---

## 0. 一页摘要（TL;DR）

**目标**：把“代码交付但没人在真实种子上看过画面”的 D-B 地貌骨架，收敛为 **2 组固定的真实 seed/profile + 同版本存档**（A=支脊山口 `mountain_pass_v1`、B=岩壁河谷 `river_valley_v1`+RiverCliff 已接受），并按 07 号 §4.4/§11 口径完成实机验收（每场景 96 视图 + 模拟推进 + 生命周期 + 性能），输出证据包 `docs/plan/tech/assets/ta09-evidence-*/`。

**总体路线**（详见 §4）：

```text
探针几何筛选（seed 矩阵，不过滤漂亮截图）
  → 布局筛选（营地/POI/路网与鞍部/浅滩/崖体的空间关系，含 tick 推进后复核）
    → 视觉初选（mapOnly + 实机短看，对照构图草案 composition-draft-v1.png）
      → 冻结 2 主选 + 2 备选/场景（含存档与相机锚点）
        → 96 视图矩阵采集 + 像素指标 + 人工评审
          → 最小视觉修补闭环（如需，每项独立提交、可 A/B）
            → 证据包归档 + 文档同步 + TODO 勾选
```

**关键决策**：

| # | 决策 | 结论 |
|---|------|------|
| 1 | 主验收管线 | **当前默认 WebGL 路径**（地形 GL + 2D 覆盖层过渡形态，`RENDER_CONFIG.useWebgl=true`）；07 号 §4.4“若排在 TC-03 之后按 WebGL 口径”——当前装饰主体已在 GL（v1.50.83~84），故直接按默认路径验收，不另建 Canvas 对照矩阵（Canvas 仅作问题定位对照） |
| 2 | 物理 vs 视觉 | **严格分离**：TA-09 是纯视觉样板，不替代 06 号 §5.8/§18 物理门禁；物理断言只做“存在性复核”（崖体/鞍部/浅滩/连通分量），不重做注入器验收 |
| 3 | 与 S4-X3 的边界 | TA-09 **不实现**峭壁脚专属景观配方（碎石堆/岩隙灌木归 S4-X3）；TA-09 只验收崖体本身、道路/取水点避让与现有装饰/景观的无违例 |
| 4 | 代码量预期 | **零或极小**：首选零代码（纯筛选+取证）；仅当评审明确失败时做最小视觉修补（§8），且每项独立提交、A/B 可回退 |
| 5 | 版本判定 | 纯筛选+取证=**纯文档/证据改动，不升版**（TA-06 证据包先例）；若有视觉代码修补则按根 AGENTS.md §4.9 升 patch（只改前端=patch 且兼容线不动；若动 Rust=重编译 WASM 并评估生成器版本） |

**交付物清单**：

1. 冻结样板：2 场景 ×（主选 seed + 备选 seed + 同版本存档 + 相机锚点 + 配置摘要），记录于 07 号 §4.4.1；
2. 证据包 `docs/plan/tech/assets/ta09-evidence-<date>/`（report.md + manifest.json + metrics + visual，复用 TA-06 证据包结构）；
3. （如有）最小视觉修补代码 + A/B 对照截图；
4. 文档同步：07 号 §1.2/§4.4/§11、TODO.md、changelog（§13 清单）。

**工作量**：**中**（与 07 号难度口径一致），估计 **5~8 人日**（§10 分解：筛选 1.5~2、冻结 0.5、矩阵采集 2~3、修补闭环 0~2、归档 1）。

---

## 1. 背景与目标

### 1.1 TA-09 是什么

- **编号**：TA-09（= D-B1-8），07 号 §1.2 登记为“完整场景样板：支脊山口与岩壁河谷实机交付”，难度**中**。
- **两步交付**（07 号 §4.4）：① 构图草案评审色板与主次关系（**已完成**，2026-09-14 用户明确接受，见 §1.2）；② **实机样板**（本文范围）：用真实 seed/profile 跑出来的画面交付，覆盖四季、四方位、低高俯角、远中近景及模拟推进。
- **不做会怎样**（07 号原话）：D-B 地貌骨架停留在“代码交付但没人在真实种子上看过画面”；主路可读性、岩壁遮挡、树屋人穿插无验收。

### 1.2 已接受的构图方向（评审权威，不重复评审）

权威记录：[07 号 §4.4.1](docs/plan/tech/07-terrain-art.md)；构图草案图：`docs/plan/tech/assets/references/composition-draft-v1.png`（**视觉目标，非实机截图**）。

| 场景 | 已接受的构图方向 | 实机对齐要求（本方案必须逐项核对） |
| :--- | :--- | :--- |
| **A 支脊山口** | 主脊与不对称支脊围出低鞍部；土路从前景聚落引向山口；坡脚疏林成组、山体保留露岩 | 真实 `mountain_pass_v1` 世界；主路必须对应**可达路网**；中景可读支脊与鞍部，近景核对坡脚接地 |
| **B 岩壁河谷** | 远岸层岩为主地标，河流引导视线；近岸低河阶安置聚落，浅滩与岩壁错开 | 真实 `river_valley_v1` 且**实际接受 RiverCliff** 的世界；普通陆路只经合法浅滩跨河，崖体不得压盖近路与取水点 |

### 1.3 前置状态复核（本方案开工前的事实核对）

| 前置 | 07 号旧表述 | **当前事实（以代码为准）** |
| :--- | :--- | :--- |
| TB-01 支脊 | 前置 | ✅ 已落地：`terrain.rs::sample_branch_ridges`（1~2 条不对称支脊 + 鞍部禁区），诊断字段 `TerrainMap::branch_ridges`，探针 `terrain_probe.rs` 有 `brN/brSlope/brDet` 列与 60 种子验收判定块 |
| 阶段三 RiverCliff | “当前空壳 `accepted=false`”（v1.50.50 基线旧语） | ✅ **已落地（v1.52.6，生成器 13→14）**：`hydrology.rs::apply_river_cliff` 在第 5 步几何事务域内实施；`Cliff` 特征 id=224 + `TerrainSubFeature` id=1005；144 离散尺度组合全过 + seed 0~85 矩阵 12 例注入全过（components 恒 1）；前端 `render_terrain.js` 有 Cliff 岩层阴影绘制分支（仅可视化） |
| TA-08 遮挡实测 | 原前置 | ✅ 已删除视为完成（2026-09-17）；遮挡判据以 [31 号 §8.5 验收矩阵](docs/plan/tech/31-canvas-to-webgl-migration.md) 为准，技术解由 GPU 深度缓冲承担 |
| 视觉散布 | — | ✅ 四类视觉子特征已落地（v1.52.5）：ForestedSlope / RockyOutcrop / RiversideForest / GravelBeach，hash 追加装饰 |

**结论**：TA-09 的代码前置已全部就绪；剩余工作 = **筛选 + 冻结 + 取证 +（如需）最小修补**。07 号 §1.2 的 TA-09 依赖格应在收口时改写为“✅ 前置已就绪（TB-01 + RiverCliff v1.52.6）”。

### 1.4 成功标准（Definition of Done）

1. **冻结样板**：2 场景各 1 主选 + 1 备选 seed（显式 profile），附同版本存档、相机锚点、配置/版本/窗口/DPR 记录，任何人用同环境可复现同画面；
2. **矩阵通过**：每场景 96 视图（四季×四方位×两俯角×三缩放）采集完整，像素指标与人工评审结论归档，无 P0 违例（§7/§11）；
3. **推进验证**：初始 tick + 发展后 tick（含路网成型）两态画面均核对，主路/浅滩/崖体关系符合 §1.2 实机对齐要求；
4. **生命周期与确定性**：暂停/读档/回溯/换世界无残留、无串味；同种子重演逐位一致；
5. **性能**：新增工作不引入绘制代码时零增量；如有修补则满足 07 号 §11.3 预算（新增绘制 p95 增量 ≤3ms，视觉缓存 ≤64MiB 建议上限）；
6. **文档**：07 号 §4.4.1 实机交付记录、§1.2 台账、TODO.md、changelog、证据包全部归档。

---

## 2. 现状基线盘点（开工前必须确认的事实）

### 2.1 版本与契约基线

| 项 | 值 | 来源 |
| :--- | :--- | :--- |
| 应用版本 | v1.52.6 | `docs/current/01-changelog.md` 头部 |
| `TERRAIN_GENERATOR_VERSION` | 14 | `crates/sim_core/src/geo/terrain.rs` |
| `SAVE_FORMAT_VERSION` / `SAVE_APP_VERSION` | 7 / 1.52 | `crates/sim_core/src/spatial/world_save.rs` |
| FABS `FORMAT_VERSION` | 4 | `snapshot_bin/layout.rs` / `frontend/js/snapshot-bin.js` |
| 地形分辨率/世界尺寸/步长 | 256 / 764m / ≈2.996m | `config.js terrainGridRes` |
| random 候选池 | 8 路（T1/T2/草原/半坡/台地/冲积扇/盆地/湖畔），各 ~12.5% | `resolve_profile` |
| WASM 双副本 | `frontend/rust/sim_wasm.wasm` + `frontend/sim_wasm.wasm`（须一致） | 构建产物 |

> 开工时冻结并记录：源码提交 SHA、WASM 双副本 SHA256、完整模拟配置及 SHA256（TA-06/D-B1-9 两态对拍先例做法）、Chrome 版本、窗口尺寸与 DPR。TA-09 取证全程不得更换其中任一项；若必须更换则重采全部矩阵。

### 2.2 地貌能力 inventory（TA-09 直接消费）

| 能力 | 状态 | 关键入口 |
| :--- | :--- | :--- |
| T1 主脊+鞍部（36°~41°硬禁行，山口唯一通道） | ✅ | `geo/terrain.rs`（`terrainPassRidgeWidth/Amplitude` 参数契约见 14 号 §9.3.1） |
| TB-01 不对称支脊（1~2 条）+ fBm 多尺度起伏 | ✅ | `geo/terrain.rs::sample_branch_ridges`，`TerrainMap::branch_ridges` 诊断字段 |
| T2 主河+双浅滩+河阶+泉谷+共享水池 | ✅ | `geo/hydrology.rs::generate_river`（影响带收敛版） |
| RiverCliff 注入（段长 55~80m、崖高 6~12m、18m 崖体+12m 回落+6m taper、整域 RockFace、Cliff id=224 / subfeature id=1005） | ✅ v1.52.6 | `geo/hydrology.rs::apply_river_cliff`、`geo/geometry_transaction.rs` |
| 几何事务域（候选隔离、失败原子回滚、空注入逐位不变） | ✅ v1.52.4 | `geo/geometry_transaction.rs` |
| 有界降级环（禁结构子特征→去支脊→flat_baseline） | ✅ | `spatial/creation_fallback.rs` |
| 探针 `terrain_probe`（60 种子矩阵、T1 门禁窗、支脊列） | ✅ | `crates/sim_core/examples/terrain_probe.rs` |
| v1.52.6 已知注入矩阵 | ✅ 证据 | changelog v1.52.6：seed 0~59 注入 9 例 + 扩展至 85 共 12 例（**具体 seed 号须重跑探针枚举，本文不填猜测值**） |

### 2.3 表现层现状（TA-09 画面由这些部分构成）

| 层 | 现状 | TA-09 关注点 |
| :--- | :--- | :--- |
| 地形/沙盘侧壁 | WebGL 底层 `sim-canvas-gl`（`webgl/layers/terrain/terrain-renderer.js`），光照进 shader，帧率已解限 | 岩壁区 RockFace 着色是否可读（与 2D `computeElevationColor` 口径是否一致） |
| 水系（河面/岸线/浅滩踏石） | 2D 覆盖层 `render_terrain.js::drawFeatureItem`（River 分段 clip 填充、ShallowFord 踏石） | 浅滩与岩壁错开是否肉眼可辨；游鱼经 sink 进 GL（`river_life.js`） |
| Cliff 崖顶折线 | 2D 覆盖层：岩体阴影带（`rgba(96,88,80,0.38)` 宽描边）+ 崖缘暗线（`rgba(58,52,48,0.55)`） | **当前为最小可视化**，无层岩纹理；TA-09 先验收“可读+不压盖”，层岩质感若评审不通过则走 §8 最小修补（不实现 S4-X3 配方） |
| 装饰五类 + 季相 + 受光 + LOD | ✅ TA-01~04/06/07/11/12/14/16/17 通用部分；边缘 3% 保护区 + 水面 3m 净空（v1.52.0） | 坡脚疏林成组、山体露岩保留、树屋人穿插、接地无悬空/入土 |
| 资源景观五类 + 几何遮罩 + 标签聚合 | ✅ S4-01~08（`landscape-model/mask`、`label-layout`） | 崖体/鞍部附近的遮罩无违例（车道胶囊/房屋圆/POI 操作区）；标签 5 秒可读性 |
| 季相 | Tree/Bush 连续季相 ✅；**地表不读季节**（TA-13 未做） | **已知局限**：冬景地表仍绿，TA-09 如实记录为局限，不阻塞（不提前画雪） |
| 季节光照 | `SimLighting` 年周期光弧 + 阴影图（GL） | 四季×四方位光向协调，亮部不黏屏幕（复用 TA-06 受光分离方法） |

### 2.4 工具与证据先例

- **TA-06 证据包** `docs/plan/tech/assets/ta06-evidence-2026-09-22/`（report.md + manifest.json + metrics + visual）：TA-09 直接复用其**目录结构、像素指标口径、逐张回读校验、拼图归档**做法（§9）。
- **植被观察台** `frontend/js/ta06-acceptance.js`（`?ta06=1`）：季节锁定/光向/相机/飞往/标注层的实现可作为 TA-09 矩阵脚本的参考实现；TA-09 **不新建常驻面板**（§5.5 决策）。
- **地图图鉴** `?mapOnly=1&nogate=1`：与正式游戏同源地貌（不播撒生态），适合筛选阶段快速扫图；**冻结样板必须用正式世界**（world_create，含生态/路网/房屋），不能只用图鉴。

---

## 3. 范围与边界

### 3.1 在范围内

1. 种子筛选（几何→布局→视觉三级漏斗，§5），冻结 2×（1 主+1 备）样板（§6）；
2. 每场景 96 视图矩阵 + 像素指标 + 人工评审（§9）；
3. 模拟推进两态核对（创世 tick0 + 路网/聚落成型后），主路/浅滩/崖体/取水点关系验证（§7）；
4. 生命周期（暂停/读档/回溯/换世界）、确定性、性能复核（§7.5/§11）；
5. **如需**的最小视觉修补（§8）：仅限纯表现层、每项独立提交、A/B 可回退；
6. 证据包归档与文档同步（§9.4/§13）。

### 3.2 明确不在范围内（防止范围蔓延）

| 项 | 归属 | TA-09 内如何处理 |
| :--- | :--- | :--- |
| S4-X3 峭壁专属景观（崖脚碎石/岩隙灌木配方） | S4-X3（已解锁、待交付） | 不实现；TA-09 只验“崖体+现有装饰/景观无违例”，配方缺失记为已知局限 |
| 新物理注入（FootLake/RidgeWaterfall/OxbowLake/R0） | 阶段八 | 不碰；B 场景若命中 OxbowLake 候选则视为未注入（当前为空注入），筛选时以“RiverCliff 实际接受”为准 |
| 地表季节反照率（冬雪/秋枯地表） | TA-13 | 不做；矩阵中如实记录“地表季相缺失”局限 |
| 飘叶/落叶地被 | TA-15 | 不做 |
| 台地美术 | TA-10 | 不做 |
| WebGL 迁移（装饰/实体层、2D 退役） | TC-03/31 号 | 不做迁移工作；TA-09 只**消费**当前管线 |
| 动态水文/景观演化 | TC-01/TC-02 | 不做 |
| 新 profile / random 池变更 | — | 不做；样板用**显式 profile**锁定，不用 random |
| 快照/FABS/存档结构变更 | — | 不做（Cliff 编解码已就绪） |
| 新 SimConfig 字段 | — | 不新增；视觉调参只走 `config.render.js` / `config.lighting.js` |

### 3.3 铁律（实施全程遵守，违反即返工）

1. **装饰纯视觉**：不因样板好看而让装饰参与通行/资源/碰撞/建造判定；不画不存在的水/路/防御效果（07 号 §2.2/§4.4）；
2. **不用漂亮截图代替种子矩阵**（07 号 §4.4）：必须先过探针矩阵与布局筛选，再谈视觉初选；冻结 seed 必须附带矩阵上下文（命中数/拒绝原因分布）；
3. **样板不冒充物理验收**：TA-09 通过≠注入器物理门禁通过；两者证据分开归档；
4. **确定性**：同版本同种子逐位一致；换世界/读档/回溯零残留（`_invalidateWorldStaticCaches` + `STR_TAB.start_index==0` 判据）；
5. **工程纪律**：单文件 ≤800 行；不提交临时单元测试（临时脚本验证后删除，根 AGENTS.md §4.10）；新增持久化字段才考虑存档版本（§10.3）。

---

## 4. 总体技术方案

### 4.1 筛选漏斗（核心流程）

```text
┌──────────────────────────────────────────────────────────────┐
│ L0 输入：显式 profile × 种子扫描窗（A: T1 seed 0~N；B: T2 seed 0~M，│
│    M 需覆盖 ≥10 个 RiverCliff 实际接受种子，按 06 §5.4.D.7 扩展窗） │
└──────────────────────┬───────────────────────────────────────┘
                       ▼
┌──────────────────────────────────────────────────────────────┐
│ L1 几何筛选（纯创世态，terrain_probe + 临时 RiverCliff 探针列）│
│  A: 主脊>34° ∧ components=1 ∧ detour∈[2.0,4.8] ∧ buildable    │
│     ∧ 支脊≥1 ∧ 鞍部走廊可通行                                │
│  B: plan 命中 RiverCliff ∧ 实际接受（subfeature 1005 在场）   │
│     ∧ components=1 ∧ RockFace≥230 ∧ 连续带≥70% ∧ 浅滩/取水点保护│
│  输出：候选表（seed, 关键指标, 拒绝码分布）                   │
└──────────────────────┬───────────────────────────────────────┘
                       ▼
┌──────────────────────────────────────────────────────────────┐
│ L2 布局筛选（完整世界：生态播撒 + 路网 + tick 推进两态）       │
│  A: 初始营地→水/粮可达；主路经山口（绕行比验证）；聚落前景区   │
│     与山口形成“前景聚落→山口”纵深；坡脚可建带连续              │
│  B: 双浅滩完好；崖段与浅滩 y 区间错开≥保护带；崖体 AABB 不压    │
│     取水点/主路；近岸河阶聚落带连续可建                       │
│  输出：短名单（每场景 4~6 seed，附布局快照与量测）             │
└──────────────────────┬───────────────────────────────────────┘
                       ▼
┌──────────────────────────────────────────────────────────────┐
│ L3 视觉初选（mapOnly 扫图 + 实机短看，对照 §1.2 对齐要求）     │
│  远景读地标/主路，中景读支脊-鞍部/崖-河-阶层次，近景读接地/穿插 │
│  输出：2 主选 + 2 备选/场景 + 初选评审记录（通过/淘汰原因）     │
└──────────────────────┬───────────────────────────────────────┘
                       ▼
┌──────────────────────────────────────────────────────────────┐
│ L4 冻结 + 96 视图矩阵 + 评审 +（如需）最小修补闭环 + 证据包    │
└──────────────────────────────────────────────────────────────┘
```

### 4.2 主验收管线与对照口径

- **主口径**：默认 WebGL 路径（`?seed=<n>&nogate=1`，`useWebgl=true`，装饰 GL sink 生效）。矩阵截图、像素指标、人工评审结论全部以该路径为准。
- **对照口径**（仅问题定位，不建矩阵）：`?accentgl=0` Canvas 装饰对照（TA-06 用过，首帧全黑已知现象需规避）；`?mapOnly=1` 仅用于 L3 扫图，不作为冻结证据。
- **版本冻结**：L4 启动前冻结 §2.1 基线表；冻结后任何代码/配置变更都必须重采受影响矩阵并记录。

### 4.3 数据流（TA-09 消费，不改造）

```text
Rust: plan_subfeatures(seed) → 几何事务(apply_river_cliff / 支脊基底)
  → finalize_slope_and_surface → validate_static_terrain_geometry
  → features(id=224 Cliff)/sub_features(id=1005)/cells(RockFace)
  → FABS Section 18/22 + TERRAIN → rustworld.js (三通道独立缓存)
  → GL 地形层（cells 着色） + 2D 覆盖层（drawFeatureItem Cliff/浅滩/河面）
  + 装饰/景观/标签（AccentModel/LandscapeModel/Mask/LabelLayout）
```

TA-09 不改变上式任何一环；§8 修补最多触及“GL/2D 落笔样式”一端，且不得改变几何派生与缓存契约。

---

## 5. 详细设计（一）：种子筛选

### 5.1 扫描窗与工具

| 场景 | 显式 profile | 初始扫描窗 | 扩展规则 | 工具 |
| :--- | :--- | :--- | :--- | :--- |
| A 支脊山口 | `mountain_pass_v1` | seed 0~59（与探针门禁窗一致） | 候选不足则按 seed 60 起顺序扩展至最多 255（06 §5.4.D.7 同款规则） | `terrain_probe --profile mountain_pass_v1 --seeds 60`（已有 `brN/brSlope/brDet` 列） |
| B 岩壁河谷 | `river_valley_v1` | seed 0~59 | 同上扩展至 85（v1.52.6 已验证 12 例落在此窗内），仍不足则继续至 255；**至少 10 个实际接受种子**才准入 L2（06 §5.4.D.7） | L1 需**临时扩展探针列**：`plan_hit`（Oxbow/RiverCliff 命中）、`accepted`（1005 在场）、`rockface_n`、`band_ratio`、`cliff_len/h/side`、`ford_clearance`（临时脚本，提交前删除） |

> **禁止填写猜测 seed**：v1.52.6 changelog 只给了“12 例”计数，未列 seed 号。TA-09-1 的第一步就是重跑探针枚举出 seed 表，本文不预填任何 seed。

**探针命令速查**（以仓库实际参数为准，`--help` 核对）：

```bash
cargo run --release -p sim_core --example terrain_probe -- --profile mountain_pass_v1 --seeds 60
cargo run --release -p sim_core --example terrain_probe -- --profile river_valley_v1 --seeds 60
```

### 5.2 L1 几何门（创世态，纯地形+注入）

**场景 A（T1+支脊）门**：

| # | 断言 | 阈值/来源 | 失败码 |
|---|------|-----------|--------|
| A-G1 | 可行走连通分量 | `components == 1` | `A-G1-disconnected` |
| A-G2 | 主脊硬禁行真实存在 | `max_slope > 34°`（14 号 §9.3.1 实测带 36°~41°） | `A-G2-ridge-weak` |
| A-G3 | 山口唯一通道 | 绕行比 `detour ∈ [2.0, 4.8]`（探针验收窗） | `A-G3-detour` |
| A-G4 | 可建面积不塌陷 | `buildable ≥ 10500`（探针窗） | `A-G4-buildable` |
| A-G5 | 支脊在场 | `brN ≥ 1`（`branch_ridges.len()`），且侧翼峰值坡度 `brSlope` 落在探针实测带内 | `A-G5-no-spur` |
| A-G6 | 鞍部走廊可通行 | 山口段最大坡度 <30°（TB-01-4 实测口径 17.7°~23.2°） | `A-G6-saddle-blocked` |

**场景 B（T2+RiverCliff）门**：

| # | 断言 | 阈值/来源 | 失败码 |
|---|------|-----------|--------|
| B-G1 | 选择器命中 RiverCliff | `plan_subfeatures` 含 kind=5（OxbowLake 占位命中不算，当前为空注入） | `B-G1-plan-miss` |
| B-G2 | **实际接受** | `sub_features` 含 id=1005 且 `features` 含 id=224（9 点崖顶折线） | `B-G2-not-accepted` |
| B-G3 | 连通分量不增加 | 注入后 `components == 1`（与同 seed 基线比不增加） | `B-G3-disconnected` |
| B-G4 | 岩壁规模 | `RockFace ≥ 230` 格（v1.52.6 矩阵下限） | `B-G4-rockface-small` |
| B-G5 | 硬禁行连续带 | `band_ratio ≥ 0.70`（Δ/2 采样，06 §5.4.D.3） | `B-G5-band-short` |
| B-G6 | 浅滩保护 | 崖段 y 区间与两处浅滩走廊（半宽+20m）零重叠 | `B-G6-ford-conflict` |
| B-G7 | 取水点保护 | 崖体 AABB 与任一 `WaterAccessPoint.interaction_radius` 圆零相交 | `B-G7-access-conflict` |
| B-G8 | 图缘保护 | 崖体距边界 ≥40m（注入器内建，探针复核） | `B-G8-edge` |

L1 输出：**候选表 CSV/JSON**（seed, 全部指标列, 通过/失败码）+ **拒绝分布统计**（每失败码计数，用于证明“不是只挑漂亮截图”）。

### 5.3 L2 布局门（完整世界，含生态/路网/tick 推进）

L2 必须跑**完整世界**（`world_create`，不是 `world_create_map`），因为营地/POI/路网只在完整创世后存在；且必须看**两态**：创世完成未推进 tick（t0）与路网/聚落成型后（t1，推进至主路可辨，建议 t1 取“第 N tick 使 wear 主干形成”，具体 tick 数在 TA-09-2 中按实测固定并记录）。

**场景 A 布局门**：

| # | 检查 | 方法 | 通过标准 |
|---|------|------|----------|
| A-L1 | 生存链路完整 | `diagnose_survival`（STAGE2-6）+ 营地→水/粮/市场可达 | 全营地 ok，无 `SpawnDisconnected/SurvivalCostExceeded` |
| A-L2 | 主路经山口 | 主干车道（wear 最高分位）几何与鞍部窗口相交；跨脊替代路线被 `validate_curve` 拒绝 | 主路穿山口，横穿主脊路线不存在 |
| A-L3 | 前景聚落→山口纵深 | 初始营地/房屋簇质心与鞍部形成可读纵深（t1 态截图人工确认 + 距离量测） | 中景一屏内同时可读聚落与山口 |
| A-L4 | 坡脚可建带连续 | 坡脚带 `band` 连续宽度 ≥ 房屋直径（`2×terrainFootprintHalfExtent=14m` 口径，半坡模板同款） | 无被支脊/陡坡掐断的孤立可建口袋 |
| A-L5 | 坡脚疏林 vs 山体露岩 | 装饰分布复核：坡脚 Tree/Bush 成组、主脊带 RockFace 上无 Tree（v1.52.0 水面/边缘禁区已保水岸，此处查陡坡） | 无“崖顶浮空树”（S4-X3 口径预检） |

**场景 B 布局门**：

| # | 检查 | 方法 | 通过标准 |
|---|------|------|----------|
| B-L1 | 生存链路完整 | 同 A-L1 | 全营地 ok |
| B-L2 | 双浅滩完好 | `connections` 2 条在场；跨河车道仅经浅滩走廊（抽查跨河路径几何） | 无借道崖体/深水跨河 |
| B-L3 | 崖-滩错开 | 崖段 y 区间 vs 浅滩 y ±（半宽+20m）：数值断言 + 远景截图可辨 | 浅滩与岩壁在空间上明确错开 |
| B-L4 | 崖体不压近路/取水点 | 崖体 AABB vs t1 态主干车道胶囊、取水点操作区：零压盖（`landscape-mask.js` 保护区口径复用） | 数值零违例 + 近景截图确认 |
| B-L5 | 近岸河阶聚落带 | 近岸（崖对侧优先）河阶连续可建带 ≥55m 口径（河谷聚落模板同款带宽语义，按实际 `terrace` 宽度校准） | 聚落可读地落在低河阶上 |

L2 输出：短名单（每场景 4~6 seed）+ 每 seed 的布局快照（t0/t1 各 1 张远景 + 关键量测表）。

### 5.4 L3 视觉初选（对照构图草案）

- 输入：L2 短名单；工具：`?mapOnly=1` 扫图（快速）+ 正式世界实机短看（`?seed=<n>&nogate=1`，t0/t1 两态各停 3 个机位：远/中/近）。
- 方法：评审人按 §1.2 对齐要求 + §7 清单逐项打分（通过/淘汰+原因），**淘汰原因必须记录**（避免“只留漂亮截图”的观感）。
- 输出：**2 主选 + 2 备选/场景** + 初选评审记录表。主选 = L4 矩阵对象；备选 = 主选在 L4 失败时的替换（替换需重采矩阵）。

### 5.5 工具决策（临时 vs 常驻）

| 工具 | 形态 | 去留 |
| :--- | :--- | :--- |
| L1 RiverCliff 探针列扩展 | 临时：直接改 `terrain_probe.rs` 加列 **或** 新建临时 example（任选其一） | **验证后删除/还原**（根 AGENTS.md §4.10 持久化测试禁令） |
| L2 布局量测脚本（读 FABS/存档，算 L2 门） | 临时 Node/Rust 脚本（workspace 外，不入库） | 删除，只保留输出 CSV 与方法记录 |
| L4 96 视图矩阵采集脚本 | 临时 playwright 脚本（TA-06 同款做法，ASCII-only，eval 分块 <700 字符） | 删除，只保留截图与 `matrix.json` |
| 浏览器内季节/机位控制 | 复用 `ta06-acceptance.js` 的**做法**（覆写 `sim.currentSeason/seasonProgress`、相机反算钉点、`SimLighting.markDirty()`），不新建常驻面板 | 不新增文件 |

---

## 6. 详细设计（二）：样板冻结

### 6.1 冻结物（每场景 1 主选 + 1 备选，备选同规格）

| 项 | 内容 | 示例格式（占位，实施时填写实测值） |
| :--- | :--- | :--- |
| seed / profile | 显式 profile + 固定 seed | `seed=<N>, terrainProfile='mountain_pass_v1'`（A）/ `'river_valley_v1'`（B） |
| 存档 | 同版本（v1.52.x）t0 态 + t1 态存档各 1 份 | `ta09-A-t0.json`, `ta09-A-t1.json`, …（File System Access 导出，Chrome） |
| 相机锚点 | 世界坐标目标点 + 默认机位（rotZ/rotX/zoom/pan） | `__TGT_A_SADDLE=(x,y)` 等，矩阵脚本钉点用 |
| 配置摘要 | 完整 SIM_CONFIG + SHA256（冻结配置法，D-B1-9 先例） | `sim-config-frozen.json` + SHA |
| 环境 | 源码 SHA、WASM 双副本 SHA256、Chrome 版本、窗口/DPR | 记入证据包 report.md §1 |
| 矩阵版本 | 96 视图参数表（§9.1）版本号 | `matrix-v1`（参数变更即升版重采） |

### 6.2 冻结规则

1. **显式 profile 锁定**：样板一律用显式模板名，不用 `random`（random 映射随候选池变更，08 路→未来扩池会漂移）；
2. **t0/t1 双态冻结**：t0=创世完成未推进 tick（与 D-B1-9 跨构建差分同口径）；t1=路网/聚落成型态（tick 数按实测固定，全矩阵用同一 t1）；
3. **存档链路**：冻结存档必须走**真实 Chrome File System Access API** 导出/导入验证（TA-05/TA-06 的 LOAD 缺口教训：`?nogate=1` 不能替代 LOAD 验证）；
4. **备选激活规则**：主选在 L4 任一 P0 门失败且判定为“种子特异性”（非系统性 bug）时，启用备选并**重采全部矩阵**；若为系统性问题则修代码（§8）后重采。

---

## 7. 详细设计（三）：画面核对清单

### 7.1 通用清单（两场景共用，映射 07 §11.2/§11.4 + 31 §8.5）

| # | 检查 | 方法 | 通过标准 |
|---|------|------|----------|
| G-1 | 5 秒可读性 | 普通视图远景截图，评审人 5 秒内指出主要聚落、水源区、主路 | 3/3 命中（07 §11.2） |
| G-2 | 接地 | 近景抽查树/屋/人/石：无悬空、无入土半截、无树冠裁断 | 零违例（07 §11.4 旋转与遮挡行） |
| G-3 | 树屋人穿插 | 四方位×低高俯角旋转切片：高树与坡地/房屋穿插由深度缓冲正确解决；选中信息卡始终可读 | 31 §8.5 矩阵第一行 |
| G-4 | 道路不切屋顶 | 中近景抽查主路与房屋关系 | 零穿透（07 §11.1 已通过项回归） |
| G-5 | 装饰不挡路/不挡取水点 | `landscape-mask.js` 保护区数值复核 + 近景截图 | 车道/房屋/POI 保护区内禁入图元零违例 |
| G-6 | 季节可辨 | 四季同机位对照：春芽/夏冠/秋色/冬枝可辨；冬落叶树叶量约 0~5%、常绿保轮廓 | 07 §11.4 四季行（地表季相缺失记局限，不判失败） |
| G-7 | 受光 | 固定季相变光向（azimuthOffset 0/90/180/270 + `markDirty()`）：枝干/树冠/岩面亮部与投影协调，亮部不黏屏幕 | TA-06 受光分离同款方法（固定光转相机 + 固定季相变光向） |
| G-8 | 旋转/缩放稳定性 | 四方位×两俯角×三缩放：无纹理漂移、无闪烁、无漏画 | 07 §11.4 分级与剔除行 |

### 7.2 场景 A 专属清单（支脊山口）

| # | 检查 | 通过标准 |
|---|------|----------|
| A-V1 | 主脊+支脊+鞍部中景可读 | 中景（zoom≈2.5）一屏内可辨主脊走向、≥1 条支脊、鞍部低口 |
| A-V2 | 土路从前景聚落引向山口 | t1 态：前景聚落→山口有连续可读主路（wear 高分位），与内核路网几何一致 |
| A-V3 | 坡脚疏林成组 | 坡脚 Tree/Bush 成组分布，不遮挡主路与房屋；组间留白可读 |
| A-V4 | 山体保留露岩 | 主脊/支脊陡带 RockFace 可读，无“全山刷绿” |
| A-V5 | 山口走廊近景 | 近景核对山口段：路面连续、无崖体/装饰侵入走廊 |

### 7.3 场景 B 专属清单（岩壁河谷）

| # | 检查 | 通过标准 |
|---|------|----------|
| B-V1 | 远岸层岩为主地标 | 远景一屏内崖体可读为最显著地标（色相/明暗与周边区分） |
| B-V2 | 河流引导视线 | 远/中景：河面连续，视线沿河-崖组织 |
| B-V3 | 近岸低河阶聚落 | 近岸河阶上有可读聚落带，房屋接地、无水淹（占地不碰浅水/深水） |
| B-V4 | 浅滩与岩壁错开 | 中景一屏内可同时看到浅滩踏石与崖体，且空间错开关系明确 |
| B-V5 | 崖体不压近路/取水点 | 近景：崖体与最近车道/取水点的空间关系清晰，无压盖（数值 B-L4 + 截图双重确认） |
| B-V6 | 跨河只经浅滩 | 抽查跨河路径：普通陆路跨河点均落在两处浅滩走廊内 |

### 7.4 模拟推进核对（t0 vs t1）

| # | 检查 | 说明 |
|---|------|------|
| P-1 | t0 态地貌骨架 | 创世完成未推进：支脊/鞍部/崖体/河面/浅滩几何即最终态（静态地形承诺） |
| P-2 | t1 态聚落/路网涌现 | 路网成型、房屋落位后：主路关系、聚落带位置符合 §7.2/§7.3；装饰/景观无随 tick 抖动（骨架与候选坐标 100% 确定，07 §8） |
| P-3 | 资源断流反馈 | 若 t1 推进中出现断流：漂亮水面/树林不得掩盖“当前不可采”事实（07 §11.2 ◐项，能观察到即记，不能则记 N/A） |

### 7.5 生命周期/确定性/性能核对

| # | 检查 | 方法 | 通过标准 |
|---|------|------|----------|
| L-1 | 暂停冻结 | 暂停后季相/飘叶冻结（飘叶未做则只查季相不受 tick 影响） | 07 §11.4 生命周期行 |
| L-2 | 读档/回溯重建 | LOAD t0/t1 存档、REWIND 回滚后重推进：地形/路网/装饰/景观无残留、无串味 | 与冷重建逐位一致（TA-06 §4.4 同款 A→B→A 链路） |
| L-3 | 换世界 | A↔B 种子切换：旧 `terrain_features/accents/sub_features` 零残留 | 同上 |
| L-4 | 确定性 | 同种子同 tick 两次运行画面逐位一致（截图 SHA 或 FABS 指纹） | 零漂移 |
| L-5 | 性能 | 固定镜头 + 连续拖动/跟随，预热后采样 ≥60s（07 §11.3 口径） | 主线程 p95 ≤16ms 参考；**如有新增绘制**则增量 p95 ≤3ms；仿真吞吐下降 ≤5%；视觉缓存 ≤64MiB |

---

## 8. 详细设计（四）：最小视觉修补（条件触发，非默认）

### 8.1 触发规则

1. L4 评审中任一 P0 检查（§7 中 G-1~G-5、A-V1/V2、 B-V1/B-4/B-5/B-6 任一项）失败时，先判定：**种子特异性**（换备选可解）还是**系统性**（换种子仍在）；
2. 种子特异性 → 启用备选（§6.2.4），不写代码；
3. 系统性 → 进入本节，每项修补独立任务、独立提交、A/B 对照，修完重采受影响矩阵；
4. 修补范围**严格限于纯表现层**；若评审暴露的是物理/布局 bug（如崖体压取水点数值违例），则**不是 TA-09 修补**——应另立缺陷任务修内核（TA-09 暂停，待修完重跑 L1/L2）。

### 8.2 候选修补项（按可能性排序，实施时按需取用，不预设全做）

| ID | 候选修补 | 触发条件 | 改动点（估计） | 不做 |
| :--- | :--- | :--- | :--- | :--- |
| TA-09-F1 | Cliff 落笔增强（崖缘走向可读性） | B-V1/B-V4 失败：远景崖体不显著，或崖缘与河岸混淆 | `render_terrain.js::drawFeatureItem` Cliff 分支：调整带宽/透明度/色相（世界光向调制，`SimLighting` 现有接口）；必要时加崖脚碎石色带（**贴面色带，非 S4-X3 子图元**） | 不加层岩纹理算法、不做悬挑/洞穴、不碰 cells |
| TA-09-F2 | RockFace 受光/色相校准 | 崖体/主脊露岩在某光向/俯角下发黑或发灰、与 2D 色板脱节 | `math.js::computeElevationColor` RockFace 段或 `terrain-style.js` RockFace 乘色（profile 白名单内调参）；GL 侧 shader 若与 CPU 色板脱节则对齐公式（**公式同源，不另发明**） | 不改坡度阈值（34°）、不改地表派生 |
| TA-09-F3 | 主路可读性微调 | A-V2/G-1 失败：主路在山口段/林缘段不显著 | `config.render.js` 道路样式（线宽/色阶 `wear` 映射）、遮罩留白余量；只调**表现层**参数 | 不改路网生成、不改 wear 语义、不加发光（07 §3.2：明艳色只在 R 热力图） |
| TA-09-F4 | 坡脚/崖脚装饰违例清理 | G-5/A-L5/B-L4 数值违例：装饰侵入走廊/压取水点 | 先查 `landscape-mask.js` 保护区覆盖是否漏了 Cliff/鞍部几何（补保护源，不改判定）；或核对装饰生成禁区（v1.52.0 两道禁区是否覆盖崖体——崖体是 RockFace+NO_WALK，生成禁区已覆盖 NO_WALK，**大概率无需改**） | 不改 `accent_rng` 消费序（改序=全图装饰重排，须升版并重采全部矩阵） |
| TA-09-F5 | 标签/聚合微调 | G-1 失败：聚落/水源/主路 5 秒不可读 | `config.render.js` 标签阈值（`labelClusterMaxZoom` 等，S4-07 既有参数） | 不改布局算法 |

> **每项修补的交付格式**：改动文件 + 参数 diff + 修前/修后同机位 A/B 截图 + 受影响矩阵重采记录 + 门禁（`frontend-check.js` 全绿；若动 Rust 则全套 R.5 门禁 + WASM 双副本 + `test-wasm/test-determinism/snapshot-check`）。

### 8.3 修补禁区（再强调）

- 不改 `plan_subfeatures`、注入器、事务域、地表派生、路网、生态、快照/FABS/存档；
- 不新增 SimConfig 字段（视觉参数只进 `config.render.js` / `config.lighting.js`）；
- 不新增常驻面板/常驻测试文件；单文件 ≤800 行（`render_terrain.js` 现 ~554 行，F1/F2 增量须控制在百行内，否则先拆分）。

---

## 9. 详细设计（五）：取证矩阵与证据包

### 9.1 96 视图矩阵（每场景）

复用 07 §4.4.1/§11 与 TA-06 矩阵口径：**四季 × 四方位 × 两俯角 × 三缩放 = 96 张/场景**，两场景共 192 张。

| 维度 | 取值 | 说明 |
| :--- | :--- | :--- |
| 季节 | 春/夏/秋/冬（季中 `u=0/0.25/0.5/0.75`） | 覆写 `sim.currentSeason/seasonProgress` + `SimLighting` 相位 + 地形烘焙色（TA-06 `ta06-acceptance.js` 同款做法，矩阵脚本内联） |
| 方位 | rotZ = 0/90/180/270° | 相机旋转 |
| 俯角 | rotX = 1.05（标准）/ 0.55（低俯角） | TA-06 同款两档；必要时加 1.30 陡俯角为补充（不计入 96，由评审决定） |
| 缩放 | zoom = 1.0（远）/ 2.5（中）/ 6.0（近） | TA-06 同款三档 |
| 钉点 | 场景锚点（§6.1 `__TGT`）钉到屏幕中心 | `project3D()` 反算 pan，一次即中 |
| tick 态 | t1 冻结态为主；t0 态每场景加采 12 张代表帧（四季×三缩放，rotZ=0/rotX=1.05） | t0/t1 双态覆盖 §7.4 |

**逐张回读校验**（TA-06 同款，不可省略）：每张记录注入返回值 + `zoom/rotZ/rotX/pxPerM/season/lightDir` 回读；回读不符的截图作废重拍。脚本注意 TA-06 踩坑：playwright eval 源码 <700 字符分块、脚本 ASCII-only（中文注释会吞行）、改光向必须调 `SimLighting.markDirty()`。

### 9.2 专项取证（矩阵之外）

| 专项 | 内容 | 张数估计 |
| :--- | :--- | :--- |
| 受光分离 | A 固定光转相机（4 方位回读 `lightDir` 一致 + 最亮质心移动）；B 固定季相变光向（azimuthOffset 0/90/180/270，截图 md5 全不同） | 8/场景 |
| 生命周期 | A→B→A 换世界链 + LOAD/REWIND 各 1 组 | ~6 |
| 推进对照 | t0/t1 同机位对照（远/中/近 × 3） | 6/场景 |
| 近景专证 | A: 山口走廊/坡脚接地；B: 崖-滩错开/崖-路关系/取水点 | ~8/场景 |
| 拼图 | 每场景四季拼图（TA-06 `sheets/` 同款 3×4） | 8（两场景×两俯角×？按实际拼） |

### 9.3 像素指标（复用 TA-06 口径）

每张计算 `meanRGB / greenRatio / brownRatio / brightRatio / 最亮 10% 像素质心`，输出 `metrics/matrix.json` + 四维聚合表。**口径边界**（TA-06 已论证，TA-09 沿用）：`greenRatio` 不是落叶/植被的干净代理（地形基底本身偏绿），季节可辨性由 meanRGB 明暗 + brownRatio 峰谷 + 数值层叶量 + 同株近景对照共同支撑；崖体可读性建议**新增 `rockRatio`**（暖灰 R≈G≈B 高亮低饱和像素占比，阈值在 TA-09-4 中校准并记录）作为 B 场景的辅助指标，不作为通过门（避免指标游戏）。

### 9.4 证据包布局（复用 TA-06 结构）

```text
docs/plan/tech/assets/ta09-evidence-<YYYYMMDD>/
├── report.md                  # 验收报告（环境/结论/逐项对照/未做项/已知偏差）
├── manifest.json              # 入库文件清单（路径/说明/sha256-16/字节数 + 矩阵逐张回读）
├── metrics/
│   ├── matrix.json            # 192 视图 + 专项逐张指标 + 四维聚合
│   ├── seed-screening.json    # L1/L2 候选表 + 拒绝分布（★ TA-09 特有，防“漂亮截图”质疑）
│   └── fixture.json           # 冻结样板（seed/存档哈希/相机锚点/配置 SHA）
├── visual/
│   ├── matrix/                # 代表帧原图（每场景 6~12 张）
│   ├── sheets/                # 四季拼图 8 张
│   ├── scene-A/               # A 专属（山口走廊/坡脚接地/推进对照）
│   ├── scene-B/               # B 专属（崖滩错开/崖路关系/取水点）
│   ├── lighting/              # 受光分离 16 张
│   └── lifecycle/             # 生命周期 6 张
└── saves/                     # 冻结存档（t0/t1 × 2 场景，.json；若体积大则只入哈希+导出说明）
```

> 原始 192 张全部分辨率截图与临时脚本保留在工作区不入库（TA-06 同款做法）；入库为拼图 + 代表帧原图 + 全量指标 JSON。

---

## 10. 任务分解（WBS）

| ID | 任务 | 输入 | 步骤概要 | 产物 | 验收 | 估计 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| TA-09-1 | L1 几何筛选 | §5.1/§5.2 | ① 冻结 §2.1 基线并记录；② 跑 T1/T2 探针矩阵；③ 临时扩展 RiverCliff 探针列并枚举接受 seed；④ 输出候选表 + 拒绝分布 | `seed-screening-L1.json`（候选表+拒绝分布） | A 窗内通过门种数 ≥4；B 实际接受 ≥10（不足按扩展窗补样）；临时探针代码已删 | 1~1.5 人日 |
| TA-09-2 | L2 布局筛选 + t1 固定 | L1 候选表 | ① 完整世界创世（t0）；② 推进至路网成型并固定 t1 tick；③ 量测 L2 门（A-L1~L5/B-L1~L5）；④ 输出短名单 4~6/场景 | 短名单 + 布局快照 + t1 tick 记录 | 短名单每场景 ≥4；t1 固定值记录；量测方法可复现 | 1~1.5 人日 |
| TA-09-3 | L3 视觉初选 + 样板冻结 | L2 短名单 | ① mapOnly 扫图 + 实机短看；② 对照 §1.2/§7 打分并记录淘汰原因；③ 冻结 2×（1 主+1 备）+ 存档 + 相机锚点 + 配置 SHA | 冻结样板（§6.1 全项）+ 初选评审记录 | 主/备齐备；存档真实 LOAD 验证通过；锚点钉点可用 | 0.5~1 人日 |
| TA-09-4 | 96 视图矩阵采集 + 指标 | 冻结样板 | ① 写临时矩阵脚本（含逐张回读）；② 采 192 张 + 专项；③ 算像素指标 + 聚合；④ 拼图 | `matrix.json` + 截图 + 拼图 | 192 张回读全符；指标 JSON 完整；作废重拍记录留存 | 1.5~2 人日 |
| TA-09-5 | 人工评审 + 修补闭环 | 矩阵 + 清单 §7 | ① 按 §7 逐项评审（通过/失败+证据）；② 失败项按 §8.1 分流（换备选/修补/另立缺陷）；③ 修补项独立提交 + A/B + 重采 | 评审结论表 +（如有）修补代码 + A/B + 重采记录 | P0 零失败（或失败项均有处置+证据）；修补门禁全绿 | 0~2 人日（零修补时只计评审 0.5） |
| TA-09-6 | 生命周期/确定性/性能 | 冻结样板 | 按 §7.5 执行 L-1~L-5 | 测试记录（截图/指纹/耗时表） | L-1~L-4 通过；L-5 满足预算或如实记录偏差 | 0.5~1 人日 |
| TA-09-7 | 证据包归档 + 文档同步 | 全部产物 | ① 组装 §9.4 证据包 + report.md；② 按 §13 同步文档；③ TODO 勾选（需满足 §11 勾选条件） | 证据包 + 文档 diff | §13 清单全勾；`cross-doc-check` 冲突 0 漂移 0 | 0.5~1 人日 |

**合计**：5~8 人日（零修补下限 ~5，单项修补 ~6.5，多项修补上限 8）。

---

## 11. 验收门禁

### 11.1 门禁总表

| 类 | 门 | 通过标准 | 证据 |
| :--- | :--- | :--- | :--- |
| 物理复核 | L1/L2 门全过 | §5.2/§5.3 表格零失败 | `seed-screening.json`（候选表+拒绝分布） |
| 视觉 | §7.1~7.3 清单 | P0 项（G-1~G-5、A-V1/V2、B-V1/B-4/B-5/B-6）全过；非 P0 失败记已知局限 | 192 视图 + 专项 + 评审结论表 |
| 推进 | §7.4 | P-1/P-2 通过；P-3 能观察即记 | t0/t1 对照截图 |
| 生命周期 | §7.5 L-1~L-4 | 全过 | 截图/指纹记录 |
| 性能 | §7.5 L-5 | 满足预算或偏差经评审接受（TA-04-8 维持现状先例：偏差可归档但必须明确记录） | 耗时表 |
| 确定性 | L-4 + 跨运行 | 同种子同 tick 逐位一致 | 指纹/截图 SHA |
| 工程 | 门禁脚本 | `frontend-check.js` 全绿；如动 Rust 则 R.5 全套（cargo test/WASM 双副本/test-wasm/test-determinism/config-check/snapshot-check）+ `cross-doc-check` | 门禁输出 |
| 文档 | §13 | 清单全勾 | 文档 diff |

### 11.2 勾选条件（TODO.md D-B1-8 / 07 §1.2 TA-09 标 ✅ 的充要条件）

1. §11.1 全表通过（性能偏差如有则已按 TA-04-8 先例明确归档）；
2. 证据包入库且 report.md 有明确结论句；
3. 07 号 §4.4.1 已回填实机交付记录（含 seed/存档/版本/配置/窗口 DPR/相机参数）；
4. **构图草案通过≠勾选**（07 号原话：草案通过不得提前勾选）——本次勾选的是实机交付，草案早已通过不重复计。

### 11.3 评审角色建议

- 执行人：跑筛选+矩阵+初评；
- 评审人（用户）：§7 中 G-1（5 秒可读性）与 §1.2 对齐要求两项必须由**非执行人**独立判定（避免自评偏差）；
- 记录人：证据包与文档同步（可与执行人同一人）。

---

## 12. 风险与缓解

| # | 风险 | 概率 | 影响 | 缓解 |
|---|------|------|------|------|
| R1 | B 场景接受种子不足 10 个（RiverCliff 结构型 ~20% × 局部拒绝） | 中 | 高（阻塞 L2） | 扩展窗至 255（06 §5.4.D.7 已允许）；v1.52.6 在 0~85 已有 12 例，复跑大概率复现；若仍不足则检查探针列实现（临时代码 bug 优先排查） |
| R2 | 主路不经山口/聚落带不理想（布局随机性） | 中 | 中 | L2 短名单放宽到 6 个；t1 tick 可调（固定后全矩阵一致）；主选失败用备选 |
| R3 | Cliff 当前落笔太弱，B-V1 评审失败 | 中 | 中 | §8 F1/F2 最小修补路径已预留；修补前先确认是“落笔弱”还是“种子差”（换 2 个备选对照） |
| R4 | 地表无季相导致冬季评审观感扣分 | 高（确定存在） | 低 | 预先声明为 TA-13 已知局限，不列 P0；评审聚焦地貌骨架/道路/遮挡/接地 |
| R5 | 双管线差异（GL 地形 vs 2D 覆盖层特征）导致崖缘错位观感 | 低 | 中 | 主口径就是默认混合管线，所见即所得；若发现系统性错位则另立缺陷（非 TA-09 修补） |
| R6 | 存档 LOAD 链路无 Chrome 环境（重蹈 TA-05/TA-06 缺口） | 低 | 高 | TA-09-3 明确要求真实 Chrome；若确实无环境则**如实记 NOT_RUN 且不勾选**（不重蹈“先勾后补”） |
| R7 | 矩阵脚本踩坑（eval 长度/编码/光照闸） | 中 | 低 | TA-06 §过程坑三条直接复用（分块/ASCII/`markDirty()`）；先跑 8 张冒烟再全量 |
| R8 | 范围蔓延（顺手做 S4-X3/TA-13/层岩算法） | 中 | 中 | §3.2 边界表 + §8.3 禁区；评审中出现“想要”时记入对应任务 backlog，不在 TA-09 内做 |

---

## 13. 版本/门禁/文档同步清单

### 13.1 版本判定树

```text
TA-09-5 结论为零修补？
├── 是 → 纯文档/证据改动：不升版、不编译 WASM（TA-06 证据包先例）
└── 否 → 有前端-only 修补？
    ├── 是 → bump patch（如 v1.52.6→v1.52.7），兼容线不动，旧档照用
    └── 否（动了 Rust）→ 重编译 WASM + 同步双副本；
        是否改变物理生成？是 → 递增 TERRAIN_GENERATOR_VERSION（§8.3 已禁止此类修补，此分支理论不可达）
```

### 13.2 门禁清单（按变更性质执行）

- 纯筛选/取证/文档：`cross-doc-check.js`（冲突 0 漂移 0）+ `doc-link-check` + `bump-version.js --check`（07 §11.5 纯文档口径）；
- 前端修补：上 + `frontend-check.js`（Chrome 存档链路走[浏览器指南](docs/current/tech/27-browser-automation.md)，`?nogate=1` 只做非存档验证）；
- Rust 修补（理论不可达）：上 + R.5 全套 + `snapshot-check.js`（若碰快照）+ `config-check.js`（若碰配置）。

### 13.3 文档同步清单（TA-09-7 执行）

| # | 文件 | 改动 |
|---|------|------|
| 1 | `docs/plan/tech/07-terrain-art.md` | §1.2 TA-09 行：依赖改“✅ 前置已就绪（TB-01 + RiverCliff v1.52.6）”+ 状态 ✅；§1.3/§1.4 移除 TA-09 待办节点；§4.4.1 回填实机交付记录（seed/存档/版本/配置/窗口 DPR/相机参数 + 证据包链接）；§11.2 待验收项勾选对应项 |
| 2 | `docs/plan/tech/06-terrain-templates.md` | R.1/R.1.1/R.3/R.4/§19/§20 中 TA-09/D-B1-8 行改“实机样板已交付”+ 证据包链接 |
| 3 | `TODO.md` | D-B1-8 条目勾选（附证据包链接与冻结 seed）；§0 收官区按需增补 |
| 4 | `docs/current/01-changelog.md` | 增条目（纯文档收口无版本变更，或 patch 版本条目） |
| 5 | `docs/current/tech/31-code-map.md`（如有） | 若证据包新增目录则登记 assets 树（TA-06 先例） |
| 6 | 本方案 | 评审通过后状态改“已评审”；收口后按 TA-05/TA-11 先例决定保留或归档删除（建议：收口后删除本文件，权威收敛到 07 号 §4.4.1 + 证据包 report.md，避免三处维护漂移） |

---

## 附录 A. 文件清单（TA-09 触及/消费）

| 文件 | TA-09 中的角色 | 是否改动 |
| :--- | :--- | :--- |
| `crates/sim_core/src/geo/terrain.rs`（支脊/选择器/生成器版本） | 消费（L1 探针读数） | 否（探针列临时扩展除外，用后还原） |
| `crates/sim_core/src/geo/hydrology.rs`（RiverCliff/主河） | 消费 | 否 |
| `crates/sim_core/src/geo/geometry_transaction.rs` | 消费 | 否 |
| `crates/sim_core/examples/terrain_probe.rs` | L1 主工具 | 临时扩展，用后还原/删除 |
| `frontend/js/render_terrain.js`（Cliff/浅滩/河面落笔） | F1 候选修补点 | 条件触发（§8） |
| `frontend/js/math.js`（`computeElevationColor`）/ `terrain-style.js` | F2 候选修补点 | 条件触发 |
| `frontend/js/config.render.js` / `config.lighting.js` | F3/F5 调参 | 条件触发（只调参） |
| `frontend/js/landscape-mask.js` | G-5/B-L4 数值口径 | 大概率不改（F4 先查覆盖源） |
| `frontend/js/ta06-acceptance.js` | 矩阵脚本做法参考 | 否（不新建常驻面板） |
| `docs/plan/tech/assets/references/composition-draft-v1.png` | L3 对照目标 | 否 |
| `docs/plan/tech/assets/ta09-evidence-*/` | 证据包（新建） | 新增 |
| 临时脚本（探针扩展/量测/矩阵） | L1/L2/L4 工具 | **用后删除** |

## 附录 B. 96 视图矩阵参数表（matrix-v1，冻结后变更即升版）

| 参数 | 值 |
| :--- | :--- |
| 季节覆写 | `sim.currentSeason/seasonProgress` = 春季中点/夏季中点/秋季中点/冬季中点（`u=0/0.25/0.5/0.75` 季中语义，TA-06 同款）+ `SimLighting` 相位同步 + `markDirty()` |
| rotZ | 0 / 90 / 180 / 270（度） |
| rotX | 1.05 / 0.55（补充档 1.30 由评审决定是否加采） |
| zoom | 1.0 / 2.5 / 6.0 |
| 钉点 | 场景锚点居中（`project3D` 反算 pan） |
| 视口 | 1280×720 CSS px，DPR 记录实测值（TA-06 用 DPR 1；TA-09 按实际 Chrome 窗口记录，不强制同一 DPR，但全矩阵内必须一致） |
| tick 态 | t1 冻结态 96 张 + t0 态 12 张代表帧/场景 |
| 回读 | 每张记录 `zoom/rotZ/rotX/pxPerM/season/lightDir`，不符作废重拍 |

## 附录 C. 种子记录模板（L1/L2 输出，JSON/CSV 任选其一，字段固定）

```json
{
  "scene": "B",
  "seed": 0,
  "profile": "river_valley_v1",
  "generator_version": 14,
  "app_version": "1.52.6",
  "L1": {
    "plan_hit": "RiverCliff",
    "accepted": true,
    "subfeature_1005": true,
    "feature_224_verts": 9,
    "components": 1,
    "rockface_cells": 0,
    "band_ratio": 0.0,
    "cliff_len_m": 0.0,
    "cliff_h_m": 0.0,
    "cliff_side": "+1/-1",
    "ford_clearance_m": 0.0,
    "access_clearance_m": 0.0,
    "verdict": "pass",
    "fail_code": null
  },
  "L2": {
    "t0_tick": 0,
    "t1_tick": 0,
    "survival": "ok",
    "crossing_via_ford_only": true,
    "cliff_road_overlap": 0,
    "verdict": "pass"
  }
}
```

## 附录 D. 初选评审记录模板（L3 输出）

| 场景 | seed | L1/L2 | 初选结论 | 淘汰/入选原因 | 对齐项（§1.2）逐项 | 截图 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| A | — | pass | 主选/备选/淘汰 | … | 主路可达□ 支脊鞍部□ 坡脚接地□ | 远/中/近各 1 |

## 附录 E. 命令速查

```bash
# L1 探针（参数以 --help 为准）
cargo run --release -p sim_core --example terrain_probe -- --profile mountain_pass_v1 --seeds 60
cargo run --release -p sim_core --example terrain_probe -- --profile river_valley_v1 --seeds 60

# 实机查看（正式世界 / 图鉴扫图）
http://localhost:3000/?seed=<N>&nogate=1
http://localhost:3000/?seed=<N>&mapOnly=1&nogate=1
# Canvas 对照（仅问题定位）：&accentgl=0
# 植被观察台做法参考：&ta06=1

# 门禁
node tools/frontend-check.js
node tools/cross-doc-check.js
node tools/config-check.js            # 仅配置变更时
node tools/test-wasm.js               # 仅 Rust 变更时
node tools/test-determinism.js        # 仅 Rust 变更时
```

## 附录 F. 参考链接

- 任务台账：[07 号 §1.2/§1.3/§1.4](docs/plan/tech/07-terrain-art.md) · 组合地貌美术规则 [07 §4.4](docs/plan/tech/07-terrain-art.md) · 实机交付记录 [07 §4.4.1](docs/plan/tech/07-terrain-art.md) · 验收 [07 §11](docs/plan/tech/07-terrain-art.md)
- 注入器规格：[06 号 §5.4.D RiverCliff](docs/plan/tech/06-terrain-templates.md) · 创世流水线 [06 §5.3](docs/plan/tech/06-terrain-templates.md) · 有界重试 [06 §5.8](docs/plan/tech/06-terrain-templates.md) · 验收 [06 §18](docs/plan/tech/06-terrain-templates.md)
- 遮挡判据：[31 号 §8.5](docs/plan/tech/31-canvas-to-webgl-migration.md) · 现状管线：[16 号 §2.1](docs/current/tech/16-frontend-overview.md)
- 证据先例：[TA-06 证据包 report.md](docs/plan/tech/assets/ta06-evidence-2026-09-22/report.md) · 收口先例：[09 号验证方案](docs/plan/tech/09-vegetation-verification.md)
- 待办：[TODO.md D-B1-8](TODO.md) · 专属景观边界：[STAGE-04-SUBFEATURE-LANDSCAPES-TODO.md](STAGE-04-SUBFEATURE-LANDSCAPES-TODO.md)
