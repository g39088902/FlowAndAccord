# TA-09 技术实施方案：支脊山口与岩壁河谷实机样板交付（D-B1-8）

> **状态**：草案（已完成源码对照修订，待实机执行与用户验收）｜ **日期**：2026-09-22
> **任务权威**：[07 号 §1.2 / §4.4.1](docs/plan/tech/07-terrain-art.md) / [06 号 D-B1-8](docs/plan/tech/06-terrain-templates.md)。本文规定执行方法，不把规划、历史验收或本次文档审查写成 TA-09 已通过。
> **审查基线**：源码 `aedf00e6475c6ddc120ea4f29dea4cc10a2ed54d`，应用 **v1.52.6**，`TERRAIN_GENERATOR_VERSION=14`，`SAVE_FORMAT_VERSION=7` / `SAVE_APP_VERSION=1.52`，FABS `FORMAT_VERSION=4`，格网 256×256、世界跨度 764m、步长约 2.996m。
> **前置结论**：TB-01 与 RiverCliff 注入器已落地，**可以开始筛选，但不能据此承诺视觉验收无阻塞**。当前仍是混合管线，房屋、人物、道路、水面与 Cliff 描线尚未全部进入 GL 深度缓冲；[31 号 §8.5](docs/plan/tech/31-canvas-to-webgl-migration.md) 的跨层遮挡结果须实测，不能由“装饰已迁 GL”推定通过。

---

## 0. 一页摘要（TL;DR）

**目标**：交付两组真实、可复现的实机场景：A=支脊山口 `mountain_pass_v1`；B=岩壁河谷 `river_valley_v1` 且最终世界实际保留 RiverCliff。每场景冻结 **1 主选 + 1 备选**（合计 2 主、2 备），每个样板保留 t0/t1 存档、完整配置、相机锚点与环境记录。

```text
TA-09-0 环境/入口/遮挡冒烟，确认可执行性
  → L1 地形种子矩阵（记录所有结果，不先挑漂亮画面）
    → L2 完整世界复核 + 有界、精确 Tick 推进
      → L3 构图初评，冻结每场景 1 主 + 1 备
        → L4 两个主选各 96 视图 + t0 对照 + 专项
          → 评审 / 有限视觉修补 / 必要时转交阻塞缺陷
            → 可下载证据归档 + 权威台账同步
```

| 决策 | 执行口径 |
| :--- | :--- |
| 验收路径 | 当前生产默认的 GL 地形/装饰 + Canvas 覆盖层；记录实际活动后端，不能只记录 `useWebgl=true`。不另建 Canvas 对照矩阵，不在 2D 侧追加遮挡机制 |
| 物理与视觉分工 | 消费已有 TB-01 / RiverCliff 物理能力与历史准入记录；复核样板和扫描窗，不重做全部离散尺度组合。任何物理违例仍须转缺陷，不能按“样板选材”掩盖 |
| 数量 | 主矩阵 2×96=192 张；t0 补充 2×12=24 张，**基础采集共 216 张**；主矩阵拼图 2×4 季×2 俯角=**16 张**。备选不重复全矩阵，激活时才补齐 |
| 代码范围 | 首选零代码；必要修补只限既有表现层的色板、线样式、遮罩消费与标签参数。物理、创世、存档、协议和 TC-03 迁移另立任务 |
| 版本 | 文档/证据不升版；不改变模拟与存档契约的前端表现修补走 patch。按**行为变化**而非文件语言决定版本，详见 §13 |
| 工期 | 无修补 **6～9 人日**，有限修补另加 **0～2 人日**，合计 **6～11 人日**；外部缺陷、Chrome 环境和用户评审等待另计（§10） |

**交付物**：冻结清单及 8 份存档（4 个 seed×t0/t1）、报告与全量矩阵索引、16 张主矩阵拼图和代表帧、可恢复的原始证据归档、必要的修补 A/B、07/06/TODO 对应记录。主选 seed 尚未实测，本文不预填。

### 0.1 本轮审查修正的关键问题

| 原问题 | 修订 |
| :--- | :--- |
| 把地形/装饰 GL 化等同于全管线统一遮挡 | 增加早期跨层遮挡冒烟；未迁移导致的系统性失败转 TC-03，不以换种子隐藏 |
| T1 沿用 `buildable≥10500`、`detour≤4.8` 等旧门槛 | 对齐当前 256 格探针：`buildable≥47600`、`detour_max∈[2.0,5.2]`、硬禁行比例 2%～5.5%；支脊坡度是信息列而非硬阈值 |
| 只给 `?seed=`，却要求显式 profile；忽略创世降级 | 补齐配置注入→RESET→回读流程；L2 验最终世界，不拿图鉴/候选替代。登记 Worker 对 seed=0 的既有入口缺陷 |
| “运行到好看”为止、同 Tick 截图 SHA 等同于确定性 | 预先固定有界推进规则；区分内核全量快照、派生模型、截图三层证据；墙钟游鱼等不纳入逐字节截图承诺 |
| 主备数量、拼图数量、工时合计互相矛盾 | 统一 2 主+2 备、216 张基础帧/16 张拼图，工期由 WBS 相加 |
| 大存档仅留哈希、失败有处置即可勾选、照抄历史测试命令 | 要求可下载存档与原图；失败/未测不自动算通过；按当前工作流门禁执行并补文档门禁 |

---

## 1. 背景与目标

### 1.1 已接受的构图方向

[07 号 §4.4.1](docs/plan/tech/07-terrain-art.md) 记录：2026-09-14 用户已接受构图方向。`docs/plan/tech/assets/references/composition-draft-v1.png` 是**视觉目标，非实机截图**；本任务不重复草案评审，也不要求逐像素复刻概念图。

| 场景 | 构图方向 | 必须由真实世界满足的条件 |
| :--- | :--- | :--- |
| A 支脊山口 | 主脊和不对称支脊围出低鞍部；土路从前景聚落引向山口；坡脚疏林、山体露岩 | 最终 `mountain_pass_v1` 世界保有支脊；可达路网穿过鞍部，聚落、支脊和山口有可读层次；近景接地正确 |
| B 岩壁河谷 | 远岸层岩为地标，河流引导视线；近岸低河阶聚落，浅滩与岩壁错开 | 最终 `river_valley_v1` 世界保有 RiverCliff；普通陆路只经授权浅滩跨河；崖体不侵入道路/取水操作区、不产生错误屏幕遮挡 |

“前景/近岸/远岸”相对于冻结的**构图主机位**定义。其余旋转机位用于证明接地、遮挡和连续性，不要求四个方位都把同一岸称为远岸，或每张近景都包含整个聚落。

### 1.2 前置状态与证据边界

| 项 | 本次源码核对 | 不能由此推导的结论 |
| :--- | :--- | :--- |
| TB-01 | `terrain.rs::sample_branch_ridges`、`TerrainMap::branch_ridges` 及探针支脊统计已存在 | 每个经过创世回退的正式世界仍保有支脊 |
| RiverCliff | `hydrology.rs::apply_river_cliff` 经几何事务注入；Cliff id=224、RiverCliff id=1005；生成器版本 14 | 选择器命中即代表最终 world_create 成功保留崖体 |
| 历史物理准入 | changelog v1.52.6 记载 144 组合通过、seed 0～85 内 12 例注入通过 | 本轮已重跑、当前所有配置都通过，或旧实测最小值都是规范阈值 |
| TA-08 | 任务已删除视为完成，遮挡判据移交 31 号 §8.5 | TC-03 的实体迁移/选中描边已完成 |
| 四类视觉子特征 | v1.52.5 已实现 `ForestedSlope` / `RockyOutcrop` / `RiversideForest` / `GravelBeach` | S4-X3 峭壁专属景观配方已完成 |

**计划修订即同步前置事实，不等任务收口才纠正过时台账**。07 号/TODO 中旧“RiverCliff 空壳”表述保留为历史背景时必须标明日期；TA-09 本身继续未完成。

### 1.3 完成定义（Definition of Done）

1. 两场景主/备样板、t0/t1 存档与复现说明齐备；相同冻结环境下能重现相同模拟事实与构图。
2. 两主选 192 张主矩阵、24 张 t0 补充及专项覆盖完整；逐张状态回读合格，人工结论能反查原图。
3. 真实推进、暂停、Chrome/Edge 文件存读、回溯、换世界已验证；不能只靠季节覆写夹具证明端到端正确。
4. §7 清单通过；已知限制、未测项、外部阻塞和用户明确接受的偏差分别记录，不把它们改写为 PASS。
5. 性能按 §7.6 实测；零代码时新增成本记 N/A，不写成已测得“0ms”；有修补时给出同条件前后对照。
6. 按 §11 / §13 收口。**本次文档修订不代表以上任一实机验收已完成。**

---

## 2. 现状基线盘点

### 2.1 版本、配置与环境冻结

| 项 | 当前值/冻结要求 | 权威入口 |
| :--- | :--- | :--- |
| 应用/存档/生成器/FABS | 1.52.6 / 7 与 1.52 / 14 / 4 | `frontend/index.html`、`world_save.rs`、`geo/terrain.rs`、`snapshot_bin/layout.rs` |
| 格网/世界跨度 | 256×256 / 764m；探针当前也硬编码该尺寸 | `config.js`、`sim_worker.js`、`terrain_probe.rs` |
| random 池 | 8 路；本任务不用 random 冻结 | `resolve_profile` |
| WASM | 双副本 SHA256 相同；本轮读取为 `9842bf7270315099a0a3db6ce0e7f7a9c5fcec2ab2244be3fbdd0d47489dc028` | `frontend/rust/sim_wasm.wasm` / `frontend/sim_wasm.wasm` |
| 模拟配置 | 实际注入的完整 `SIM_CONFIG`（含决策顺序、层级、升级矩阵）及再生倍率、agent/camp 数、dt | `rustworld.js` INIT/RESET 合并链；探针读取 `examples/config.json`，两者必须对齐 |
| 表现配置 | 实际 `RENDER_CONFIG`、`SIM_LIGHTING`、风格/模型版本、主题、图层开关、URL 参数和 FPS 设置 | 不只保存模拟配置；localStorage 中的个人偏好须隔离并记录 |
| 环境 | 源码 SHA；如含未提交修补则另记补丁 SHA；浏览器完整版本、OS、CPU/GPU/驱动、WebGL renderer、硬件加速、CSS 视口、画布像素尺寸、DPR | 模拟与视觉证据均引用唯一 `baseline_id` |

同一版证据不得混用不同配置、后端或环境。实施期间如有修补，按 §6.3 建立新基线并明确重采范围；冻结并不意味着禁止迭代，而是禁止把不同轮次冒充同一次验收。

### 2.2 地貌和生产入口

- T1 主脊、鞍部、不对称支脊和 fBm 已存在；`branch_ridges` 是 `#[serde(skip)]` 的诊断字段，**不会随存档/FABS 保留**，读档为空不能直接判定“支脊消失”，须比较实际高程/地表或使用创世诊断记录。
- T2 有主河、双浅滩、河阶、泉谷与共享水池。RiverCliff 段长 55～80m、抬升 6～12m，横向崖体 18m + 回落 12m，沿程两端各 6m taper；完整支撑域写 RockFace，不能把全部 RockFace 格都当作坡度 ≥34°。
- `world_create` 走 `new_seeded_with_config_bounded`，失败可禁用结构子特征、去支脊或降至 `flat_baseline`。`world_create_map`/纯地形探针没有完整生态/生存门禁，**相同 seed/profile 也不能自动认定最终地形一致**。
- 最终身份以快照 `terrain_profile` / `terrain_generator_version`（前端 `sim.terrain.profile/generatorVersion`）及存档 `seed/terrain_profile/terrain_state` 为准，不以请求参数或图鉴显示推断。

### 2.3 表现层实况与可用修补入口

| 层 | 当前实现 | TA-09 必查边界 |
| :--- | :--- | :--- |
| 地形/侧壁 | `webgl/layers/terrain/terrain-renderer.js`；CPU 反照率 + shader 受光 | `math.js::computeTerrainAlbedo` 才是色基座，仓库没有 `computeElevationColor`；RockFace 不保证自动呈现层岩纹理 |
| 装饰/景观主体/游鱼 | 既有绘制函数产生几何，经 `WebGLAccentLayer` sink 送 GL；阴影走 `WebGLShadowPass` | 与 GL 地形共享深度，不代表能对覆盖层实体做跨 Canvas 深度测试 |
| 河面/岸线/浅滩/Cliff | `render_terrain.js::drawFeatureItem`，Canvas 2D 覆盖层 | Cliff 目前只有宽阴影带和崖缘暗线，必须验其与 GL 装饰/地形及近路的关系 |
| 房屋/人物/道路/标签 | `render_world.js` / `render_agents.js` / `render_depth_queue.js`，仍有 2D 绘制 | 树后人物、岩后道路等不受同一 GPU 深度缓冲自动保障 |
| 季相/光照 | `SimTreeTint` 读真实相位；`SimLighting` 有限速、`resync()`、`lightRev()` | GL 路径跳过 CPU `cell.color` 重烘焙，不能用该字段是否变化判断光照成功；游鱼按墙钟更新，暂停不冻结全部像素 |
| 色板/道路配置 | `TerrainStyle` 现仅为冲积扇/盆地/湖畔提供白名单；自然道路色阶/线宽在 `cacheLaneStyle` | T1/T2 调 `TerrainStyle` 现有表不会生效；`config.render.js` 也没有现成的全部道路色阶参数，不得给出无消费点的“只调参”承诺 |

地表季节反照率尚属 TA-13；冬季地面仍偏绿是预先声明的范围限制。S4-X3 配方缺失亦不在本任务补齐，但**崖体地标、道路和聚落的构图要求不能因此降低为“有一条线就通过”**。

### 2.4 工具复用

复用 [TA-06 证据包](docs/plan/tech/assets/ta06-evidence-2026-09-22/report.md) 的目录、逐张回读和拼图方法；植被观察台 `ta06-acceptance.js` 可参考季节/相机钩子，不必新增常驻 TA-09 面板。

TA-06 的 `playwright-cli eval` 长度问题与 Windows PowerShell 5.1 编码问题是**该工具/环境的历史限制**，不是 Playwright 通用的“700 字符/ASCII-only”硬约束。当前环境先做失败可见的冒烟；需要时分块、使用 UTF-8 文件或 ASCII 脚本。

---

## 3. 范围与边界

### 3.1 在范围内

种子扫描与拒绝分布、完整世界布局筛选、有界推进、主备冻结、96 视图/场景、真实生命周期/交互检查、性能取证、必要的有限表现修补、证据与文档归档。

### 3.2 不在范围内

| 项 | 归属/处理 |
| :--- | :--- |
| 峭壁脚碎石堆/岩隙灌木配方 | S4-X3；不得以“贴面碎石色带”为名变相增加未评审配方 |
| FootLake/RidgeWaterfall/OxbowLake/R0、新 profile/random 池 | 各自后续阶段；不强开注入、不调概率来凑样板 |
| 地表季相、飘叶、台地、动态水文 | TA-13/TA-15/TA-10/TC-01 等 |
| 统一实体深度、2D 退役、描边 pass | TC-03 / 31 号；不增加 Canvas 排序、切片或局部透明遮挡补丁 |
| 物理/生态/创世/存档/FABS/SimConfig 修改 | 外部缺陷或独立任务；本计划先暂停受影响门禁，再重新冻结 |
| 重新开启已关闭的 TA-05/06/07 测试工作 | 不做。本文只检查 TA-09 场景；其他任务的取消/豁免也不自动授权 TA-09 跳过验收 |

### 3.3 铁律

- 不画不存在的水、路或资源，不移动 POI/房屋、不强制 agent 行为、不伪造 wear 来美化截图。
- 候选淘汰保留原因；物理失败保留原始记录与缺陷引用，不能仅删除失败 seed。
- 临时验证脚本按根 AGENTS.md §4.10 使用后清理；不修改随机消费顺序、稳定 ID、快照缓存协议来获得好画面。
- 新增代码文件遵守 ≤800 行；已有超长文件不得继续膨胀，必要时先拆分。仅文档变更不升版、不改运行时行为。

---

## 4. 总体流程与早期可行性门

### 4.1 TA-09-0：先冒烟，再批量取证

在大规模矩阵前用临时候选检查：

1. 默认路径的 GL 地形、装饰、阴影均初始化成功，覆盖层正常；无 404、异常、黑屏和静默回退，记录实际 `webglTerrainActive` 与 renderer 就绪状态。
2. 正式世界可正确锁定 profile、seed、配置及 Tick，完整静态快照到齐；真实文件存读环境已具备或明确为 NOT_RUN。
3. 各场景至少 8 个粗筛机位（四方位×两俯角，中景），加一组自然形成的树/屋/人及崖/路/取水点重叠视角；不得靠永不重叠的机位代替遮挡检查。
4. 若当前混合管线必然违反 31 号 §8.5 的对应结果，登记 **BLOCKED:TC-03** 或独立渲染缺陷。L1/L2 可继续，**不批量采集注定失效的 192 张矩阵，不宣称 TA-09 完成**。

### 4.2 L1～L4 产出关系

| 层 | 输入/工作 | 退出产物 |
| :--- | :--- | :--- |
| L1 几何 | 显式 T1/T2 × 顺序 seed 窗；复用生产生成/坡度/通行判据 | 全窗指标、选择/接受状态、失败原因与数量分布 |
| L2 布局 | 生产有界创世；复核最终模板/子特征；t0/t1 两态 | 每场景目标 4～6 个短名单及固定推进规则；不足则扩窗或报阻塞 |
| L3 初评 | 对照构图草案看真实世界，不以 mapOnly 定稿 | 每场景 1 主+1 备、主机位和专项锚点、淘汰记录 |
| L4 取证 | 冻结存档、相机、环境、矩阵参数 | 主矩阵/专项/人工结论；失败按 §8 分流 |

### 4.3 数据与失败分层

```text
plan_subfeatures（命中，不等于接受）
  → 几何事务局部接受/回滚 → 定稿地形/装饰
  → world_create 完整生态与全局门禁 → 可能降级 → 最终世界
  → FABS Terrain(8) / Features(18) / Accents(21) / SubFeatures(22)
  → rustworld 静态缓存 → GL 地形/装饰 + 2D 覆盖层 → 合成画面
```

每个阶段单独记录结果。缺少静态 section 的增量帧表示“未发送”，**不表示空集合**；诊断先强制全量帧再核对。纯图鉴只作快速观察，不作冻结样板或世界级门禁证据。

---

## 5. 详细设计（一）：种子筛选

### 5.1 扫描窗、配置和探针

| 场景 | profile | 初始窗 | 扩展/样本口径 |
| :--- | :--- | :--- | :--- |
| A | `mountain_pass_v1` | 0～59 | 候选不足按 seed 升序扩至最多 255；L2 目标短名单 4～6 个 |
| B | `river_valley_v1` | 0～59 | 先覆盖至 85，仍不足扩至最多 255；至少记录 10 个局部实际接受种子，并在完整世界上统计保留数。历史“12 例”不可直接充当本轮数据 |

```bash
# --seeds N 是数量：0..N-1；扫描到 85 用 86，到 255 用 256
cargo run --release -p sim_core --example terrain_probe -- --profile mountain_pass_v1 --seeds 60
cargo run --release -p sim_core --example terrain_probe -- --profile river_valley_v1 --seeds 86
```

**命令边界**：现有 CLI 支持上述参数，但没有专门 `--help` 分支；不要用 `--help` 误触默认扫描。现有输出不包含完整 RiverCliff 接受/拒绝诊断，且 T1 的失败判定是 `TB01_7_HAS_FAIL` 文本，不能只看进程退出码 0。

探针用 `include_str!("config.json")`，不是浏览器运行时配置。先逐字段比对冻结配置；如需临时配置/诊断扩展，保留差分、方法与输出后还原，**不得覆盖或提交生产 WASM**。`plan_subfeatures`/注入器为 crate 内部入口，临时 example 不能直接当作 public API 调用；需要的命中/局部拒绝原因须用临时 crate 内诊断或同源记录获得，不从最终缺失反猜。

### 5.2 L1 几何门

**A：按当前 `terrain_probe.rs::print_tb017_verdict` 口径。**

| ID | 判定 | 说明 |
| :--- | :--- | :--- |
| A-G1 | `components == 1` | 保留探针 walkable 定义及邻域，不和注入器内部只按 NO_WALK 的计数混名 |
| A-G2 | 主脊存在硬禁行带，`hard_ratio∈[2%,5.5%]` | 当前格网/默认物理配置下的探针窗口；全图最大坡度只作补充读数，不能证明主脊位置 |
| A-G3 | `detour_max∈[2.0,5.2]` | 是 `detourMx`，不是 `detour95`；绕行比本身不能证明“山口唯一通道”，路径另查 A-L2 |
| A-G4 | `buildable≥47600`（共 65536 格） | 是探针以坡度≤16°等条件统计的格点数，不等于完整房屋占地合格数；配置/格网改变须另评基线，不能套旧 10500 |
| A-G5 | `brN≥1`；全窗支脊检出率 100% | `brSlope/brDet` 为信息列，含主脊交汇段，不编造固定坡度合格带 |
| A-G6 | 鞍部上至少一条足宽走廊通过生产 `validate_curve` | 记录端点、控制点、走廊宽度和最大坡度；使用冻结的 `terrainMaxWalkSlope`（当前 30°），不只测一个格点 |

**B：区分规范门槛、存在性和历史实测值。**

| ID | 判定 | 说明 |
| :--- | :--- | :--- |
| B-G1 | 计划命中 `RiverCliff`，并记录 OxbowLake 占位/未命中 | 未命中属于不适合作 B 样板，不是物理生成失败 |
| B-G2 | 实际有 kind=`RiverCliff`、id=1005，绑定 kind=`Cliff`、id=224 的 9 点折线 | 同时核对 kind、ID、feature_ids 和非空有限 bounds，不能只查 id |
| B-G3 | 生产口径连通分量不增加；正式候选的通行分量为 1 | 两种判据的输入/邻域分别记；同 seed 对照只禁用 RiverCliff，保留其他配置，不能用 flat_baseline 冒充未注入基线 |
| B-G4 | 支撑域内有真实 RockFace/NO_WALK/NO_BUILD，记录面积与格数 | v1.52.6 的“≥230 格”是历史样本最小值，**不是新增硬门槛**；按支撑域统计，不能用全图岩格凑数 |
| B-G5 | 崖面最长连续陡带覆盖 ≥0.70×目标长度 | 复用实际格点坡度与 Δ/2 采样，记录采样线、步长、端点累计口径；整域 RockFace 覆盖不等于陡带覆盖，禁止由格数/包围盒推算通过 |
| B-G6 | 双浅滩及半宽+20m 保护带不受崖体侵入 | 记录实际 connections 与完整支撑域（含 taper/回落）的几何关系，不只比较未经旋转的候选中心 y |
| B-G7 | 支撑域 AABB 与所有取水点交互圆无相交 | 复用内核圆-AABB 判据，报告净距；验证记录不能用 `LandscapeMask` 的视觉半径替代 |
| B-G8 | 复核完整支撑域图缘净距（目标 ≥40m） | 不把注入器注释当作全域已验证的证明；如实现/规格不一致，记录缺陷并暂停该候选 |

B 的强制 ≥10 注入样本和局部通过/合法候选 ≥90% 是 06 号 §5.4.D.7 的物理准入口径。本轮保留扫描窗的各阶段计数并核对既有基线；若结果不符，先核配置/工具，再转物理复核，不能在 TA-09 内改概率或放宽标准。本轮不据此宣称重新完成整套离散尺度准入。

**L1 输出**：每 seed 一行的 CSV/JSON，包含指标、判据版本、`plan_hit/local_accepted/global_retained`（后者待 L2 回填）、`verdict` 和 `fail_codes[]`。未知值为 `null/NOT_RUN`，不以 0 或 true 占位；一个 seed 可有多个失败码，拒绝统计同时给唯一 seed 数和逐码次数。

### 5.3 L2 完整世界与布局门

生产 WASM 通过 `world_create(0,764,seed,agentCount,campCount)` 创建完整世界，检查返回码、最终模板、所需几何和存档配置；native 辅助诊断须走同一 bounded 构造+生态准备路径，保留 `creation_diagnostic.attempts/degraded`。该诊断不在 FABS/存档中，不能假设浏览器可直接读取。最终 WASM 身份和事实必须另行复核。

**回退排除**：降级到 flat、去支脊的 A、去掉 RiverCliff 的 B 均不得入选；仅 `requested_profile==effective_profile` 仍不足以排除“同模板但禁掉子特征”。图鉴候选几何与正式世界不一致时，以后者为准并记录原因。

| ID | 检查 | 通过标准/方法 |
| :--- | :--- | :--- |
| A-L1 / B-L1 | 生存和路网 | t0 走生产 `validate_terrain_world` / `diagnose_survival`；记录营地到水、粮、市场结果；不得有 `SpawnDisconnected/SurvivalCostExceeded`。t1 复查道路合法性与实际存活/资源状态 |
| A-L2 | 山口主路 | 指定跨脊起讫营地/资源节点与连续 lane 链，链穿过鞍部；记录路径及 wear。跨硬脊的对照曲线应被拒；不能用最高分位散落几段车道或单一 detour 数代替路径证据 |
| A-L3 | 前景聚落→鞍部 | 主机位中景能读聚落/山口层次；记录房屋/营地 ID、质心与山口坐标，不能人为挪位 |
| A-L4 | 坡脚可建空间 | 选定带内用生产 `validate_footprint` 验证完整房屋占地、坡度和道路接入；14m 只是当前 2×半边长，不是所有树屋都能放下的充分条件，不强求全图所有坡脚连续 |
| A-L5 | 坡脚疏林与露岩 | 检查实际可见装饰，而非所有生成候选；主脊露岩可读，树根接地，无保护区侵入 |
| B-L2 | 授权跨河 | 双 connections/浅滩在场；t0/t1 **所有跨水车道**以生产 corridor 判据检查 crossing_id、宽度、采样点，不能只抽查主干路 |
| B-L3 | 崖滩错开 | 世界空间检查沿用 B-G6；中景专项机位能读两者关系 |
| B-L4 | 崖、路、取水点 | 物理走廊合法；取水操作圆保护；另查屏幕错误压盖。崖 AABB 与道路胶囊只作粗筛，精查用真实支撑域/格网；合法绕崖道路不能因保守 AABB 相交被误判非法 |
| B-L5 | 近岸河阶聚落 | 实际完整房屋占地合法、能接路且位于构图近岸低河阶。记录可建带宽/面积，不引用已删除 `river_valley_settlement_v1` 的 55m 门槛；当前 T2 名义 terrace 宽 65m 也不保证有效可建宽度 |

### 5.4 t1 选择规则与 L3 初评

- **先定推进规则再挑画面**：L2 开始时冻结所有候选共用的检查 Tick 序列与上限，例如 `[14400,28800,57600]`；只是建议起点，须在批量运行前确认。不得边看画面边无限延长某 seed 的演化。
- t1 取该 seed **最早满足**布局条件的检查点：仍有存活族人、目标聚落有合法房屋、指定主路链在普通视图可辨。路链选取、wear 可见门槛与房屋集合判据在扫描前写入方法；上限内不满足记 `layout_not_mature`。
- 每个样板的 t1 可以不同，但**该样板全部矩阵固定同一 t1**；记录实际 tick、人口、房屋数、lane 数和主路 ID。高密度另按 §7.6 取真实检查点，不把任意 t1 冒充高密。
- 每场景目标短名单 4～6 个，t0/t1 各看远/中/近；mapOnly 只作补充扫图。评审按 §1.1 与 §7 留入选/淘汰原因，最终选 **1 主+1 备/场景**。
- 到最大扫描窗仍不足，交付部分结果与阻塞原因，不靠强制建房、改物理配置或关闭保护区凑数。

### 5.5 临时工具与可复现性

临时 Node/浏览器脚本及原始日志放仓库已忽略的 `reviews/ta09-<date>/`，不写工作区之外；Rust 临时诊断扩展如必须位于 crate 内，验证后只还原本任务改动。既有 `terrain_probe.rs` 不删除。

交付前删除临时测试/注入钩子，但**先**归档命令、工具版本、参数、算法/统计定义、必要的诊断补丁说明和结果，使别人可重建相同步骤；不能只剩截图而丢失测量方法。证据保留位置与可下载要求见 §9.4。

---

## 6. 详细设计（二）：样板冻结与复现

### 6.1 profile/seed/精确 Tick 操作契约

**浏览器没有 `?profile=` 参数**，`?seed=<N>` 默认仍走 `SIM_CONFIG.terrainProfile='random'`。正式世界冻结应按如下顺序，不改线上默认配置文件：

1. 独立浏览器配置目录/会话，隔离旧 localStorage/自动续档，不覆盖用户既有存档。等 `sim._ready`，显式暂停；使用 `saveWorld()` 返回的内核 Tick 作暂停后的核对点，不能只信 UI 按钮。
2. 将冻结配置（已合并决策/升级矩阵）应用到主线程配置，设 `SIM_CONFIG.terrainProfile` 为 A/B 模板，并固定再生倍率与人数；调用 `sim.initEcology(agentCount, seed)` 让 RESET **在创世前**传配置。仅 `applyConfig()` 不会把既有世界重生为另一模板。
3. 等本次 `RESET_DONE`，回读 `sim.terrain.profile/generatorVersion`、实际 seed、t0=0 及所需特征，导出 t0；此时尚未开始任何视觉季节覆写。**发现 Tick 已推进则暂停后重置，不把非零 Tick 标作 t0。**
4. 精确推进优先复用 Worker 已有 `STEP` 消息，暂停态串行发送有界 count，等待对应目标 Tick 快照后再发送下一批；或用同一 release WASM 的 `world_tick_steps(N,1/60)` 产生存档再载入。都须核对内核存档 `tick_counter`，不能用“等若干秒+点暂停”保证 t1。
5. 冻结后禁止再下发 CONFIG/SET_REGEN；LOAD 后按存档自带配置续演，不再把当前默认 `SIM_CONFIG` 热注入覆盖它。

**已知入口缺陷（不是本次已修复项）**：`sim_worker.js` 的 INIT/RESET 使用 `msg.seed || Date.now()`，会把合法 **seed=0** 替换为墙钟值。L1 仍统计 0；修复前，0 不能通过这两条浏览器入口冻结为可复现主/备样板。若确需选 0，先另立入口缺陷、完成修复与回归后重新冻结；每次均比较请求 seed、READY/RESET_DONE seed 与存档 seed，禁止悄悄改成 1。

### 6.2 每个主/备样板的冻结物

| 项 | 要求 |
| :--- | :--- |
| 身份 | 场景、primary/backup、seed、请求/最终 profile、生成器版本、子特征/支脊确认来源、创世回退记录 |
| t0/t1 | 精确 Tick、存档文件、完整 SHA256、字节数、人口/房屋/lane 数；每样板 2 份，共 8 份 |
| 配置 | 完整模拟配置、再生倍率、实际渲染/光照配置、相关偏好及 SHA256；不能仅留“使用默认值” |
| 相机 | 主构图机位 + 各景别目标点 `(x,y,z)`、`rotZ/rotX/zoom/panX/panY`、屏幕钉点、关键对象 ID；z 来自真实地表/模型而非一律 0 |
| 环境/矩阵 | `baseline_id`、源码/补丁/WASM 哈希、浏览器/GPU、视口/DPR、`matrix_version`、视觉夹具状态 |
| 文件链路 | Chrome/Edge 真实 File System Access 导出、重新导入并续演到验证 Tick；`saveWorld/loadWorld` 程序化调用只能证明引擎链，不等于文件选择器和权限链已验证 |

引擎导出的 JSON 可以作为制作夹具的来源，但每份交付存档须在真实文件链路中实际恢复。受限环境可先做 `?nogate=1` 的非存档检查，文件链记 NOT_RUN；不宣称全量验收。`v1.52.x` 不是足够的存档身份：生成器版本也必须匹配，不能用生成器 13 的旧档替代 14。

### 6.3 换备选、修补和重采

- **换备选**仅用于合法世界不符合构图/发展条件。物理违例或系统性渲染问题必须保留为缺陷，不能通过选一个不触发它的 seed 报“已解决”。备选启用后采该场景完整 96+12 和专项。
- 代码/模拟配置/生成器/WASM 改变：冻结新基线，重跑受影响 L1/L2；不得混用旧存档和新事实。全局表现代码或浏览器/GPU/DPR 改变：两个主选全矩阵重采。
- 经证明仅某 profile 的局部表现改动：该场景重采全矩阵；另场景做定点回归并记录豁免全矩阵的依据。只有相机锚点变更则该场景重采；文字说明/证据编排调整无需重采。
- 原始 A/B 和失败轮次保留。最终报告逐项引用基线，不把修前图和修后指标拼成同一次 PASS。

---

## 7. 详细设计（三）：验收清单

### 7.1 通用视觉与交互

| ID | 检查 | 通过标准 |
| :--- | :--- | :--- |
| G-1 | 5 秒可读性 | 在约定主构图普通远景（非热力图/调试标注），非执行人约 5 秒内指出主要聚落、水源区、主路；记录 3 项结果，不要求每张局部近景都覆盖这三者 |
| G-2 | 接地/裁切 | 近景及高低俯角检查树、屋、人、石，无悬空、入土半截、树冠错误裁断；保留对象 ID 与位置 |
| G-3 | 遮挡 | 自然重叠样板按 31 号 §8.5 检查模型内穿插、树后人物轮廓/选中高亮、信息卡可读、崖不错误压盖近路/取水点；“卡片能读”不代替其余项目 |
| G-4 | 路与房屋 | 中近景及旋转过程中无道路切屋顶/错误穿透，不能以最终一帧正常代替整个过程 |
| G-5 | 保护区 | 以实际可见基础装饰与景观子图元、各自 footprint 检查车道/房屋/POI 留白；`LandscapeMask` 只负责视觉遮罩，不是物理道路合法性证明 |
| G-6 | 季相 | 同株春芽/夏冠/秋色/冬枝可辨；落叶树冬叶量约 0～5%，常绿保轮廓；地表 TA-13 限制另列 |
| G-7 | 世界受光 | 固定季相转光向、固定世界光转相机；指定树干/树冠/岩面 ROI 的亮部与阴影协调，不黏屏幕；只看截图哈希不同不足以通过 |
| G-8 | 连续相机稳定性 | 四方位之外补连续 360° 旋转、远近往返和视口边缘扫掠，无纹理漂移、闪烁、漏画；过程录像/采样帧附时间索引 |
| G-9 | 拾取/跟随 | 四方位和代表高低俯角下，点击指定 agent/house/poi 返回正确 ID；开关跟随、取消选中后镜头状态正确，无遮挡导致的点击错位 |

矩阵是覆盖清单而非自动通过证明。某项未发生（如样板没有树屋人重叠）应补机位/真实检查点，或记 NOT_RUN，不填“零违例”。

### 7.2 A 专属构图

| ID | 检查 | 通过标准 |
| :--- | :--- | :--- |
| A-V1 | 主脊/支脊/鞍部 | 约定中景主机位一屏可辨主脊走向、至少一条支脊和低鞍部 |
| A-V2 | 聚落→山口 | t1 有连续、真实可达且普通视图可读的主路，与 A-L2 指定路链一致 |
| A-V3 | 坡脚疏林 | 植被成组并有留白，不侵入主路/房屋操作区 |
| A-V4 | 山体露岩 | 主脊/支脊陡带有可读岩面，不呈全山均匀刷绿 |
| A-V5 | 山口近景 | 路面连续、接地正确、无装饰错误侵入或屏幕压盖 |

### 7.3 B 专属构图

| ID | 检查 | 通过标准 |
| :--- | :--- | :--- |
| B-V1 | 远岸岩壁地标 | 主机位远景崖体可辨，岩面/崖缘有层次并符合已接受的层岩方向；是否需要进一步美术工作由评审判断，不预先把“层岩”降成一条描线 |
| B-V2 | 河流引导 | 河面连续，河—崖层次明确，无错误压盖与岸线漂浮 |
| B-V3 | 近岸聚落 | 合法房屋落在低河阶、接地且完整占地不触水，聚落带可读 |
| B-V4 | 崖滩错开 | 专项中景同屏展示合法浅滩与崖体的错开关系；锚点可不同于主构图机位 |
| B-V5 | 崖路/取水点 | 世界空间保护成立，近景也不错误遮住近路/操作区；自然的远处遮挡与错误压盖须区分 |
| B-V6 | 合法跨河 | 全量跨水车道数值核对通过，选定实际跨河行程只经授权浅滩 |

### 7.4 推进与真实季节

| ID | 内容 | 证据 |
| :--- | :--- | :--- |
| P-1 | t0 静态地貌 | t0/t1 的地形/特征指纹不变；不能把按世界变化的装饰可见性（新房遮罩等）要求为恒定 |
| P-2 | t1 自主发展 | 房屋/路网自然演化；记录主体 ID、wear、接地与留白。t0/t1 对照须固定相机和视觉季相，否则不能归因于发展 |
| P-3 | 真实四季 | 关闭全部季相覆写，从冻结世界按真实 dt 推进至少一个年周期，记录实际四季及跨季边界帧；此专项不是同 Tick 季节夹具的替代品，反之亦然 |
| P-4 | 资源断流反馈 | 自然出现时记录库存及可采反馈；未发生可记 N/A（本任务不强制耗尽资源），不能据此勾完 07 号所有资源反馈任务 |

### 7.5 生命周期与确定性：分三层证明

1. **内核层**：同 release WASM、完整配置、seed、指令序列与目标 Tick，两次冷启动、存读后续演、回溯后重演结果一致。使用 `tools/snapshot-reader.js` 的全量解码稳定串/事实指纹；每次 world_create/world_load 后 `resetCaches()`，强制地形/路网全量帧。**不直接比较增量 FABS 原始字节**（字符串表与脏 section 随读取历史变化），不以截图 SHA 替代内核验证。
2. **派生/生命周期层**：A→B→A、t0/t1 LOAD、REWIND 回滚再推进，与独立冷重建比较静态数据及 `AccentModel/LandscapeModel` 几何摘要，核对旧 features/accents/subFeatures、遮罩、标签和字符串无残留。`STR_TAB.start_index==0` 仍是驻留表重置判据。LOAD 会重置 Worker 检查点，只能回溯到现有历史范围；要验 t0→t1 回溯，必须先载 t0 并在同一会话推进建立历史，不能载 t1 后假设已有 t0 检查点。
3. **像素层**：固定同机硬件/浏览器、机位、主题、光向、季相、选择状态和视觉时刻。游鱼由 `performance.now()` 驱动，暂停并不冻结全部像素；诊断可临时固定视觉时间或屏蔽动态 ROI（必须记录且原图保留），再做容差/差异图。跨 GPU/浏览器不承诺逐字节一致；截图哈希用于文件完整性。

暂停验收应在**取消季节钩子**后证明 Tick/季相不再推进（光相允许先收敛），再恢复推进；不能用被锁住的 getter“证明暂停成功”。文件选择器/权限流程按 §6.2 单独记结果。

### 7.6 性能口径

- 固定设备、浏览器/GPU、分辨率/DPR、FPS 设置、人口/房屋/lane/装饰数、倍速及配置；预热后固定镜头和规定拖动/跟随轨迹**各采样≥60s**。另记首帧和 LOAD/RESET 缓存重建耗时。
- 采**逐帧原始渲染+UI耗时**求 p50/p95，不能从 HUD 的 EMA 平滑值反推 p95；rAF 间隔、CPU 提交时间与可得的 GPU 时间分列。`tools/profile-benchmark.js` 用于内核吞吐，不冒充浏览器渲染基准。
- 预算沿用 07 号 §11.3：参考主线程 p95≤16ms；有绘制修补时同条件 `p95(after)-p95(before)≤3ms`，吞吐下降≤5%，新增视觉缓存建议≤64MiB；GPU 资源若无法准确统计要列估算方法/缺口，不能用总 JS heap 充当缓存字节数。
- 零代码交付仍记录场景性能基线，增量比较记 N/A；有修补时两端恢复同一存档，至少 3 轮同条件 A/B，不减 Tick、不降低人口/图层掩盖回退。
- 高密场景须给实际人口/房屋/车道/可见图元数及固定存档；优先主选更晚的真实检查点，若没有则补已有高密基准或记缺口，不把 t1 自动命名为高密。
- 历史 TA-04-8 超标豁免**不自动转用于 TA-09 的新回退**。超标保留 FAIL，优化复测或由用户对本项明确作出取舍后记“接受偏差”，不得执行人自行放行。

---

## 8. 有限视觉修补与缺陷分流

### 8.1 触发规则

先判定为：① 合法候选不符合构图→记录淘汰，可换备选；② 既有表现入口的小范围缺陷→本节修补；③ 物理/入口/存档/跨管线系统性缺陷→独立任务，受影响验收 BLOCKED。重要失败立即处理，不等全矩阵拍完再发现方向不可行。

### 8.2 可选修补项（不是全部必做）

| ID | 可做 | 实际入口与限制 |
| :--- | :--- | :--- |
| F1 崖缘可读性 | 调既有 Cliff 色相/线宽/透明度，确认受光语义 | `render_terrain.js::drawFeatureItem` 既有分支；不在 2D 新增层岩/贴面/遮挡算法。真正层岩几何需求或跨 Canvas 错序交后续任务 |
| F2 岩面色基座 | 修确实存在的反照率/受光错误 | `math.js::computeTerrainAlbedo`、`lighting.js`、GL shader 共用口径；当前无独立 RockFace 色段，不能假设改不存在的函数即可。若扩大 `TerrainStyle` 到 T1/T2，须先解决 LOAD/REWIND deactivate 导致丢风格的生命周期问题，不作为免验证捷径 |
| F3 主路可读性 | 调自然模式既有线样式 | `render_world.js::cacheLaneStyle`；如抽出渲染参数，字段与真实消费点同步，不改 wear/路网，不新增发光或遮挡机制 |
| F4 遮罩消费纠错 | 修既有 `LandscapeMask` 同步/可见性消费漏项 | mask 当前索引车道、房屋、POI，不原生包含 Cliff/鞍部；新保护源不是“零成本补漏”。内核装饰落点过滤另立任务，不修改 `accent_rng`/生成事实 |
| F5 标签参数 | 调现有聚合/显隐参数 | `config.render.js` 的已消费键；评审普通模式可读性与拾取，不顺手重做布局算法 |

每项记录改动/理由、前后同机位图、基线/参数差分、影响面、性能与回归。每项独立可回退；若共享几何/模型参数改变，遵守 `accentModelStyleVersion` 等缓存版本契约。

### 8.3 修补退出边界

不得触碰物理阈值、生成器、生态、快照/FABS/存档结构或 SimConfig；不得做 S4-X3、TA-13、TC-03。超出有限修补预算或已接受构图仍无法满足时，交付失败样板及后续任务建议，不强行以“已知局限”收口。

---

## 9. 取证矩阵与证据包

### 9.1 主矩阵与视觉夹具

每主选 **四季×四方位×两俯角×三缩放=96 张**；两主选 192 张。另各采 t0 的四季×三景别（主机位方位/标准俯角）12 张，共 216 张基础帧。

| 维度 | 值/契约 |
| :--- | :--- |
| 季节 | `currentSeason=Spring/Summer/Autumn/Winter`，**`seasonProgress=0.5`**；原始年相位 `u=wrap((seasonIndex-0.5+progress)/4)`，对应 0/0.25/0.5/0.75，不能把 u 直接当 progress |
| 光照 | 每次季相/光向修改后 `SimLighting.resync()` + `markDirty()`；回读 phase/lightDir/lightRev，并等待 GL 与阴影后续帧完成，不能只改主线程字段立即截图 |
| 方位 | 表中角度 0/90/180/270°，写入 `camera.rotZ` 须转**弧度** |
| 俯角 | `camera.rotX=1.05/0.55` 弧度；补充 1.30 陡俯角接地/裁切检查，不计入 96 |
| 缩放 | 候选 1.0/2.5/6.0；L3 先核对场景尺度与 viewport，必要时在全矩阵前一次性校准。固定每场景三档，不逐张调成好看；远中近景不是 LOD far/mid/near 档位的等义词 |
| 锚点 | 每景别预设世界目标 `(x,y,z)`，可另有崖滩/取水专项锚点；用真实投影反算 pan，关闭相机跟随/悬浮/自动飞往干扰 |
| 视口 | 建议 1280×720 CSS px、DPR=1；实际值在 L3 固定。合成截图应包含 GL+2D+必要 DOM，不能只导出单个 Canvas |
| 夹具边界 | 只覆写主线程渲染读数，不回传 Worker、不写入交付存档。temperature 也影响光色，保持该 t1 的真实温度并记录为受控季相夹具；真实四季另验 P-3 |

**逐张采集事务**：恢复样板/确认 Tick → 应用机位与视觉夹具 → 等地形/装饰/阴影就绪、光相对齐且至少两个完整渲染帧稳定 → 回读 → 合成截图 → 再回读。保留 `baseline_id/scene/seed/profile/tick/season/progress/temperature/u/lightDir/lightRev/zoom/rotX/rotZ/pan/target/viewport/DPR/backend`，设有限超时；不符或无图则作废并记录重拍原因。

数值回读容差和光相稳定判据在冒烟中固定（建议机位/相位误差≤1e-6、钉点误差≤1 CSS px），不要以“等 1 秒”代替就绪检查。相机旋转和光相修改可能跨帧上传，不能以初次注入成功代表画面已应用。

### 9.2 专项与数量

| 专项 | 采集要求 |
| :--- | :--- |
| 受光分离 | 每场景固定光转相机 4 张、固定季相转光 4 张，合计 16；选同一对象/ROI，验证法线与投影，不用“md5 全不同”充当正确性 |
| t0/t1 | 固定视觉季相与相机的远/中/近对照；能复用基础 216 张的用 frame_id 引用，不重复计数 |
| 近景 | A 山口/坡脚，B 崖滩/崖路/取水点；至少覆盖 G-2/G-3/G-5，额外机位写明用途与对象 |
| 真实推进与生命周期 | P-3、暂停、文件 LOAD、REWIND、A→B→A，各保留步骤、指纹与对应帧，不能预设“6 张”就代表覆盖完成 |
| 连续相机与交互 | G-8/G-9 的短录像或时间采样序列；主矩阵四张方位图不能证明连续过程中不闪烁 |
| 拼图 | 每场景 4 季×2 俯角=8 张；**总计 16 张**，每张 3 列缩放×4 行方位=12 格，完整覆盖 192 个主 frame_id |

### 9.3 像素指标与评审记录

可复用 `meanRGB/greenRatio/brownRatio/brightRatio/最亮10%质心` 作辅助，明确定义阈值、亮度公式、像素裁剪/ROI、动态掩膜及归一化方式。固定原分辨率计算，拼图/缩图不作测量输入；UI/天空不能任意混进植被或崖体指标。

`greenRatio` 不是落叶率，亮部质心会随构图变化；`rockRatio` 如采用须在正式矩阵前校准阈值，只作岩面 ROI 的辅助量，不作为通过门。季相需数值叶量+同株画面，崖体可读性需独立人工判断。评审行至少包含 `check_id/status/frame_ids/对象ID/原因/复核人/日期`。

### 9.4 归档与可恢复性

```text
docs/plan/tech/assets/ta09-evidence-YYYY-MM-DD/
├── report.md                 # 结论、环境、逐项状态、偏差/阻塞、复现步骤
├── manifest.json             # 文件/外部归档 URI、完整 SHA256、字节数、frame_id 索引
├── metrics/
│   ├── matrix.json           # 基础 216 张 + 专项读回/指标/作废记录
│   ├── seed-screening.json   # 全扫描窗各阶段结果、拒绝分布与方法
│   ├── fixture.json          # 2 主+2 备、精确 Tick、锚点、存档身份
│   ├── review.json           # §7 逐项结论
│   └── performance.json      # 原始样本/聚合、A/B 条件或 N/A 理由
├── config/                   # 完整冻结配置、渲染/光照配置及偏好
├── visual/
│   ├── sheets/               # 主矩阵 16 张，标注全部 frame_id
│   ├── matrix/               # 每主选 6～12 张代表原图
│   └── special/              # 构图、推进、受光、生命周期与交互关键帧
└── saves/                    # 小型存档可直接入库；大文件见下方归档要求
```

- 交付前先统计大小，按现有约定把批量原图/录像/大存档留在忽略目录并转入项目认可的持久归档/附件；不把大型数据集直接加入 Git，不擅自增加 LFS 或新存储服务。
- **全部 216 张及专项原图、8 份存档必须可恢复**：可以压缩、外部保存，但 manifest 必须给实际可下载位置、完整 SHA256/大小及恢复命令，并在干净目录验证一次下载/解包/读档。仅写本机临时路径或“哈希+导出说明”不算交付。
- manifest 不给自身做循环哈希；总归档的哈希可写在外部交付记录。失败/作废轮次单独标识，不能覆盖唯一原证据。
- 若暂时没有可持续访问的存储位置，报告“归档待完成”，不能以工作区文件未来可能保留为由勾选。

---

## 10. 任务分解（WBS）

| ID | 任务 | 退出条件/产物 | 人日 |
| :--- | :--- | :--- | :--- |
| TA-09-0 | 环境、入口、遮挡冒烟 | §4.1 完成；关键阻塞早于全矩阵发现 | 0.5 |
| TA-09-1 | L1 扫描 | 正确配置/探针门槛；全窗数据和拒绝原因，B 接受样本数达标或明确阻塞 | 1～1.5 |
| TA-09-2 | L2 世界/推进筛选 | bounded 创世、最终身份、固定检查 Tick 与布局量测；短名单 | 1～1.5 |
| TA-09-3 | L3 初评与冻结 | 每场景 1 主+1 备、8 存档、机位/配置/文件恢复记录 | 0.5～1 |
| TA-09-4 | 矩阵与专项 | 216 基础帧及专项、逐张回读、16 拼图、指标 | 1.5～2 |
| TA-09-5 | 独立评审/有限修补 | §7 结论明确；失败转处置，不以“已开缺陷”当通过 | 0.5；修补另加 0～2 |
| TA-09-6 | 生命周期/性能 | 与采集复用素材但独立结论；真实 LOAD/回溯/确定性/性能结果 | 0.5～1 |
| TA-09-7 | 归档与文档 | 干净环境可恢复证据，§11/§13 达标才勾选 | 0.5～1 |

**合计**：无修补 6～9 人日；含有限修补 6～11 人日。矩阵重采、超窗筛选、外部内核/迁移缺陷不能无限塞进“中等工作量”；发生时先报告影响并调整排期。

---

## 11. 门禁、状态与勾选条件

### 11.1 状态定义与门禁总表

每个检查只能明确记为 `PASS / FAIL / NOT_RUN / BLOCKED / N/A`；用户明确接受的非硬约束偏差另记 `ACCEPTED_DEVIATION`，附决定与影响，不改写为 PASS。物理非法、错种子/错配置、伪造或不可恢复证据不可按美术偏好豁免。

| 类别 | 必须满足 |
| :--- | :--- |
| 样板身份/物理 | 主备最终 profile/几何/存档正确；L1/L2 适用门通过，扫描窗失败如实保留；物理门禁仍独立于视觉样板 |
| 视觉/交互 | §7.1～7.3 全部适用项有证据与结论；范围外限制预先列明，不能把非 P0 失败一概归为已知局限 |
| 推进/生命周期 | P-1～P-3、§7.5 完成；P-4 未发生允许 N/A 并说明；真实文件链与程序化链分开 |
| 性能 | §7.6 实测；新增回退修复或有本任务的明确取舍，不套历史豁免 |
| 工程 | 按 §13 的变更类型走相应门禁；无代码只走文档检查 |
| 证据/文档 | 原图/存档可恢复、manifest 完整、权威台账与报告一致 |

P0 用于**分诊顺序**：世界/存档错误、物理违例、G-1～G-5/G-9、A-V1/A-V2、B-V1/B-V4/B-V5/B-V6 的阻断优先处理。其余适用项也不是自动豁免。系统性遮挡问题不能靠换 seed 降为非 P0。

### 11.2 完成与部分交付

- **可标 ✅**：两组真实样板与所需备选资料交付、适用门禁通过、证据可恢复，用户对构图/可读性独立验收完成；如有允许的偏差，已明确获用户接受并同步权威记录。
- **只能标 ◐/阻塞**：有必验 NOT_RUN/FAIL/BLOCKED、缺真实样板/存档/归档或缺用户评审。已提交修补、已登记缺陷、构图草案通过，都不等于该项完成。
- Chrome 文件链受环境限制时继续允许做非存档工作，但全量验收仍待补测；若用户决定另行缩减验收范围，明确记录决定，不把测试缺口写成测试通过。

### 11.3 评审角色

执行人负责扫描、采集和技术初评；非执行人（用户）独立判断 G-1 和 §1.1 构图对齐。5 秒可读性图不预先画箭头/标答案；评审结果、日期、图号必须留档。无人独立评审时记待评，不由执行人代签。

---

## 12. 风险与止损

| 风险 | 处理 |
| :--- | :--- |
| 混合管线达不到 31 号遮挡结果 | TA-09-0 提前发现；转 TC-03/缺陷，L4 阻塞，不在 2D 侧补遮挡 |
| seed/profile 被 random、seed=0 或创世降级替换 | 请求/Worker/存档/最终几何交叉核对；错误入口先修或不用该候选，记录真实原因 |
| B 命中但局部/全局接受不足 | 分阶段计数，按序扩至 255；无足量样本转物理复核，禁止改概率/刷配置凑数 |
| 房屋/道路在上限内未形成目标构图 | 使用预先规定的 Tick 序列和备选；缺候选就报告范围/工期影响，不无限等待 |
| Cliff 表现不足，需要真正层岩或专属景观 | F1/F2 有限修补先评；超界转后续任务，不悄悄降低已接受构图标准 |
| 季节/光照异步未稳定、暂停仍有动态像素 | 回读、等后续完整帧；区分真实快照与视觉夹具；记录动态时钟/ROI，不以截图 SHA 判内核失败 |
| 真实文件存读不可执行 | 开工即检查权限/浏览器环境；NOT_RUN 如实保留，不能 `nogate` 冒充 |
| 原始截图/大存档只存在临时工作区 | 交付前验证持久归档可下载、完整哈希和干净恢复；不满足则归档门不通过 |
| 全局修补导致证据混版/工期超出 | 新建 baseline，按影响面重采；保留失败轮次，独立报告外部阻塞 |

---

## 13. 版本、工程门禁与文档同步

### 13.1 版本判定

- **仅计划/筛选/证据**：不升版、不重编译 WASM，不新增 changelog **版本条目**；收口结论写任务权威记录及 report。
- **纯表现修补**：使用 `node tools/bump-version.js --patch`，核对不改变内核行为/持久化契约；不因“前端文件”就自动认定 patch（前端 SimConfig 的数值变化同样影响模拟）。
- **物理/模拟/生成器/兼容性缺陷**：不属于 §8，另立任务，按根 AGENTS.md §4.9 评估 minor；改物理生成须同步生成器版本；仅存档结构不兼容才改 SAVE_FORMAT_VERSION。Rust 变更使用 release 构建并同步双副本。历史版本的升法不是本次豁免依据。

### 13.2 检查清单

**所有文档/证据交付**（含本次计划修订）：

```bash
git status --short
git diff --check
node tools/doc-maintenance-check.js
node tools/doc-link-check.js
node tools/cross-doc-check.js
node tools/bump-version.js --check
```

前端修补追加 `node tools/frontend-check.js`、实际浏览器视觉/交互/文件链验证；共享配置或协议发生影响时跑相应 `config-check.js` / `snapshot-check.js`。新增渲染键不并入 SimConfig。

外部 Rust 缺陷完成后，按 [工作流 §B](docs/current/tech/30-workflow.md) 做 `cargo check --lib`（可选）、release WASM 构建与双副本同步，跑 `test-wasm.js`、`test-determinism.js` 及配置/快照专项；再回到 TA-09 重建基线。此处不照抄 06 号 R.5 的历史命令，以工作流 §B 当前门禁为准。

静态门禁只查链接/指纹/语法等有限内容，不能证明本文的全部阈值、截图覆盖与实机结果；额外人工核对矩阵 ID 唯一性、缺帧、哈希、存档恢复和清单状态。

### 13.3 文档同步时机

| 文件 | 本次计划审查/开工时 | 真正收口时 |
| :--- | :--- | :--- |
| `docs/plan/tech/07-terrain-art.md` | 修正 RiverCliff 旧阻塞事实、注明混合管线与遮挡仍待验；保留 TA-09 未完成 | §4.4.1 链接冻结样板/报告；更新台账/依赖图，只勾本次实际覆盖的验收项 |
| `docs/plan/tech/06-terrain-templates.md` | 同步已交付注入器与样板仍待验的事实，不把阶段一整体标完成 | 对应 TA-09/D-B1-8 入口链接交付记录；不把其他子特征/物理门禁一并勾完 |
| `TODO.md` | 前置已落地，实机尚未交付，保持 `[ ]` | 仅满足 §11 后勾 D-B1-8，链接证据 |
| `docs/current/01-changelog.md` | 本次纯方案修订不新增版本条目 | 有代码/版本变化才记对应行为；纯取证依工作流 §G 不造新版本 |
| 对应 current 技术文档 / `frontend/AGENTS.md` | 不把拟议修补写成现状 | 仅实际机制变化才同步；新增归档目录按代码地图现有约定登记 |
| 本方案 | 标记“源码对照修订”，不代替用户验收 | 保留或归档须先确认报告已承接执行契约；如删除先清理所有入链，不能自动删除仍被引用的计划 |

---

## 附录 A. 消费/可能改动文件

| 文件 | 用途/边界 |
| :--- | :--- |
| `geo/terrain.rs`、`hydrology.rs`、`geometry_transaction.rs` | 读取生成、注入、事务事实；不在 TA-09 修改生产行为 |
| `spatial/creation_fallback.rs`、`terrain_network.rs`、`survival_diagnosis.rs` | L2 bounded 创世、道路与生存诊断 |
| `examples/terrain_probe.rs` / `config.json` | L1 主工具与实际配置；临时诊断用后还原 |
| `frontend/js/rustworld.js` / `sim_worker.js` | 配置/RESET/STEP/SAVE/LOAD/REWIND 入口核对；入口缺陷另立任务 |
| `frontend/js/render_terrain.js` / `math.js` / `lighting.js` / `webgl/layers/terrain/terrain-renderer.js` | F1/F2 的既有表现入口 |
| `frontend/js/render_world.js` / `landscape-mask.js` / `config.render.js` | F3/F4/F5 的既有消费点，不新增物理语义 |
| `frontend/js/ta06-acceptance.js` / `accent-season.js` | 视觉夹具方法与年相位公式参考，不新增常驻面板 |
| `tools/snapshot-reader.js` | 全量解码/稳定指纹与缓存隔离，不自行实现另一套 FABS 解释器 |

## 附录 B. 矩阵索引约定

`frame_id = scene + tick_state + season + azimuth_deg + pitch_rad + zoom + target_id + matrix_version`，例如文件名结构 `A_t1_Spring_az090_rx1.05_z2.5_main_m1.png`（示意，不代表已有截图）。基础期望集合固定生成：每主选 t1 96、t0 12；专项另标 `specialty`，复用图用相同 frame_id 引用，不能重复计为新覆盖。

每张完整参数以 §9.1 和 `fixture.json` 为准；拼图还须保存行列→frame_id 映射。改变实参先升 `matrix_version`，禁止维持旧标签却更换 zoom/目标点。

## 附录 C. 种子记录模板（未测值不得预填通过）

```json
{
  "schema_version": 1,
  "scene": "B",
  "seed": null,
  "requested_profile": "river_valley_v1",
  "effective_profile": null,
  "baseline_id": null,
  "generator_version": 14,
  "config_sha256": null,
  "L1": {
    "plan_hit": null,
    "local_accepted": null,
    "rockface_support_cells": null,
    "band_ratio": null,
    "components_method": null,
    "components": null,
    "ford_clearance_m": null,
    "access_clearance_m": null,
    "edge_clearance_m": null,
    "verdict": "NOT_RUN",
    "fail_codes": []
  },
  "L2": {
    "global_retained": null,
    "creation_diagnostic_ref": null,
    "t0_tick": null,
    "t1_tick": null,
    "survival_verdict": "NOT_RUN",
    "all_crossing_lanes_checked": null,
    "save_refs": [],
    "verdict": "NOT_RUN",
    "fail_codes": []
  }
}
```

## 附录 D. 初评模板

| 场景 | seed/profile | t0/t1 | L1/L2 结论 | 主/备/淘汰 | 原因/缺陷号 | 构图对齐项 | 图号/评审人 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 待填写 | 待实测 | 待实测 | NOT_RUN | 未选定 | — | — | — |

## 附录 E. 入口速查

- 探针命令见 §5.1；正式浏览器精确 profile/seed/暂停/RESET 流程见 §6.1，不存在可直接替代该流程的 `?profile=` URL。
- 正式世界非存档观察：`/?seed=<N>&nogate=1`；图鉴：`/?seed=<N>&mapOnly=1&nogate=1`；图鉴 profile 必须在 INIT 前注入配置或用预先生成的同配置正式世界观察，不能调用会创建完整生态的 RESET 假装仍是 mapOnly。
- `&accentgl=0` 只关装饰 GL，不是全 Canvas；`&webgl=0` 才是地形回退入口。仅排错使用，截图标明后端，不混入主矩阵。
- `&ta06=1` 可看植被观察台方法；正式截图关闭调试标注，恢复临时季相/光向钩子后再做真实生命周期验证。
- 服务地址取实际开发服务器输出；远程预览使用平台分配的同源 HTTPS 入口，不在用户浏览器写 sandbox 的 localhost。文件验收应在支持权限/用户手势的顶层 Chrome/Edge 窗口完成。

## 附录 F. 参考

- [07 号：TA-09 台账、构图权威、验收与性能](docs/plan/tech/07-terrain-art.md)
- [06 号：RiverCliff §5.4.D、创世/回退、物理验收](docs/plan/tech/06-terrain-templates.md)
- [31 号：全量 WebGL 迁移与 §8.5 遮挡矩阵](docs/plan/tech/31-canvas-to-webgl-migration.md)
- [14 号：当前地形/路网机制](docs/current/tech/14-terrain-and-network.md) · [16 号：前端现状](docs/current/tech/16-frontend-overview.md)
- [TA-06 证据方法与已知限制](docs/plan/tech/assets/ta06-evidence-2026-09-22/report.md) · [09 号：已关闭的植被验证任务](docs/plan/tech/09-vegetation-verification.md)
- [工作流与纯文档例外](docs/current/tech/30-workflow.md) · [浏览器指南](docs/current/tech/27-browser-automation.md)
- [TODO](TODO.md) · [S4-X 专属景观边界](STAGE-04-SUBFEATURE-LANDSCAPES-TODO.md)
