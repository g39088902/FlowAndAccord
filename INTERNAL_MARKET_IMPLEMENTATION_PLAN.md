# 内部市场：技术实现方案与任务序列

> 状态：实施规划，未实施、未排期。2026-09-14 对照当前源码复核。
> 产品规则权威：[03 内部市场](docs/plan/tech/03-internal-market.md)。本文按用户要求导出到根目录，维护技术规格和实施顺序，不另存重复技术稿。
> 本次仅修订文档；所有新增类型、接口、参数、目录与任务均为建议实现。任务编号表示依赖顺序，不代表已经完成。

## 1. 产品审查与已修复问题

| 原稿问题 | 后果 | 修复决定 |
|---|---|---|
| 两张卖单即可换出黄金再交易 | 无真实买方与货币来源，无法冷启动 | 示例明确真实持金买方；全社会无金可零成交，首期不做信用/易货 |
| 所有交易须已有对手盘才能出发 | 空簿无人下第一单 | 普通任务可先挂有期限的担保单；危机仍要求可执行深度 |
| 未定义首次价格、有效报价与改价 | 只有 last_price 时空簿报价无来源 | 配置初始参考价，预算限价，过期重报；无成交不是价格为零 |
| 系统按天赋扫描派售货员并脱离家户 | 旁路马斯洛决策，破坏婚姻、家户和继承 | 现有挣金意图下自主兼职；只裁决申请，不强制迁籍 |
| 纯佣金全职维生、自营免费本金、利润 60/40 | 收支来源不闭合，早期值守可能饿死 | 首期有限时长试岗/兼职，保留正常生存；自营和分红后置 |
| 佣金“含买卖两腿”、卖方抽佣、买方加佣并存 | 重复收费，报价与实际不同 | 每次成交只卖方付费；买方支出=卖方净金+佣金 |
| 均价成交却按卖一报价，“付款不退” | 成本预测错，买单差额被吞 | 使用较早订单限价；多档重放同算法，释放限价差额 |
| 目标资源要求买单、成交量不减 filled | 买卖方向错误，部分成交可能超售 | 买目标查卖单，卖货查买单；按双方剩余量结算 |
| 远程扣家户物资、离场撤单直接退款回家 | 货物瞬移、重复计算现货 | 到场实物托管，订单只冻结；成交与撤单结果留市集待领 |
| 未定义取货容量、休市撤单、关闭残余资产 | 无限堆积或永久锁住遗产 | 有限承诺容量，休市仍可撤单/领取，未清空不删场地 |
| 濒危可无限等待，成交量被当作到货保证 | 低价方案把人拖死 | 当前深度与排队预算准入，立即成交限价及截止时刻 |
| 抢占撤销部分成交、同户/售货员对敲 | 复制货物、洗佣金和好感 | 仅撤剩余量，已成交不可回滚，禁止同户自成交与值守关联单 |
| “每个封闭循环不得获利”、佣金是泄漏 | 错误否认正常价格收益，统计口径混乱 | 世界黄金守恒；佣金是内部转移，验证不存在机械造钱 |
| 把 200 tick 行情写成 24 小时 | 时间窗和 UI 数据错配 | 用游戏小时配置统一转 tick，24 小时为 1440 tick（默认 dt） |

保留真人撮合、双向资源交换、家庭保护、有限挂单和可选关系破平。上述参数未做运行平衡实验，不声称能降低死亡率或保证每张订单成交。

## 2. 代码基线与模块边界

| 已核验文件 | 当前事实 | 接入建议 |
|---|---|---|
| [strategy.rs](crates/sim_core/src/spatial/decisions/strategy.rs) | `ActiveTask` 包含 intent/strategy/primitive，策略类型派生 Copy | 新增携固定 ID 与阶段的 InternalTrade、CollectMarketCargo、ServeMarketplace；不塞 Vec 破坏 Copy |
| [intent.rs](crates/sim_core/src/spatial/decisions/intent.rs) | 已有 SatisfySurvival、StockHousehold、AcquireGold | 复用 L1；值守为 AcquireGold 的 L2，不增加职业强派分支 |
| [market.rs](crates/sim_core/src/spatial/decisions/market.rs) | 外部采购通过布尔资格/偏好选策，当前限成年男性户主 | 新的统一候选明确区分内部紧急授权和外部现有资格，不全局放宽外部规则 |
| [world_tick.rs](crates/sim_core/src/spatial/world_tick.rs) | 九阶段；代谢阶段已有死亡行囊处置；tick_subphase 共用 phase | 交易执行放 POI 交互阶段；身份失效在源事件同步处理，无第二套 tick |
| [journal.rs](crates/sim_core/src/spatial/ledger/journal.rs) | 余额与转账使用 f32，LedgerRef 无市场账户 | 增加托管流水引用；受检事务适配器不直接信任通用 transfer 可承担事务原子性 |
| [world_save.rs](crates/sim_core/src/spatial/world_save.rs) | agents 整体序列化，世界字段分别映射 | Agent 引用与世界市场/预约/资产记录同档保存，load 校验引用 |
| [world.rs](crates/sim_core/src/spatial/world.rs) | 世界构造显式初始化模块 | 初始化空托管与可选场地，不发放启动资产，不改变旧 RNG 消费顺序 |

新增建议目录 `spatial/marketplace/`：`mod.rs`（调度与接口）、`types.rs`（领域类型）、`custody.rs`（托管/资产桥）、`orderbook.rs`（排序匹配）、`quote.rs`（纯报价）、`settlement.rs`（原子提交）、`clerk.rs`（岗位租约）、`lifecycle.rs`（到期/产权变化）。按实现复杂度拆分，每文件少于 800 行，新增局部 AGENTS.md 并登记代码地图。

决策侧新增 `decisions/internal_market.rs`，负责候选与阶段推进；家庭保护/预约随首个消费者落地为共享小模块，不复制到市场私有库存。位置和装卸复用 `ecology/` 交互契约；外部榷场再生和交易不改归属。

## 3. 数据模型与唯一真相源

以下是逻辑结构，具体 Rust 类型沿用现有 ID 定义；禁止将概念名误当作已经存在的 API。

| 结构 | 关键字段 | 权责 |
|---|---|---|
| Marketplace | id、node、管理归属、营业状态、clerk_lease、books[4]、custody、next_order_id、next_execution_id | 世界唯一市场实体；不重复挂一份 POI 库存 |
| CustodyAccount | 稳定所有者引用、available[5]、locked[5]、commission_remainder | 市集资产唯一余额；订单不持有第二份资产 |
| Order | id、owner、principal_agent、授权 ID、resource、side、limit、remaining、filled、placed_tick、expires_tick、TIF、reserved_receipt | 全额担保，保留提交委托人用于审计和关系 |
| TradePlan | plan_id、actor、原受益主体、授权、目标/预算、阶段载荷、订单引用、最迟退出 tick | 活动计划存于世界稳定 ID 表，ActiveTask 持 plan_id 和唯一阶段；表不再复制阶段 |
| CargoProvenance | actor、resource、amount、owner、authorization_id | 行囊归属的分项元数据，不是第二份库存；分项总和不超过 carried_* |
| Reservation | id、owner、actor、purpose、remaining[5]、有效期 | 未扣账承诺；扣入行囊后等量减少 |
| ClerkLease | actor、开始/截止 tick、申请 ID、收益统计 | 租约只是任职权；是否服务还须检查 ActiveTask、位置与身体状态 |
| Execution | execution_id、双订单 ID、q、price、gross、fee、双方与售货员、tick | 审计记录，有界历史，不作为重放余额的来源 |

`owner` 与 `principal_agent` 不同：前者决定产权，后者决定授权和关系事件；人物换户不自动把原家货物带走。冻结按 order_id 可对账，`sum(各单占用)==账户 locked`。无家户者可拥有 Personal 类型托管账户；家户解散后迁移到继承结果或 PublicGranary，不能留下悬空可扣账 ID。

活跃订单按每户和全场限额约束；托管按主体合并，空账户删除，有资产账户不能因清理历史而删除。完单只保留有界流水；关系事件所需完成标志与活跃订单共同保存至订单退出，不靠扫描历史去重。

## 4. 数值、撮合与报价

### 4.1 结算精度

市场内部建议采用非负整数计量：资源最小量为 1/1024 单位，黄金最小量为 1/1048576 金；价格整数表示“每资源最小量需多少黄金最小量”。例如价格整数 102 对应约 0.09961 金/资源单位。量和价格用 u64，乘积用受检 u128，拒绝越界及非正限价。量化尺度为协议常量，不能热调；经济参数仍走配置。

一笔 `N = q * p`，其中 q 为资源最小量个数、p 为单个最小量黄金报价。买单冻结 `remaining * limit`。匹配后差额 `q * (limit - p)` 释放，剩余冻结量可精确复算。

卖方账户用跨订单费率余数防拆单舍入：`x = N * fee_bps + old_remainder`，`F = min(N, floor(x / 10000))`，`new_remainder = x - F * 10000`。净收入 `N-F`。余数是计费状态，不另算资产；正常账户小于 10000，继承合并后可暂时超过此值并由后续收入摊销。开户不能重置既有所有者余数，分家时随对应权益按比例分配、尾差归稳定 ID 最小受益人，合户时受检相加，实际补扣最多来自下一笔成交且不得超过收入。没有下一笔收入不凭空追缴。F 可为零，禁止最低手续费把小单扣成负数。

**既有 f32 不能直接宣称逐比特全世界强守恒**。边界适配器先计算源扣减和目标增加后的实际 f32 值，用 f64 核对实际差额是否等于拟转整数资产；只有两侧完全可表示且非负才提交，否则缩小到可表示数量或返回 PrecisionLimited，剩余留原处。禁止“账本扣近似值、托管加理想值”。对零余额和大余额、极小费用、反复存取做临时验证；不能满足可用性时须先调整统一资产精度设计，不用 epsilon 吞掉差额。

售货员佣金逻辑归其在市集的个人权益；能精确入随身黄金时同事务入袋，否则先留个人可领取托管，并显示“佣金待领”。这只是精度/容量受阻的延迟领取，不能产生第二份应付资产。封闭市场整数转账逐单位守恒；世界汇总用 f64 比较实际旧余额，生态代谢自身的浮点误差单列。

### 4.2 事务执行

每次操作先生成 Delta，校验完所有扣增、权限、容量、价格及数值，再统一 apply；不先扣金再尝试入货。失败不改变任意账户或订单。

1. 到期/失效撤单先于撮合；`tick >= expires_tick` 的订单本拍不可成交。
2. 收集前一决策阶段已提交且本拍仍合法的请求，按 `(submitted_tick, actor_id, request_seq)` 执行，分配单调订单 ID。决策观察和报价不能分配 ID。
3. 买单稳定排序；对每张买单选择最优合格卖单，排除同所有者、同户、当前售货员关联单及无效授权。跳过不合格对手，不在第一对失败时结束全市场。
4. 买价≥卖价，按较早 `(placed_tick, order_id)` 一方限价成交；数量取双方剩余最小值。入买单时已预留接收空间，执行仍复核。
5. 原子变更冻结、可用资产和 filled/remaining，支付售货员费用，产生唯一 execution_id。任意单剩余归零独立完成。
6. 立即成交单（IOC）剩余本拍取消；普通单保留至到期/取消。每拍限制比较次数与成交笔数；耗尽预算时从持久化游标续扫，避免头部无对手单永久饿死尾部。

接收预留采用保守规则：每品类 `当前托管实物 + 全部未成交买单预留 <= 容量`；挂单入场和买单都检查。成交所有权转移不新增场内实物，买单对应预留减少。保守预留可能拒绝本可容纳的交易，这是首期可解释的容量代价，不放开无限买单。

危机 IOC 在预算不足以执行时返回 ServiceBusy，不把被预算截断误报为市场无货；估计排队时间包含比较和撮合上限。新的普通单不得永久插队已提交请求。

### 4.3 只读报价和普通限价

`quote_buy/quote_sell` 在局部只读副本上调用与执行相同的匹配/舍入函数，返回分档执行、总支出/净收入、最低目标覆盖、容量和服务限制；原市场不变、无 RNG。模拟包含同户与售货员排除。

普通卖单：参考价来自最近有效成交，否则四品类初始配置；换算到整数价格格点向上取整，最低 1。若当前买价更高则用可执行买价；若要筹够购买资金，限价/出售量须据净收入反推并受可售上限约束。普通买单：参考价向下取整且至少 1，预算单价上限为 `floor(可用金 / 目标量)`；有卖盘且预算可覆盖则以覆盖目标量的最差卖价作限价，否则参考价与预算上限取小。低于最小格点则不挂单。

普通过期重报可按配置改价步长向对手价靠近，但不能突破冻结预算或本次最低净收入；新单重新排队。过期参考价按配置窗口失效，回退初始参考价；不会按外部现价自动钉住内部价格。具体默认幅度在平衡阶段校准。

两腿候选按“卖出筹金→买入”在同一临时副本顺序模拟，使用第一腿实际扣佣收入，不能把同一对手黄金花两次。执行仍分腿；首腿成功、次腿失败属于合法部分结果。报价输出区分“可立即执行”和“无对手待挂单”，后者不进入危机可行候选。

## 5. 决策、时间与生命周期接入

### 5.1 策略阶段

InternalTrade：`ReturnForCargo → Load → Travel → Deposit → SellOrBuy → CollectOrConsume → Return → Unload`。直接持金者跳过不需要的阶段；阶段只由 ActiveTask 保存，计划表存预算等载荷。任务结束后普通被动订单留市场，人物只保留可重建的权益索引，不保留第二个运行任务。

ServeMarketplace：`Apply → Travel → Serve → Leave`，来源必须是合法 AcquireGold 分支。空缺申请有到场截止时刻，超期租约释放；先通过生理/住宅等原守卫，再比较预计单位时间收入。冷启动允许一次配置化的短试岗，以实际收益更新下一次选择；无收入不得无限续约。clerk_score 用现有属性值，稳定 ID 破平，无全人口抽签或强制派工。

CollectMarketCargo：来源是原有资源或黄金需求，在托管有合法可用权益时生成；到场取回再正常返家。关闭场地的领取节点仍可作为目的地。取货失败时保留权益，返回原因交决策器重选。

### 5.2 tick 接线

- 代谢、死亡归集保持当前阶段顺序；死亡事件先撤销相关市场授权并登记受益权迁移，随身货物仍仅由 `settle_death_cargo` 处理一次。
- POI 交互阶段固定顺序：市场到期/资格复核 → 到场交存/领取和服务请求 → 有限撮合 → 同场已授权自救消费。消费参数复用现有规则，不能因成交绕过代谢上限。
- 运动和决策仍按当前位置和错峰节拍执行；本拍决策写下请求，下一 POI 阶段结算。报价时间计入此延迟，不能为救命临时插第二次全局决策。
- 决策提交与后续账本阶段的成婚、分家、家户解散处，在旧主体删除前同步失效委托并迁移托管权益；这些接口只改产权与授权，不派新任务。
- cleanup 清理无资产终态索引和有界流水；tick_subphase 必须复用上述 phase 方法，禁止绕过市场执行。

### 5.3 中断矩阵

| 触发 | 订单/预约 | 实物/任务 |
|---|---|---|
| 普通主动离场 | 可保留已有普通授权单；释放未提取预约 | 任务结束，已交存物留场内 |
| 高需求抢占 | 取消本任务剩余挂单及预约 | 行囊留身，托管待领；当前位置合法重路由 |
| 人物死亡 | 取消该人的委托，标记已处理事件 | 个人权益继承；家庭权益不变；不混入死亡行囊重复入账 |
| 成婚/分家/解散 | 原授权失效，冻结转可领取 | 依据社会结算的产权结果拆分/迁移权益，位置不变 |
| 售货员离岗 | 暂停撮合，岗位释放 | 仍可撤单、到期和到场领取 |
| 关市/关闭开关 | 全部剩余订单撤销 | 保留回收模式及领取节点，资产清空后才允许移除 |
| 行囊满/精度受限 | 不重复成交、不重发退款 | 分批领取或留下权益；返回具体失败原因 |

## 6. 配置、存档与表现层

### 6.1 配置建议

以下参数随真实消费者分批加入，默认值是待验证初值；共同前缀 `internalMarket`（Rust 对应 snake_case）。所有时间用游戏小时，`ceil(hours / simulationDt)`，不调整 simulationDt。

| 参数后缀 | 建议初值/范围 | 消费点 |
|---|---|---|
| Enabled | false；布尔 | 初始化、候选、关闭回收 |
| CommissionBps | 30；0～1000 | 每笔佣金；新成交用当前费率，旧收入不重算 |
| InitialPrices | 四品类正数；水粮 0.1、木 0.15、石 0.2 金/单位 | 空簿参考；不是已有市场默认值 |
| OrderLifetimeHours / QuoteMaxAgeHours | 24 / 24；正数 | 新订单 expires、旧价过期 |
| MaxOrders / MaxOrdersPerHousehold | 128 / 4；正整数 | 入单门禁，无家户按个人计 |
| CustodyCapacityPerResource | 400；正数 | 四资源场内实物与买单接收预留 |
| MaxMatchesPerTick / MaxComparisonsPerTick | 16 / 256；正整数 | 有限调度和报价服务时间 |
| ClerkShiftHours / ClerkTrialHours | 8 / 2；正数且试岗≤班次 | 值守退出及空场试岗 |
| FailureCooldownHours / RepriceBps | 2 / 500；正数 / 0～10000 | 普通失败冷却和到期重报 |

加载/配置注入验证有限数、正值、组合约束和量化可表示性。缩容低于已有占用时进入只减不增模式，不删除订单/资产；已有订单保留原 expires，热改时长只影响新单。其他运输、成人年龄、身体恢复、保护线和安全余量优先复用现有或融合契约参数；实施发现缺失再集中新增，不写业务字面量。

同步 `config.rs` 字段及注释、`config.js`、`examples/config.json`、`tools/config-check.js` IMPACT_OVERRIDES 和真实读取点；自动生成配置参考，不手改。新增配置不随意让旧世界开局获得额外黄金/资源。

### 6.2 存读档与快照

WorldSave 保存市场实体、托管与手续费余数、订单、接收预留、请求队列/序号、计划/预约/行囊归属、岗位租约、调度游标、行为冷却和 next IDs。只有可重建索引及纯展示历史可不保存；影响最近价格和报价过期的行情状态必须保存。

读档先验证 ID 唯一/计数器未回退、金额与格点、冻结与订单相等、引用合法、产权与容量预留一致，再重建索引；损坏档明确拒绝，不能通过补差额或重放成交“修好”。在每个交易阶段以及部分成交、休市、继承待领取处验证续跑确定性。

结构不兼容时按实际情况升级 SAVE_FORMAT_VERSION 及前端存档门禁；应用版本通过 bump 工具升级并重建 WASM 双副本。本次文档不改任何版本。

观察接线覆盖 `snapshot.rs → world_snapshot.rs → snapshot_bin/encode.rs → snapshot-bin.js + rustworld.js`；字典新增同步 `dict.rs`，section/layout 变更同步 FABS 格式版本与读取工具。64 位订单/执行 ID 使用可精确编码形式（字符串或分段整数），不可直接 cast 为超出 JS 安全范围的 Number。驻留表重置仍按 start_index==0。

前端新增市集 Inspector 模块、地图符号与拾取入口，避免复用外部榷场库存字段；人物任务解释同步 observation/projection 和决策视图。高频内容必须缓存，Chrome 验证建立存档、推进、暂停、查看、关窗跟随及重新加载。展示范围和文案以产品稿 §7 为准。

## 7. 可执行任务序列

每项完成后留下 diff、验证命令与结果；临时验证脚本提交前删除。P0/P1 中间版本保持功能开关默认关闭，不能把构造数据演示误报成自然闭环。

| ID | 依赖 | 改动与交付物 | 完成条件 |
|---|---|---|---|
| IM-01 基线与接口 | 无 | 读局部指南；核定产权事件、f32 桥、场地路网、配置消费表；记录禁用基线 | 每个新写入有唯一源和 tick 归属；精度不可用则先解决后续前置 |
| IM-02 资产与托管 | 01 | types/custody、流水引用、行囊来源、受检 Delta | 提货/交存/领取任意失败不部分扣账，整数与 f32 往返无增益 |
| IM-03 订单与结算 | 02 | orderbook/settlement、容量预约、手续费余数、限价差退款 | 多档部分成交、IOC、同户禁单、预算耗尽均有限且可复算 |
| IM-04 报价与空簿 | 03 | quote 复用结算、初始参考价/改价、普通挂单与危机区分 | 无对手可挂普通单；两卖单无金不成交；冻结簿报价与执行一致 |
| IM-05 场地与产权生命周期 | 02、03 | 场地生成、关闭回收、死亡/成婚/分家/解散接线 | 全部异常点资产仍可合法领取，不远程复制，不复用资源再生 |
| IM-06 P0 持久化 | 03、05 | WorldSave、校验、队列/游标与 ID 恢复 | 部分成交/到期/继承中间存读档与连续推进一致 |
| IM-07 家庭保护与装卸 | 02、05 | 共享保护/预约、成年受限自救、按速率搬运及现场消费 | 同户并发不超支，家庭消费抢占预约，无家者现场自救不开放采集囤货 |
| IM-08 自主售货员 | 04、05、06 | AcquireGold 策略、申请仲裁、在途租约、试岗/退出、佣金 | 自然人物自行到岗；无收入退出；生理抢占不被岗位拦截 |
| IM-09 内部交易与取回策略 | 04、06、07、08 | ActiveTask 阶段、两腿交易、普通离场托管、取回及抢占 | 持金采购与无金卖货筹金各有真实供需闭环；失败保留实际结果 |
| IM-10 统一 L2 选策 | 09 | 接入融合候选，自用/采收/内外部成本与真实路径时间 | 更便宜但来不及不能胜出；外部资格不被无意放宽，原 b13 守卫保持 |
| IM-11 存档全链与观察 | 06、08、09、10 | 全部新增行为态随档；FABS、Inspector、任务解释和容量原因 | 所有阶段续跑一致；Chrome 可看到现货/权益区别和真实失败原因 |
| IM-12 好感度适配（可选） | 03、11；另需 AF-10/11 接口就绪 | 同价同时间关系破平、真实成交事实和冷却 | 仅构造同价同时间案例应改变对手；非同价不变、拆单不刷分 |
| IM-13 平衡与交付 | 01～11；启用好感时追加 12 | 固定种子配对实验、性能报告、文档迁移、版本及 WASM | 守恒/确定性/长期门禁通过；没有无解释的资产偏移、岗位饿死循环或无界增长 |

主序列：`01 → 02 → 03 → 04/05 → 06/07 → 08 → 09 → 10 → 11 → 13`。斜线表示依赖允许分别推进，并非要求启动并行智能体。IM-12 不阻塞不带好感的首期交付；自营、多市集、信用和税制扩展不属于 IM-13。

## 8. 验证场景与门禁

### 8.1 临时经济/生命周期验证

| 场景 | 必须观察到的结果 |
|---|---|
| 甲石、乙水且双方零金 | 不成交、不造金，可到期取回 |
| 乙带真金买石，甲再买水 | 两笔费用分开；售货员收入+双方黄金总额不变 |
| 买限价高于旧卖单限价 | 按旧卖价成交，差额可领，余单仍全额担保 |
| 多档部分成交后撤销 | filled 不回滚，仅剩余冻结释放，单边完成独立判断 |
| 拆单、重复请求、极小金、超大余额 | 无手续费余数重置获利，无重复领取，精度失败不扣资产 |
| 同户双向单、售货员家户单挡在簿顶 | 不对敲；其他合格交易仍能继续 |
| 空簿且有人有钱有需求/有人有盈余 | 普通双侧挂单能由人物自主产生；无人愿来时如实零成交 |
| 全额托管未领取、配置缩容 | 不删除资产，新增承诺受阻，领取仍工作 |
| 危机首腿后目标卖单被吃完 | 保留实际净金，退出/重选，不恢复已售货、不等待到死 |
| 售货员到场前、饥渴退出、死亡 | 无幽灵营业；期限与取货独立继续 |
| 抢占、家庭吃掉预约、同户多人同拍装货 | 不超额扣账；已携货不远程退回 |
| 死亡、转籍、分家、解散、关市 | 货物位置不变，受益权一次迁移，无永久悬空引用 |
| 任意阶段存读档/分批推进/只读报价 | 同版本同配置同种子续跑一致，观察无行为副作用 |

### 8.2 长程与性能

同种子、同初始资源黄金、同配置配对“内部市场关闭”“内部市场启用”；若加入公仓再分配对照，明确其物资转移与劳动成本，不能暗赠启动资本。覆盖多季节至少三代、多个种子并单列灭绝样本；比较死亡、实际救急后存活、家族存续、财富集中、自然生产损失、值守空缺、订单等待、托管税负偏移及外部净流出。

记录每 tick/每阶段耗时、峰值订单与托管主体数、报价比较次数和内存；按相同人口/世界规模配对基线。所有活跃簿扫描有上限，普通订单大量占用时危机不能无解释饿死。平衡数值须有实验记录后再启用默认开关，不把“有交易”当作“改善生存”。

实现时按变更范围运行现有 WASM release 编译及双副本同步、`test-wasm.js`、`test-determinism.js`、`snapshot-check.js`、`config-check.js`、`frontend-check.js`、`diagnose.js --check all`，性能复用 profile-benchmark。只使用临时断言验证专项场景，不提交新增持久化单元测试。

每批实现同步对应 current 模块文档、局部 AGENTS、代码地图及 changelog；通过 `doc-maintenance-check.js`、`cross-doc-check.js`、`doc-link-check.js`、`code-map-check.js`、`bump-version.js --check`。统一 bump 工具升版，若变更 world_save.rs，必须重新编译并同步两个 WASM 文件。

本次仅产品审查与文档导出，执行文档一致性门禁，不编译、不升版、不宣称上述 IM 任务已经实现。
