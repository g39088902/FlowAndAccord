# TA-08 · 遮挡边界实测与验收矩阵（重定向：并入全量 GPU 迁移）

> **状态**：◐ 已重定向（2026-09-17）。原 Canvas 2D 覆盖层内近似遮挡方案（策略 A/B/C 及阶段二~五）**已取消**——架构决策已定为**全量 WebGL 渲染、不再使用 Canvas 2D**（见 [31 号迁移方案](docs/plan/tech/31-canvas-to-webgl-migration.md)）。本任务收缩为：**旋转切片实测（前置诊断）+ 遮挡验收矩阵（迁移验收标准携带者）**，遮挡问题的技术解由 WebGL 深度缓冲逐像素承担。
> **难度**：中（仅测量脚本与证据包，无覆盖层渲染改造）
> **依赖**：TA-05 → TA-06 → TA-07（LOD + 模型缓存 + 剔除）→ TA-08 实测 → TC-03（31 号迁移方案阶段三~五）
> **目标**：在不改渲染代码的前提下建立遮挡问题的真实证据包，量化「树冠遮人、岩壁压路、画家算法局限」的严重度与占比；该证据包同时充当全量 GPU 迁移的 before/after 基线。★ 历史口径（v1.50.77 起双 Canvas 架构：地形 WebGL + 实体 Canvas 2D 覆盖层）仅在测量时适用；迁移完成后同一矩阵直接复用于 WebGL 管线验收。

---

## 1. 为什么重定向

### 1.1 原方案作废原因

原 TA-08 设计了三条 Canvas 2D 覆盖层内的近似策略（模型内排序 / 实体拆有限子项 / 屏幕空间半透明兜底），其成立前提是「覆盖层长期存在」。2026-09-17 架构决策改变此前提：

1. **accent 不能单独迁**：树迁入底层 WebGL 画布后，留在 2D 覆盖层的族人永远画在树之上，树冠无法遮挡人——遮挡反向恶化。正确做法是把所有需要互相穿插的实体（装饰 + 族人 + 房屋 + 道路 + 标签）整体迁入同一 WebGL 管线，即 31 号方案阶段三~五；
2. **深度缓冲是遮挡的最终解**：WebGL 逐像素深度测试天然解决模型内穿插与跨实体遮挡，Canvas 2D 上的任何近似都是迁移后必须丢弃的一次性投入；
3. **2D 回退不再是长期形态**：迁移完成后 2D 覆盖层与 `fallback-handler.js` 回退管线一并退役，无需为回退模式单独做遮挡优化。

### 1.2 保留内容

| 保留项 | 去向 |
|--------|------|
| 阶段一旋转切片实测（`tools/occlusion-test.js` + 证据包） | 本任务保留，作为迁移前置诊断与基线 |
| 验收矩阵（一树一屋一人 / 模型内排序 / 性能预算） | 本任务携带，迁移完成后逐条复测 |
| 「最小遮挡样板」验收条目 | 仍登记于 [07 号 §11.2](docs/plan/tech/07-terrain-art.md)，归属不变 |

| 取消项 | 理由 |
|--------|------|
| 策略 A：局部三维模型内排序 | Canvas 2D 专属近似，WebGL 深度缓冲天然覆盖 |
| 策略 B：实体拆有限子项（`DEPTH_TREE_*`） | 同上；且迁移后子项拆分无意义 |
| 策略 C：屏幕空间半透明兜底 | 选中遮挡补偿改为 WebGL 关深度测试描边 pass，在迁移任务内实现 |
| 原阶段二~五子任务表（TA-08-10 ~ TA-08-43） | 全部随策略取消；性能回归改由 31 号方案性能门禁承担 |

---

## 2. 遮挡验收矩阵（迁移复用版）

迁移前测一次（Canvas 2D 现状基线），迁移后测一次（WebGL 目标形态），同一矩阵逐条对照：

| 场景 | 视角 | 通过标准 |
|------|------|----------|
| 一树一屋一人 | 四方位 × 低/高俯角 × 远/中/近景 | ① 族人在树后不被完整遮挡（半透明叶簇下可见轮廓或选中高亮）；② 选中信息卡片始终可读；③ 岩壁不压盖近路与取水点 |
| 模型内排序 | 连续旋转相机 | 枝簇按实际世界空间深度正确穿插，非简单「全前/全后」分层 |
| 性能预算 | 高密度聚落拖动 | 遮挡相关处理（含选中描边 pass）p95 增量 ≤ 3 ms（§11.3 口径） |

WebGL 侧实现要点（归 TC-03 / 31 号方案阶段三~五，不在本任务内）：

- 选中实体被叶簇遮挡时：关闭深度测试补一轮描边 pass + 叶簇半透明化，替代原策略 C；
- 枝簇穿插：GPU 深度缓冲逐像素解决，无需排序；
- 地形-实体遮挡：同一管线内统一深度，岩壁压路自然消除。

---

## 3. 任务序列（收缩版）

### 3.1 阶段一：旋转切片验证（2 人日）

**目标**：建立基线数据，确认问题是否真实存在及严重程度。

| 子任务 | 文件 | 内容 |
|--------|------|------|
| TA-08-01 | `tools/occlusion-test.js` | 脚本生成旋转镜头路径（0°→360°/5°步进），自动截图至 `assets/ta08-evidence/rotation-slice-{angle}.png` |
| TA-08-02 | `docs/plan/tech/assets/ta08-evidence` | 选取固定种子 42、tick 1,000,000、季节夏、zoom=1.2，记录问题截图 |
| TA-08-03 | `docs/plan/tech/07-terrain-art.md` | 更新 §11.2 待验收清单，追加「最小遮挡样板（一面岩壁、一座房屋、一棵树、一段道路、一个族人）」条目 |

**交付物**：证据包目录 + 诊断报告（明确问题类型与占比）。诊断报告同时回填 31 号方案阶段三方案细化（装饰层迁移批次与 LOD 分级依据）。

### 3.2 阶段二：迁移后复测（随 TC-03 触发，1 人日）

迁移阶段三~五完成后，用 §2 同一矩阵在 WebGL 管线复测；选中描边 pass 的 p95 差分走 `tools/profile-benchmark.js`，结果登记 07 号 §11.3，并将本文件状态置为 completed。

---

## 4. 工程约束

- 测量脚本只读渲染状态，**不修改**任何渲染文件、`WorldRng` 消费顺序与快照契约；
- 截图需固定种子、存档、窗口尺寸与 DPR，保证 before/after 可比；
- 证据包随 `docs/plan/tech/assets/` 归档，`doc-link-check.js` 可达。

---

## 5. 关联任务与依赖

| 依赖 | 说明 |
|------|------|
| TA-05 | P0 植被样板闭环，四季/受光已通过，LOAD 待补测 |
| TA-06 | 植被外观变体（三类乔木/三类灌木轮廓） |
| TA-07 | 细节分级 LOD + 完整投影包围体剔除 + 局部几何模型缓存 |
| TB-01 | 多尺度噪声与支脊（内核侧，决定地貌复杂度） |
| TC-03 | **全量 GPU 迁移（已定为实现路径，非条件触发）**：装饰/实体层整体迁入 WebGL 管线，迁移完成后退役 Canvas 2D 覆盖层与 2D 回退管线，见 [31 号迁移方案](docs/plan/tech/31-canvas-to-webgl-migration.md) |

---

## 6. 风险与未知数

| 风险 | 概率 | 缓解 |
|------|------|------|
| 实测发现遮挡问题占比极低 | 低 | 结论仍有价值：为迁移后的矩阵复测提供空基线，TA-08 可提前关闭 |
| 迁移排期晚于实测很久，基线过期 | 中 | 测量脚本确定性运行（固定种子/tick/相机路径），随时可重跑 |
| WebGL 下选中描边 pass 超预算 | 低 | 仅选中态启用；叶簇半透明化可用 stencil 替代 |

---

## 7. 参考资源

> 路径基准：本文件位于**仓库根目录**，下列相对路径自根起算。

- [31-canvas-to-webgl-migration.md](docs/plan/tech/31-canvas-to-webgl-migration.md)：全量迁移方案（阶段三~五为本任务的下游承接方）；
- [07-terrain-art.md §6.7](docs/plan/tech/07-terrain-art.md#67-模块拆分-模型缓存与遮挡边界)：遮挡边界设计细则出处；
- [TA-05 验收记录](docs/plan/tech/assets/ta05-evidence-2026-09-14/report.md)：当前植被样板的性能基线；
- [render_depth_queue.js](frontend/js/render_depth_queue.js)：统一深度队列实现（现状，迁移对象）；
- [render_accents.js](frontend/js/render_accents.js)：装饰绘制层源码（现状，迁移对象）。

---

## 8. 下一步行动

**建议**：立即启动阶段一（旋转切片验证），在不改代码的情况下先建立真实问题的证据包；证据包结论直接作为 31 号方案阶段三（装饰层迁移）方案细化的输入。

**负责人**：待分配
**预计开始时间**：2026-09-15（波次二第一周）
**里程碑**：2026-09-21 前完成阶段一诊断，支撑迁移排期。
